<!-- project-tasks: prefix=PG lastId=222 -->
# PROJECT TASKS

Updated: 2026-09-23 · Open: 55 (P1: 0) · In progress: 0

## GitHub Issues
- [ ] `PG-221` -> #447 **P4** [fix] `scripts/uitests.sh` passes an argument that is not a `-only-testing:` flag straight to `xcodebuild`, which then dies with «Unknown build action» and the script reports «nessun test eseguito: è un errore» without naming the cause. Observed 2026-09-23 invoking it as `scripts/uitests.sh PergamenumUITests/WikilinkNavigationUITests` — the class-path form CLAUDE.md's «an argument replaces the selection» wording reads as allowed; the usage comment at `scripts/uitests.sh:59` does show the flag form. Either reject a non-flag argument with that sentence, or accept a bare class path and build the flag — `scripts/uitests.sh` <!-- src:session kind:fix opened:2026-09-23 runs:4 promote:2026-09-23 -->
- [ ] `PG-220` -> #446 **P4** [fix] The four #188-era click-point resolution sites (`rightMouseDown`, `followLinkIfPresent`, `menu(for:)` in `CompletingTextView+Pasteboard.swift`, and their `FormattingTextView.swift` twins) each resolve a point to a link character index by hand instead of going through the one `linkCharacterIndex(at:)` helper the #191 chain added — four copies of the same three lines, which is how two of them drift apart. Deliberately deferred by that chain rather than overlooked: folding them in would have widened a bugfix diff into a refactor of code the defect did not touch — `Sources/Features/Editor/CompletingTextView+Pasteboard.swift` · `Sources/Features/Workspace/FormattingTextView.swift` <!-- src:session kind:fix opened:2026-09-23 runs:4 promote:2026-09-23 -->
- [ ] `PG-219` -> #445 **P2** [correctness] An `NSCursor` set from AppKit code hosted in this app's SwiftUI view tree never reaches the screen, app-wide: the pointing hand over a link and the ordinary I-beam over any editable text are both absent, and the I-beam apparently always has been. Three mechanisms were implemented and instrumented during the #191 chain — `resetCursorRects()`/`addCursorRect`, `mouseEntered`/`mouseExited` with `NSCursor.push()`/`.pop()`, and `cursorUpdate(with:)` with `.set()` — each confirmed by logging to fire correctly and repeatedly, none changing the visible cursor; a whole-bounds `.crosshair` rect showed nothing either. The decisive measurement: `NSCursor.current` reads the pushed cursor for every tick of a real hover while the screen draws the arrow, so AppKit's own bookkeeping and the Window Server have come apart, and `NSCursor`'s public API has no way to query or correct that from inside an override. Every working custom cursor in this codebase (`BoardHandles.swift`, `BoardCropEditor.swift`, `WorkspacePaneDivider.swift`, `EditorColumns.swift`) goes through SwiftUI's own `.onHover`, which is where a fix likely lives — for `NoteTextView`/`FormattingTextView` that means restructuring around `.onContinuousHover` across ~40 forwarded properties and five call sites, hence an ADR rather than a patch. The `cursorUpdate` code is left in place, correct and harmless. Related but not a duplicate: `PG-089` (closed won't-fix) was the same symptom through a different mechanism, `NSDraggingSession`'s own operation mask — `Sources/Features/Editor/CompletingTextView+CursorRects.swift` · `Sources/Features/Workspace/FormattingTextView+CursorRects.swift` <!-- src:session kind:roadmap opened:2026-09-23 runs:4 promote:2026-09-23 -->
- [ ] `PG-216` -> #433 **P3** [roadmap] Concurrent Claude Code sessions on this repo repeatedly race the same `TODO.md`/GitHub-issue-promotion window: at least 3-4 sessions run `/project-tasks`+`/ship` cycles in parallel, causing recurring `TODO.md` merge conflicts (several "resolve TODO.md merge conflict with origin/main" commits already in history) and a duplicate GitHub issue promotion for the same ledger entry (PG-211 promoted independently to both #415 and #417 on 2026-09-22/23, #417 closed as duplicate). Proposed fix: a `.pergamenum-todo.lock` file (gitignored, PID+timestamp+branch), acquired by `project-tasks` Step 1b/2 before reading the ledger and released after Step 7's write or on abort; a session finding it held waits with backoff (as already done for `xcodebuild` contention) or aborts with a clear message rather than writing blind; a stale-lock timeout (~20 min) covers a session that dies mid-run. Scoped to the `project-tasks` skill only, no Swift source touched. Does not address the separate `xcodebuild`/DerivedData build-system contention (`PG-214`), which needs its own fix (isolated DerivedData per worktree, not a ledger lock). <!-- src:session kind:roadmap opened:2026-09-23 runs:4 promote:2026-09-23 -->
- [ ] `PG-211` -> #415 **P3** [fix] `VaultController.reconcile` (`Sources/App/VaultController+Watching.swift:28`) opens with `guard var note = openNote, note.relativePath == change.path else { continue }`, so an external change to a dirty tab that is not the focused note — another column, or a background tab in the same column — is dropped entirely: no reload for a clean buffer, and no ADR-0001 §D3.4 conflict banner for a dirty one, which then overwrites the external change on its next save — `Sources/App/VaultController+Watching.swift:28` · Found while measuring ADR-0055 §Context (the note write guard chain). Not that chain's to take: it changes behaviour on every external-edit path in the app, not only on a rename. Named at `docs/adr/0055-note-write-guard-and-the-dead-rename-performer.md` §D5. Renumbered twice at merge — first from `PG-209`, which `main` had already assigned to an unrelated CI-trigger ticket, then from `PG-210`, which a separate concurrent `main` entry already held in `Done`. <!-- src:session kind:fix opened:2026-09-22 runs:4 adr:0055 promote:2026-09-22 -->
- [ ] `PG-209` -> #410 **P2** [fix] `pull_request` never triggered CI on PR #401, twice: GitHub's `check-suites` API showed no `GitHub Actions` suite at all for either head commit (`6b0253d`, then `75d66f50`) after several minutes, while same-day PRs from apparently the same session type got one normally; both times worked around with `gh workflow run CI --ref <branch>` (`workflow_dispatch`), which ran and passed cleanly — `.github/workflows/ci.yml` · Repo-level checks ruled out: Actions enabled (`actions/permissions` → `enabled:true`), workflow `active`, no `paths-ignore` match (Sources/Tests changed), no branch protection, no self-hosted-runner starvation (check-suite itself was never created, not stuck queued). Root cause not identified — possibly tied to how this session's git/PR auth (a GitHub App token, check-suite app name "Claude") interacts with Actions' webhook delivery, but that was not confirmed, only the symptom. <!-- src:session kind:fix opened:2026-09-22 runs:3 promote:2026-09-22 -->
- [ ] `PG-199` -> #389 **P3** [fix] `EmbedCaret.movingRightFromTheRunsLeftEdgeSkipsItInOneStep` failed once in a Stop-hook unit run with «test runner exited with code 0», not reproduced and not rerun by hand; same shape as `PG-193` and `PG-120`, and the failure is one run's evidence only <!-- src:session kind:fix opened:2026-09-21 runs:4 promote:2026-09-22 -->
- [ ] `PG-198` -> #388 **P3** [fix] Four paths of the stage-1 verdict have run only against a stubbed `xcodebuild` or `--self-test`, never a real run: the rerun of the failed tests, the launch log with a real installed copy, the `focus-taken` signal, and `external-monitor` with the LG unplugged (only the LG-attached case was seen, 2026-09-21); a green real run does not exercise any of them — `scripts/uitests.sh` · Two of the four now proven for real, 2026-09-23: a full run went red on 2 tests with signal `focus-taken` (Finder 6, GitKraken 3), the rerun of just those 2 tests went green, final verdict `green (full)` recorded for tree `4d11903` (see `PG-185`, closed by the same run). Still unproven for real: the launch log on a genuinely installed copy, and `external-monitor`. <!-- src:session kind:fix opened:2026-09-21 runs:4 promote:2026-09-22 -->
- [ ] `PG-192` -> #353 **P3** [fix] Gaps in the ADR-0052 door tests found by the review of #346: the §D8 ordering rule (door before sibling state) is not pinned for `updateTray`, `moveLedgerState` or `forgetLedgerState`, so reversing it stays green; `anUnreadableLedgerIsLeftByteIdenticalByEveryWriter` exercises the refusal branch with only two of its six writers (the other four are no-ops on an empty ledger, seed them so they would mutate); `PraticaLiveSyncTrashedMidRunTests` is at 345 of the 350 `type_body_length` error, so its next test needs a new suite — `Tests/PraticheLedgerDoorTests.swift` · `Tests/PraticaLiveSyncTrashedMidRunTests.swift` <!-- src:review kind:fix opened:2026-09-20 runs:2 promote:2026-09-20 -->
- [ ] `PG-191` -> #351 **P3** [correctness] `PraticheController.select(_:in:)` sets `selection` and `expansion` before `markOpened`, which goes through the ledger door: when the marker names another vault the door's reset clears `selection` right after it was set and the click is silently lost (low reach, needs a vault switch); fix by calling `markOpened` first and listing `select` in ADR-0052 §D8. Second variant: `updateLedger`'s `.stale` case is decided by session identity, not by file, so a vault closed and reopened on the same root during a run writes a stale result to disk without updating `self.ledger`, and the next `.live` writer saves its older memory over it (one re-sync lost, same as before PG-172) — route `.stale` to `.live` when `ledgerURL(for:)` equals `ledgerOrigin.url` — `Sources/Features/Pratiche/PraticheController+Ledger.swift` · `Sources/Features/Pratiche/PraticheController.swift` <!-- src:review kind:fix opened:2026-09-20 runs:2 promote:2026-09-20 -->
- [ ] `PG-189` -> #349 **P3** [fix] The `CLAUDE.md` working agreement to find the latest Debug build with `ls -dt .../Pergamenum-*/Build/Products/Debug/Pergamenum.app` is wrong on its own terms: `-t` sorts by the `.app` directory's mtime, which an incremental rebuild does not touch, so it returned a 09:10 build twice while the current one was 11:04 — `CLAUDE.md` · The reliable stamp is `Contents/MacOS/Pergamenum.debug.dylib`: `ls -t .../Pergamenum.debug.dylib | head -1`, then take its `.app`. Observed 2026-09-20 opening the gallery for a hand check; the stale build did not contain the gallery changes. <!-- src:session kind:fix opened:2026-09-20 runs:4 promote:2026-09-20 -->
- [ ] `PG-188` -> #348 **P3** [fix] `scripts/uitests.sh` does not handle a second monitor: with an external display attached the test windows open at negative y and 12 tests fail «not hittable» in 5-15 s, a red that reads as a real defect — `scripts/uitests.sh` · Full run 2026-09-20 (tree `9ef3cef`): 120 tests, 12 failures (DayView 3, Diary 1, TimeBlock 1, SidebarMove 4, WorkspaceBoard 2, WorkspaceIntegration 1), element frames at y -980, -530 and similar; the same 12 passed 12/12 after the external monitor was disconnected. Not the 60.2 s launch timeout of `PG-157`. <!-- src:session kind:fix opened:2026-09-20 runs:4 promote:2026-09-20 -->
- [ ] `PG-170` -> #318 **P3** [correctness] ADR-0050 §D6's named follow-up: `VaultSession+TagRename.renameTag` (`:79-85`), `+TaskDrop` (`:77-83`) and `+BoardDrop` (`:69-75`) still borrow-and-return `journalCommand` **and `journal` itself** by hand around an `await` — a second task overlapping one of them can mislabel a `command` and, for the `journal` swap, disarm the journal under the other task's write. Same hazard shape `PG-152`/ADR-0050 closed for the `operation` id, on a different property. Closing it means deciding whether `journal` (the armed net) is also task-scoped, which changes what «armed» means for the connector's `VaultAPI.arm`: its own ADR, not a patch → `docs/adr/0050-journal-gesture-travels-with-the-task.md` §D6 <!-- src:git opened:2026-09-18 kind:roadmap runs:4 promote:2026-09-19 -->
- [ ] `PG-151` -> #280 **P3** [architecture] Operator decision pending: add `Sources/Core/Vault/VaultBoundary.swift:VaultBoundary.url(for:)` to `.claude/protected-interfaces` — proposed by ADR-0041, deliberately deferred to "after this chain merges" (now true as of PR #254, 2026-09-13). The architect agent cannot write this entry itself; it is a manual operator action if wanted <!-- src:session opened:2026-09-13 kind:chore runs:4 adr:0041 promote:2026-09-16 -->
- [ ] `#204` PG-120: MailStoreReaderTests.publishReportsMailIsWritingOnATornCopyAfterOneRetry failed once during the full PergamenumTests suite, not reproduced in isolation <!-- src:github state:OPEN labels:bug -->
- [ ] `#208` PG-109: An unrecoverable followed conversation (its id gone after an index renumber, R-14) is reported via controller.report(...) only — no ledger persistence, no tray row <!-- src:github state:OPEN labels:bug -->
- [ ] `#214` PG-100: No test covers a Workspace .text card whose body contains both a real [[Wikilink]] match and a separate, coincidentally fold-matching quoted bullet on note rename <!-- src:github state:OPEN labels:bug -->
- [ ] `#215` PG-096: Sparkle: manual update-flow hand-check (R-08) still owed <!-- src:github state:OPEN labels:bug -->
- [ ] `#218` PG-014: M13 Vault completo — remaining scope: prompts in the vault, static export, import, AppIntents (rename/move/trash already shipped as PG-005) <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#219` PG-104: File-import drop closure has the same overload-resolution bug PG-101 fixed elsewhere <!-- src:github state:OPEN labels:bug -->
- [ ] `#220` PG-102: TableAttachment.swift's TextKit2 attachmentBounds callback reads a @MainActor-isolated NSView property from a nonisolated context <!-- src:github state:OPEN labels:bug -->
- [ ] `#221` PG-103: Three QuickLookPresenter.swift AppKit informal-category overrides are nonisolated but touch @MainActor state, uncovered by the file's own @preconcurrency <!-- src:github state:OPEN labels:bug -->
- [ ] `#224` PG-124: Three link-opening surfaces and the HTML exporter accept any URL scheme, while the editor's own MarkdownAttributedText.clickTarget already gates on http/https <!-- src:github state:OPEN labels:bug -->
- [ ] `#225` PG-125: The whole PergamenumRoute (capture text, search query, note path) is logged to the unified log with privacy: .public <!-- src:github state:OPEN labels:bug -->
- [ ] `#227` PG-127: Three PraticheControllerTests write MailStoreLocation.overrideKey into UserDefaults.standard, the shipping app's persistent domain, undone only by a defer <!-- src:github state:OPEN labels:bug -->
- [ ] `#228` PG-128: Guard tests that claim more than they check, and the copy-paste drift in the 21 UI-test launch harnesses <!-- src:github state:OPEN labels:bug -->
- [ ] `#230` PG-130: pergamenum://note?id=<id> can never succeed: RouteState.noteIDs is read and never populated, and no stable note id exists anywhere in the index <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#231` PG-131: «Svuota cache e ricostruisci» leaves every rendered thumbnail on disk and in memory, the Impostazioni problems list can never be dismissed, and a forgotten vault's tab session lives in UserDefaults forever <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#232` PG-132: SPEC §9's "capture must not raise the app" is described by PergamenumRoute.raisesApp but consulted by nothing <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#233` PG-133: Two command catalogues carry an SF Symbol nothing draws: pratiche top-bar buttons and calendar day-cell menu items render title-only <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#234` PG-134: Pratiche polish left half-wired: the «Rigenera» sheet never shows which attachments get rewritten, the attachment chip's context menu ignores its own title catalogue, and NoteName.sanitized is dead while its documented callers went elsewhere <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#235` PG-135: Both board floating panels take a viewport they never read, so the format bar and the wikilink completion popup cannot clamp themselves on screen <!-- src:github state:OPEN labels:enhancement -->
- [ ] `#236` PG-136: Dead state and dead tests that each need a yes/no rather than a refactor <!-- src:github state:OPEN labels:bug -->
- [ ] `#238` PG-138: Whole-vault task/link aggregations recomputed per redraw <!-- src:github state:OPEN labels:bug -->
- [ ] `#241` PG-141: Allocation-heavy parsing on the sync path and per-render filesystem stats in the timeline <!-- src:github state:OPEN labels:bug -->
- [ ] `#242` PG-142: Board gestures, and tests that re-read the tree <!-- src:github state:OPEN labels:bug -->
- [ ] `#244` PG-144: NoteTextView.Coordinator is the god object every editor feature since ADR-0018 landed on, and its decoration passes are copy-pasted twins <!-- src:github state:OPEN labels:bug -->
- [ ] `#246` PG-146: Over-limit Workspace types and a drop-feedback machine duplicated with Today <!-- src:github state:OPEN labels:bug -->
- [ ] `#247` PG-147: A second markdown grammar in the exporter, plus over-limit files and duplicated tables across Core, DesignSystem, Calendar and the app shell <!-- src:github state:OPEN labels:bug -->
- [ ] `#262` readDiary → writeDiary: a read-modify-write window wider than ADR-0043 §D8 can close <!-- src:github state:OPEN labels:bug -->
- [ ] `#343` [BUG] The shared UI verdict of #338 is proven on a real run for one partial verdict only <!-- src:github state:OPEN labels: -->

## Open Issues

- [ ] `PG-217` **P2** [fix] Manual GUI smoke test for PG-139 (list rendering, heading sizes, slash/emoji completion menus) not performed after the per-keystroke perf fix — no GUI/computer-use tool was available in the implementing session — `Sources/Features/Editor/MarkdownStyler.swift`, `Sources/Features/Editor/MarkdownAttributedText.swift`, `Sources/Features/Editor/RankableEntry.swift` <!-- src:session kind:fix opened:2026-09-23 gh:#239 pr:435 runs:6 -->
- [x] `PG-213` **P3** [fix] `PraticaConversationRenumberingTests.syncRenumbersAFollowedConversationWhoseIdMailHasChanged()` and `PraticaLedgerFolderTrashEdgeTests.prepareRegenerationRefusesATrashedFolderAndLeavesNoSheetOrClaim()` — both among the six files the `PG-208` `MailStoreOverride` gate fix (PR #397-era, `be1f02f`-adjacent) was meant to make deterministic — failed again across a run of five consecutive full `PergamenumTests` suite runs on a clean, TODO.md-only diff (no Swift source changed), each run failing a *different*, non-overlapping subset (also seen once each: `PraticaConversationRecoveryReportingTests` under two different method names, `PraticaLedgerFolderTrashEdgeTests`) — every failing test passed clean in isolation both times checked. The shape (different tests each run, all pass alone, none of them the actual diff under test) is cross-suite contention or leaked shared state that the `MailStoreOverride` gate did not fully close — `Tests/PraticaConversationRenumberingTests.swift`, `Tests/PraticaLedgerFolderTrashEdgeTests.swift`, `Tests/MailStoreOverride.swift` · Closed 2026-09-23: both named tests rerun in isolation with no concurrent `xcodebuild` process present, `** TEST SUCCEEDED **` both times (10/10). No source changed across any of the failing runs; consistent with cross-suite contention, not a real regression. <!-- src:session kind:fix opened:2026-09-22 closed:2026-09-23 runs:16 regression-of:PG-208 -->
- [x] `PG-218` -> #443 **P3** [fix] `PraticaLedgerFolderTrashEdgeTests.aPraticaRecreatedUnderTheSameNameStartsCleanAndStillImportsItsMessage()` failed during the Stop-hook full `PergamenumTests` run, third consecutive suite this session to have that same file flake — no Swift source changed this session (`TODO.md`-only diff), passed clean rerun alone (9/9, 0.489s) and in an immediate full-suite rerun (3512/3512, 5 known issues, unchanged) — same cross-suite contention/leaked shared state shape `PG-213` diagnosed for this exact file, closed the same day, and evidently not fully closed by it — `Tests/PraticaLedgerFolderTrashEdgeTests.swift` · Closed 2026-09-23: re-investigated, no second shared-mutable-state bug found beyond the one `PG-208` already fixed (`MailStoreOverride` gate fully adopted, no test bypasses it; `runExclusive` snapshots `MailStoreLocation.resolve()` before its `Task.detached` hop per ADR-0041; all fixtures use UUID-suffixed paths). Same verdict as `PG-213`: cross-suite system contention during the full parallel suite run, same family as `PG-214` (xcodebuild/DerivedData contention), not a deterministic code defect. Issue closed as not planned with findings recorded in the GitHub comment. <!-- src:session kind:fix opened:2026-09-23 closed:2026-09-23 runs:6 regression-of:PG-213 renumbered-from:PG-217 promote:2026-09-23 gh:closed#443 -->

- [x] `PG-203` **P2** [fix] Eight UI tests are red on `main` and were red before #371 (same messages on `5aa78b0`, and with no window watcher): five in `WikilinkNavigationUITests` ("Not hittable: Link" at {577.6, 309.8}), `WorkspaceBoardUITests` `testACornerGripResizesTheCard` and `testTheArrowToolConnectsTwoCards`, `TaskTimeUITests` `testAScadenzaWithAnHourCanAlsoBecomeABlockOnItsDay`; the full run of 2026-09-21 was `contaminated` (Finder took focus 26 times) so no verdict is `green` for this tree, and the reds must be fixed or retired before stage 3 can name a green baseline — `UITests/WikilinkNavigationUITests.swift`, `UITests/WorkspaceBoardUITests.swift`, `UITests/TaskTimeUITests.swift` · Closed: `UITests/TaskTimeUITests.swift` no longer exists, retired in the stage-3 retirements (`eadc5487`); superseded by that work. <!-- src:session kind:fix opened:2026-09-21 closed:2026-09-22 runs:22 -->
- [ ] `PG-173` **P3** [fix] Two cosmetic residuals of PG-169, both rare and self-clearing, neither losing data: a sync queued but not yet running for a pratica that gets trashed reaches `runExclusive`'s `guard let dossier` and reports «"…" non ha un dossier leggibile in pratica.md» in the pane (`SyncRunQueue.pending` is private to `PraticaLiveSync` and the tombstone is only recorded for a claimed path); and `forgottenPraticaPaths` is cleared only by `pruneRedirectsIfIdle`, so a pratica trashed and recreated under the same name can have its sync refused with `.praticaTrashed` while an unrelated claim (e.g. a regeneration preview sheet) stays open — `Sources/Features/Pratiche/PraticheController+Ledger.swift` · `docs/plans/pg-169-pratica-ledger-forgotten-on-folder-trash.md` §"Risks, residuals and HITL gates" <!-- src:review opened:2026-09-19 kind:fix runs:38 promote:declined -->
  - 2026-09-20: the second residual (`forgottenPraticaPaths` cleared only by `pruneRedirectsIfIdle`) is fixed by PR #346, the tombstone now falls per path in `endSync`/`endRegeneration` (ADR-0052 §D6); only the first residual, the queued-but-not-running sync reporting «non ha un dossier leggibile», remains.
- [ ] `PG-158` **P3** [fix] Manual verification still owed on the real pratica that originally reported PG-155/PR #273's symptom: change Mail's account "Scarica allegati" setting from "Recenti" to "Tutti i messaggi e i loro allegati", wait for Mail to finish downloading, then confirm attachments resolve in Pergamenum — the real-world root cause of "attachments never download" (Mail never fetched the bytes), distinct from the `regeneratePending` code bug PR #273 fixes and already verified by unit tests <!-- src:session opened:2026-09-15 kind:fix runs:50 gh:#273 -->
- [ ] `PG-153` **P3** [correctness] `VaultSession+Diary.writeDiary`'s prose is read by `readDiary` in one user gesture and written in another, with a UI round trip between — wider than ADR-0043 §D8's `expecting:` precondition can close (plumbing a hash across a UI round trip is a design change, not a mechanical adoption). ADR-0001 §D3.4's conflict prompt, now firing at this call site per ADR-0043 Task 8, is the existing partial answer; whether it is sufficient is an open design question → `docs/adr/0043-vault-write-ordering-concurrency-races.md`, Task 9's "Decline" list, `gh:#262` <!-- src:session opened:2026-09-14 kind:feature runs:50 adr:0043 gh:#262 -->
- [ ] `PG-149` **P2** ADR-0042 (Pratiche inline image placeholders) merged to `main` via PR #250 (`f9dc424`) — implementation and full unit suite (2900/2900) verified green in-session; `scripts/uitests.sh` run (118 tests, 3 failures, all pre-existing and unrelated — see `PG-108` and `testACornerGripCanStillBeGrabbedWhenZoomedOut`, no Pratiche/inline-image test among them) <!-- src:session opened:2026-09-12 kind:fix runs:51 gh:#250 -->
  - Merged first, manual hand-check deferred deliberately to a Sparkle-updated real build (a DerivedData Debug build never keeps Full Disk Access for Apple Mail across rebuilds — TCC ties the grant to the app's on-disk code identity, which changes every rebuild). Still to do once the release lands: the ADR-0042 section of `Pergamenum - checklist verifica Pratiche allegati (ADR-0040).txt` (10 items, none yet run — take a backup of the pratica folder first).
- [ ] `PG-120` **P3** `MailStoreReaderTests.publishReportsMailIsWritingOnATornCopyAfterOneRetry` failed once during the full `PergamenumTests` suite (Stop-hook run), not reproduced in isolation — `Tests/MailStoreReaderTests.swift:139` <!-- src:session opened:2026-09-11 kind:fix runs:52 gh:#204 -->
  - 2026-09-21: failed again in a Stop-hook unit run of this session («test runner exited with code 0»), same test, not rerun by hand; still not reproduced in isolation.
  - Seen at the tail end of the 2026-09-11 Pergamenum 1.4 release session, on the final Stop-hook full-suite run (the one that hit `STOP_GATE_MAX_REENTRY=3` and disarmed). No file this session touched (`Project.swift` marketing-version bump, `TODO.md`) is anywhere near `MailStoreReaderTests`/`MailStoreReader`. Same suite-context-only shape as `PG-110` (fails inside the full suite, not investigated in isolation yet) — possibly related, not confirmed.
  - 2026-09-19 (#204, PR #316): not reproduced in 9 runs on a fresh build (the suite alone, the full `PergamenumTests` suite, 6 consecutive full-suite runs), and reading `MailStoreCopy`, `MailStoreConnection` and `MailStoreFixture` found no path to a wrong answer and no shared global state. The report never said whether the failure was an `#expect` or a thrown fixture error (`Tests/MailStoreReaderTests.swift:139` is the function declaration; the fixture shells out to `/usr/bin/sqlite3`). `be1f02f` adds the source index size and the state directory listing to the `#expect` message so the next failure says what the disk held. **The flake itself is not fixed and its cause is unknown**; the entry stays open.
## In Progress

*Nothing in progress.*

## Roadmap new feature

Speculative feature ideas, explicitly deferred with a stated reason, not yet scoped or designed.
Their GitHub issues were closed 2026-09-12 as "not active work" — kept here as backlog only.

- [ ] `PG-121` **P3** [roadmap, future] Swift-native template engine inspired by Knap (obsidianmd/knap, npm, MIT) <!-- src:session opened:2026-09-11 runs:1 kind:roadmap gh:closed#206 -->
  - Idea surfaced 2026-09-11 while analyzing Knap (github.com/obsidianmd/knap), Obsidian's Node/TypeScript template engine that converts JSON data into Markdown with YAML frontmatter using a Twig/Liquid-style syntax (`{{ variable | filter }}`, `{% if %}`, `{% for %}`) and an AST interpreter with no arbitrary code execution.
  - Not integrable as a dependency: Knap requires a Node 20+ runtime, incompatible with Pergamenum's offline Swift 6 stack (Principle 1/2). The idea is to reimplement from scratch, in pure Swift, only the syntax plus a minimal set of filters relevant to the domain (`date`, `wikilink`, `yaml_property`, `table`, `upper/lower/title`), as an AST parser under `Sources/Core`, zero external dependencies.
  - Use case: generating Markdown notes from external data (Plaud import, Pratiche) with a user-customizable format, instead of today's fixed Swift logic (`Dossier.render`, `ImportNaming`).
  - Explicitly not to be started now: today `Dossier.render` and `ImportNaming` cover the case with a fixed format, which is correct as long as the output does not need to be user-configurable. Start only if/when a real need for output-format customization emerges — otherwise this is a premature abstraction for a single consumer.

- [ ] `PG-111` **P3** [roadmap, future] Detect new counterparts that join an *already-followed* pratica's conversation during regular sync, not only at wizard creation time <!-- src:session opened:2026-09-10 runs:2 gh:closed#207 -->
  - Explicitly deferred by Stefano when scoping the new "detect new counterparts" wizard feature (2026-09-10): the wizard-time detection (analyze the whole conversation, propose addresses not yet in `Dossier.counterparts`, one-by-one selector) is being built now via `concept-to-code`. This item is the natural follow-up he flagged as "interessante": once a pratica is already being followed, a later sync can encounter a message from/to an address never seen in that pratica before (someone new joins the thread after the pratica was created) — right now nothing surfaces that, the person only finds out by reading the email itself. Likely lands near `PraticaLiveSync`/the tray mechanism (`PraticaTrayModel`), proposing new *people* rather than new *conversations*. Not started - scope, ADR impact and UI (tray row? sheet? notification?) still to be designed.

- [ ] `PG-112` **P3** [roadmap, future] «ChatGPT inside Pergamenum»: an in-app assistant over a pratica, rather than an export command out of one — no file yet, its own chain <!-- src:session opened:2026-09-10 runs:2 gh:closed#209 -->
  - Deferred out of the Pratiche chain by Stefano's explicit choice (SPEC.md "Out of scope", ADR-0036 §D20, R-41). The stated direction is to bring ChatGPT into the app, not to export to it, so **no «Esporta per ChatGPT» command was built either**: the timeline is plain markdown on disk and `perg pratica <titolo>` prints it, which already covers copy-and-paste by hand.
  - One measured obstacle worth keeping (SPEC.md:44): ChatGPT.app registers only the `codex://` URL scheme, so there is no way to hand it text through a URL. Whatever this becomes, it is not a URL hand-off.

- [ ] `PG-113` **P3** [roadmap, future] «Apri come board»: turn a pratica's timeline into a `.canvas` with one card per message — `Sources/Features/Pratiche/**` + `Sources/Vault/CanvasStore.swift` <!-- src:session opened:2026-09-10 runs:2 gh:closed#210 -->
  - Deferred out of the Pratiche chain (SPEC.md "Out of scope", ADR-0036 §D20, R-41). Nothing was built toward it: no command, no `.canvas` writer, no node layout. The pieces that would serve it already exist independently — `CanvasStore`, `CanvasID.generate(avoiding:)` and the JSON Canvas 1.0 round trip (principle 4) — so this is composition, not new machinery.

- [ ] `PG-114` **P3** [roadmap, future] `pergamenum://pratica/add?message=…` URL-scheme entry point, so a message can be filed into a pratica from outside the app — `Sources/Core/Conventions/PergamenumRoute.swift` + `Sources/App/**` <!-- src:session opened:2026-09-10 runs:2 gh:closed#211 -->
  - Deferred out of the Pratiche chain as M6 territory (SPEC.md "Out of scope", ADR-0036 §D20, R-41). The in-app path exists and is what this would automate: «Aggiungi a pratica da Mail…» (Cmd+Shift+P) reads the selected message through `MailLink.selectedMessage()` and files it. A new route case would need the same Automation consent story and a decision about what happens when the app is closed.

- [ ] `PG-115` **P3** [roadmap, future] Connector **write** access to pratiche (`perg pratica add-note`, an MCP write tool) — `Sources/Connector/VaultPratiche.swift`, `Sources/CLI/Commands/PraticheCommands.swift`, `Sources/MCPServer/ToolCatalogue+Writing.swift` <!-- src:session opened:2026-09-10 runs:2 gh:closed#212 -->
  - Deferred out of the Pratiche chain: v1 is read-only on purpose (SPEC.md "Out of scope", ADR-0036 §D20, R-36/R-41). `VaultAPI.pratiche(_:)` and `VaultAPI.pratica(_:_:)` shipped, both reading only what is on disk — and a purity test forbids `MailStore`/`EMLXReader`/`SQLite3` under `Sources/Connector`, `Sources/CLI` and `Sources/MCPServer`, which a write path must keep honouring: writing a manual entry touches `pratica.md`, never Mail.
  - What it would cost, from the existing shape: `PraticaEntry.insert(kind:at:counterpart:in:)` is already pure and shared, so the write itself is one `VaultSession.write` behind `VaultAPI.arm` (dry-run + `UnifiedDiff` + `WriteJournal`, ADR-0007 §D6), plus the MCP tool's `dryRun`-defaults-to-true lock. R-29's daily-note mirror would have to be decided on: the app writes it, a connector arguably should not.

- [ ] `PG-156` **P2** The M11 Views/board renderer (ADR-0009, `RenderedViewBlock`) is orphaned by ADR-0029's editor unification: a `pergamenum-view` fence never renders as a table/gallery/calendar/kanban board anywhere reachable in the live app, only as raw fenced text — `Sources/Features/Editor/EditorDecorationDelegate.swift`, `Sources/Features/Views/RenderedViewBlock.swift` <!-- src:session opened:2026-09-06 kind:roadmap -->
  - Recovered from an abandoned branch (`emdash/plain-cameras-read-gxgvl`, never merged) during a branch-cleanup pass on 2026-09-15; the underlying finding predates and was never superseded, so the id is reassigned to PG-156 to avoid colliding with PG-099 (already reused twice on `main` since).
  - Found during a chain-memory consistency audit, cross-checked against NotePlan's own live-rendering feature set at Stefano's request. `PG-012` (closed 2026-08-20) shipped the board/table/gallery/calendar renderers and the board's guarded drag-to-write, but that rendering lived exclusively in the separate Lettura mode (`MarkdownBlocksView`). ADR-0029 (2026-09-02/03, `editor-wysiwyg-unification`) removed the Modifica/Lettura toggle everywhere and unified editing into one always-editable `NoteTextView`, but never re-wired `RenderedViewBlock` into the new editor's live styling path (`EditorDecorationDelegate`/`applyStyling` has zero references to it, to `pergamenum-view`, or to any table/gallery/board rendering). Confirmed the single remaining call site of `RenderedViewBlock` in the whole repo is inside `MarkdownBlocksView.swift`, itself explicitly retained only as dead code for the main editor (ADR-0029 §D14) — its only live callers today are `TranscludedNoteView` (read-only preview of a `![[nota]]` transclusion) and `NoteExporter` (HTML export). The "Viste" sidebar pane (`ViewsPane.swift`) only catalogues views (name, renderer, filter, match count) and opens the note at the block's location — it does not render the board itself. Net effect: today there is no way to see or drag a kanban card, or see a rendered table/gallery/calendar view, anywhere in the live app. Not a regression anyone flagged: `PG-012`'s closure predates ADR-0029 by two weeks, and neither ADR-0029's own manifest nor its Consequences section mentions the Views feature at all.
  - Comparison against NotePlan (researched on request): NotePlan's own board/kanban view is *also* a separate, non-inline surface (Folder Cards view, not drawn inside a note's text flow) — so the fix here is not "make it render inline like a table," it is "give `RenderedViewBlock` *any* live surface again," matching what already shipped once under `PG-012`. Every other construct checked against NotePlan (headings, emphasis, lists, checkboxes, blockquotes, hr, wikilinks, tables, code fences with syntax highlighting) already matches or exceeds NotePlan's live-rendering behavior in this app; highlight (`::text::`) and underline (`~text~`) are NotePlan features Pergamenum's SPEC never scoped in, so their absence is a deliberate scope difference, not a gap.
  - Not yet scoped how the fix should look (inline `NSTextAttachmentViewProvider`-hosted board like ADR-0029's GFM table, or a live-updating panel opened from the "Viste" row) — needs its own interview before implementation, likely through `concept-to-code` given it reopens ADR-0009/ADR-0029 territory.

## Backlog / To Add

`scripts/uitests.sh` run by hand on 2026-09-10 (114 tests, 88-1400s runs depending on the pass): first
pass was 113/114 green with one failure in the new `PraticheUITests.testRigeneraShowsADiffPreviewAndAnnullaLeavesTheFileOnDisk`
(PG-116's UI coverage, added by the PG-116-119 follow-up plan). Root cause was a bug in the test
itself, not in production code: it synced a message from the fixture and immediately triggered
«Rigenera» without ever editing the note, so `PratichePane.regenerationReadySheet` correctly found
no diff and rendered its no-op "Chiudi" branch instead of "Annulla"/"Rigenera" — the test then
waited 5s for an "Annulla" button that structurally wasn't there in that state. Fixed by having the
test hand-edit the synced note before invoking «Rigenera» (so a real diff exists) and by tightening
the final assertion to compare exact file content, not just existence. Verified only via
`build-for-testing` (never run by the agent, per the standing UI-suite rule) — **not yet
re-verified by a real `scripts/uitests.sh` hand-run, and not yet committed.**

- [x] `PG-116` **P2** Rigenera trashes existing message files before a replacement is available, with no ADR-0036 D6 diff shown before confirmation — `Sources/Features/Pratiche/PraticaCommandActions.swift:~262` <!-- src:session opened:2026-09-10 closed:2026-09-10 runs:48 -->
  - Raised by RTF's Codex review (astra, xhigh) every cycle since cycle 1 of the Pratiche RTF review, deferred each time as architectural. Fix needed the replacement message prepared and its `UnifiedDiff` shown for confirmation BEFORE any existing file is touched, then an atomic replace only after approval.
  - Closed by ADR-0036 §D21 (`docs/superpowers/plans/2026-09-10-pratiche-pg105-pg108.md` Task 5): `PraticaSyncEngine.regenerationPreview`/`commitRegeneration` acquire the replacement text and a `UnifiedDiff` before anything is trashed; `PraticheController.RegenerationState`/`PratichePane`'s sheet show it and require an explicit confirm. `DiffView` moved to `Sources/DesignSystem/DiffView.swift`, shared with `TagRenameSheet`. A build-for-testing-only UI test (`PraticheUITests.testRigeneraShowsADiffPreviewAndAnnullaLeavesTheFileOnDisk`) covers the sheet appearing and «Annulla» leaving the file untouched — not run by the agent, per this repo's standing UI-suite rule.

- [x] `PG-117` **P2** Keyword evaluation only sees followed/included messages; `autoFollowedConversations` is computed but never persisted — `Sources/Features/Pratiche/PraticheController.swift:~1262` <!-- src:session opened:2026-09-10 closed:2026-09-10 runs:48 -->
  - Raised by RTF's Codex review every cycle since cycle 1, deferred as architectural. A real fix needed counterpart messages from unfollowed conversations included in the membership-evaluation snapshot, plus `autoFollowedConversations` persisted through the dossier.
  - Closed by Task 4 of the same plan: `MembershipStoreSnapshot` gained `unfollowed: [Int: [MailMessageRow]]`, `MembershipRule`'s keyword arm evaluates against it, and `DossierWriter.update` persists newly auto-followed conversations into `pergamenum-dossier-conversations` instead of recomputing them from scratch every sync.

- [x] `PG-118` **P2** Sync never records ledger message/ROWID/conversation-id triples and never invokes conversation recovery (ADR-0036 R-14) — `Sources/Features/Pratiche/PraticheController.swift:~456` <!-- src:session opened:2026-09-10 closed:2026-09-10 runs:48 -->
  - Raised by RTF's Codex review every cycle since cycle 1, deferred as architectural. This was unimplemented SPEC data-model work: persisting the ledger's message/ROWID/conversation-id bridge for imported messages, invoking R-14's recovery when a followed conversation id disappears, and reporting unrecoverable ones.
  - Closed by Task 1 of the same plan (ADR-0036 §D23): `PraticaSyncEngine.SyncOutcome.bridge` records one `PraticaLedger.Entry` per imported message; `PraticheController.recordSyncOutcome` merges it into the ledger; `runExclusive` remaps a followed conversation's id when the index has renumbered it and reports `unrecoverableConversations` rather than silently dropping them.

- [x] `PG-119` **P2** Tray membership's counterpart check only matches the sender, so an outgoing-only conversation addressed to the counterpart is never picked up — `Sources/Core/Pratiche/MembershipRule.swift:141` <!-- src:session opened:2026-09-10 closed:2026-09-10 runs:48 -->
  - Raised by RTF's Codex review every cycle since cycle 1, deferred as architectural. Investigated during the Pratiche RTF cycle 5 triage: `MailMessageRow` (`Sources/Core/Email/MailMessageRow.swift`) carried no recipient (To/Cc) field at all, only `sender` — fixing this needed a new join/query added to `MailStoreConnection.swift`, the ONE file in the repo `SharedSourcesPurityTests`/the isolation guard permit to call `sqlite3_*`.
  - Closed by Tasks 2-3 of the same plan: a HITL live probe against a published copy of the real Envelope Index (`docs/adr/0036-pratiche.md` "Follow-up — Task 2 probe results, PG-119") confirmed the `recipients` join's shape; `MailMessageRow` gained `recipients: [String]` (defaulted, declared last) and `MembershipRule`'s `touches()` predicate now matches To/Cc addresses, not sender alone.
  - **Left open, filed separately below:** §D23.5's remaining tray-row work (persisting `unrecoverableConversations` in the ledger and drawing a tray row for it) — deliberately deferred for this batch per the plan's own Task 6 instruction, not part of PG-119's original scope.

- [ ] `PG-109` **P3** An unrecoverable followed conversation (its id gone after an index renumber, R-14) is reported via `controller.report(...)` only — no ledger persistence, no tray row — `Sources/Features/Pratiche/PraticheController.swift` (`runExclusive`, `unrecoverableConversations`) <!-- src:session opened:2026-09-10 runs:47 gh:#208 -->
  - Named as open, on purpose, by ADR-0036 §D23.5 rather than folded into PG-118/PG-119: it needs an invalidation rule for the tray row that has not been designed (when does the row clear — a new sync that re-resolves the conversation? a person dismissing it by hand?), and persisting `unrecoverableConversations` in the ledger is a shape decision, not a bounded code change.

- [x] `PG-099` **P3** An open Workspace board's pending (debounced) autosave can silently clobber a rename/move's board-repoint write, losing the rewrite with no error shown — `Sources/Features/Workspace/WorkspaceController.swift` (`scheduleSave`/`save`, ~lines 553-585) <!-- src:session opened:2026-09-07 closed:2026-09-22 runs:47 gh:#213 -->
  - Closed by `docs/adr/0054-board-origin-marker-and-canvas-write-preconditions.md` and its plan (`docs/plans/pg-213-workspace-autosave-race.md`, `fix/pg-213`): `WorkspaceController` now records a `BoardOrigin` (the hash of the bytes a board was last read from or saved to) and `CanvasStore.save(_:board:expecting:)` refuses a write whose bytes have moved on since. A refusal reconciles automatically when the divergence is a pure `.file`-node repoint (PG-099's own repro, `CanvasDocument.reconcile`), and enters a non-modal `.conflicted` state on the board's own toolbar — «Mantieni le mie modifiche» / «Ricarica dal disco» — for anything reconciliation cannot decide, rather than silently overwriting or discarding either side.
  - The other four `.canvas` writers that could race an open board's autosave the same way — `BoardFileOperations.renameBoard`/`moveBoard`, `FolderFileOperations.renameFolder`/`moveFolder`, and `VaultSession+Tasks.writeTaskSource`'s board branch — adopted the same `writeRepoint`/`expecting:` guard, reporting a refusal (not a silent clobber, not an unrelated failure) when it fires.
  - **Left open, filed separately below (ADR-0054 §D8):** the **note** half of the same board/folder-rename verbs still writes through an unguarded `store.write` (PG-205), and `NoteFileOperations.rename(_:to:knownPaths:)`/`move(_:toFolder:)` have no production caller at all (PG-206).
- [ ] `PG-100` **P4** No test covers a Workspace `.text` card whose body contains both a real `[[Wikilink]]` match and a separate, coincidentally fold-matching quoted bullet on note rename — `Tests/NoteFileOperationTests.swift` (compound case for `renamingDoesNotRewriteAnUnrelatedQuotedBulletInATextCard`, commit `9bb0b50`) <!-- src:session opened:2026-09-07 runs:47 gh:#214 -->
  - The `includeQuotedRelated: false` gate at both branches of `NoteRename.rewritingLinks` (`Sources/Core/Conventions/NoteRename.swift:31-34` and `:42-43`) was independently verified correct by a reviewer subagent reading the code directly, but no test exercises the compound scenario: a card with both a matching wikilink and an unrelated quoted bullet that happens to fold-match the old title in the same body. Low priority — logic is confirmed sound, this is coverage debt only.
  - Found manually hand-checking R-06/R-07 of the rename/move/trash silent-failure chain (`docs/manifests/2026-09-07-rename-move-trash-silent-failure-fix.manifest.yml`), which added `.text`-card wikilink rewriting to `NoteFileOperations.repointBoardsPlan`/`repointBoards`. That rewrite itself is correct — verified via `perg note rename` on the CLI (shares the exact same `VaultSession.renameNote` path) and via a from-scratch GUI repro where the board had no pending edits: both correctly rewrote `[[Capture]]` → `[[Capture rinominata]]` on disk.
  - Repro that fails: create/edit a Workspace board's text card (setting `hasUnsavedChanges = true` and scheduling `WorkspaceController.scheduleSave()`'s 1-second debounce, `autosaveDelay` at `WorkspaceController.swift:155`), then within that window trigger an unrelated note rename from the Note sidebar that repoints that same board. The rename's `VaultSession.renameNote` writes the correctly-rewritten `.canvas` file to disk immediately. Moments later the board's own pending debounced `save()` fires and unconditionally overwrites the file with the stale in-memory `document` (still holding the pre-rename wikilink) — `save()` (`WorkspaceController.swift:570-585`) has no way to know the file changed externally in the interim, and only guards on its own `hasUnsavedChanges`, not on the file's mtime/hash.
  - Pre-existing, not introduced by this chain: the same race would already clobber the `.file`-node (embed) repointing ADR-0026 added for board moves/renames, since `save()`'s blind overwrite applies regardless of which node kind changed. Discovered only because this chain's manual R-06/R-07 check happened to create the board moments before renaming.
  - Deliberately left open per Stefano's explicit call (2026-09-07): out of scope for the rename/move/trash chain, which is otherwise complete and verified (R-01–R-07 all pass in the no-pending-edit case). A real fix likely means either flushing every open Workspace board's pending save before a rename/move/trash writes board-repoint changes, or having `save()` refuse/merge when the on-disk file no longer matches what was loaded — needs its own analysis before implementing either.

- [ ] `PG-096` **P3** Sparkle integration for signed update delivery — code and pipeline shipped, only the manual update-flow hand-check (R-08) remains <!-- src:session opened:2026-09-04 updated:2026-09-05 runs:47 gh:#215 -->
  - **Entry corrected 2026-09-05: the "not started" note below is stale, superseded the same day it was written.** ADR-0031 was designed and implemented after this entry was opened: `docs/adr/0031-sparkle-auto-update-integration.md` exists, `Tuist/Package.swift` carries the Sparkle 2.9.6 dependency, and PR #167 (`feat(app): add Sparkle auto-update integration (PG-096)`, commit `d4c65ad`) is merged into `main`. `istefox/pergamenum-updates` is live and public (verified via `gh api`, created 2026-09-04), holds `appcast.xml`, and already has one published release, `v1.1-641` (`Pergamenum-1.1-641.zip` asset, published 2026-09-04T21:46Z) — so `scripts/release.sh` has already been run for real, not just built.
  - **Still open, not yet found recorded anywhere:** the R-08 hand-check from `PROJECT_BRIEF.md`'s 2026-09-04 Status entry — installing the released build over `/Applications`, cutting a second release, and confirming from the old running copy that «Cerca Aggiornamenti…» offers, downloads, signature-verifies and installs the update, plus the "no update available" and "no network" cases. No commit, TODO entry or memory file found recording that this happened. Until it does, treat the update flow as shipped but never exercised end-to-end.
  - Original (now superseded) note, kept for the record: "Not started. `scripts/release.sh` produces a signed, notarized, numbered build (`CFBundleVersion` = commit count) but has no update-check or appcast path; `Tuist/Package.swift` carries no Sparkle dependency; nothing under `docs/adr/` mentions it." — this was true at 2026-09-04 opening time, false by the same evening.
  - **Auto-memory drift, not a fact (context for the above, now resolved).** An earlier session's `.remember/recent.md` claimed "ADR-0030 (Sparkle updates) approved 2026-09-03" with no matching ADR file — that claim was checked and found wrong; the real work landed as ADR-0031 on 2026-09-04, not ADR-0030 on 2026-09-03. No action needed here beyond noting the drift for anyone who finds the old memory entry.

- [x] `PG-095` **P1** A newly created note never bumps `scanGeneration`, so it stayed invisible in the Note sidebar tree until a manual rescan or relaunch — `Sources/Vault/VaultController+Notes.swift` (`createNote`, `openDailyNote`) <!-- src:session opened:2026-09-02 closed:2026-09-02 runs:49 -->
  - Found manually reproducing PG-029 on the real "Pergamena" vault: Stefano created a note ("Prova") via Cmd+N, it opened correctly in its own tab and was written to disk (`Prova.md` confirmed present at vault root), but never appeared in `NoteListPane`'s sidebar tree.
  - Root cause: `createNote` (`VaultController+Notes.swift:16`) writes via `session.createNote` → `VaultSession.write` (`VaultSession.swift:187`), which updates the in-memory index (`index.update(...)`, line 196) but never bumps `scanGeneration`. `NoteListPane.rebuild()` (`NoteListPane.swift:562`), which builds the `tree` the sidebar renders, only runs from `.task(id: vault.scanGeneration)` (line 134) — it does not react to `vault.index.allNotes` changing on its own, deliberately, per its own comment ("rebuilt when the index changes rather than in `body`... sorting every note on every keystroke is work nobody asked for"). `scanGeneration` is bumped only by `rescan()` and `clearCache()` (`VaultController.swift:217`, `:320`). Every other write path that adds/moves/removes something already knows this and explicitly follows its write with `Task { await rescan() }`: `VaultController+Move.swift:166`, all four sites in `VaultController+Folders.swift`, `VaultController+TaskDrop.swift:37`, two sites in `VaultController+Files.swift`. `createNote` and `openDailyNote` (same file, `:86-92`, which calls `session.dailyNote(for:)` directly) are the two write paths that skip it.
  - The note is not lost or corrupted — searchable and linkable immediately via the in-memory index, just absent from the one view that matters for finding it by eye. P1 because it silently defeats the primary way of using the app (create a note, expect to see it where notes live) with no error, no clue, and no workaround short of knowing to hit refresh.
  - Fixed by adding `Task { await rescan() }` after the write in `createNote`, mirroring the pattern every other write path already uses. `openDailyNote` got the same call, guarded by `!session.exists(dailyNotePath(for: date))` so re-opening an already-existing daily note (the common case) does not trigger an extra rescan for nothing. `xcodebuild build` succeeded, unit suite 2017/2017 green, and Stefano confirmed by hand on the real "Pergamena" vault that a note created via Cmd+N now appears in the sidebar tree immediately, with no manual refresh needed.

- [x] `PG-089` **P3** Hovering an Outline row (not a gap) during an outline-section drag shows macOS's default "+" cursor instead of a "not allowed" indicator — `Sources/Features/Editor/OutlinePane.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-02 runs:50 -->
  - Found by Stefano during PG-019's manual R-10 hand-check. Functionally correct (no insertion line, no-op), just the system cursor over a row with no `.dropDestination` under it defaults to the generic "+" (copy) icon.
  - **Attempted and reverted 2026-09-01.** Added a `DropDelegate` on the row (`dropUpdated(info:) -> DropProposal?` returning `.forbidden`), the fix this note originally proposed. Confirmed via a debug build with stderr logging that `dropUpdated` fires repeatedly and returns `.forbidden` on every call while hovering — the delegate is wired correctly — but Stefano confirmed by hand the cursor still shows the green "+" regardless. `DropProposal(operation: .forbidden)` does not translate into an actual "not allowed" `NSCursor` in this app's AppKit/SwiftUI hybrid setup, at least on this OS/SDK. Reverted cleanly (`git diff` empty against the pre-attempt state) rather than leave an untested drag primitive in the tree for no visual benefit.
  - **Closed 2026-09-02 as won't-fix, root cause understood.** `OutlinePane.swift` drives the drag entirely through SwiftUI's `.draggable`/`.dropDestination`, which on macOS hands the whole drag session to AppKit's `NSDraggingSession` — that session recomputes and reapplies the cursor on every mouse-moved event from its own `operationMask`, overriding any `NSCursor.set()` the app calls during the drag. `DropProposal(operation: .forbidden)` (SwiftUI's own drop-side signal, already confirmed firing correctly in the 2026-09-01 attempt) has no path into that mask: SwiftUI's `.draggable()` does not expose the `NSDraggingSource` delegate (`draggingSession(_:sourceOperationMaskFor:)`) an app would need to actually influence it. A real fix means replacing `.draggable`/`.dropDestination` for this one interaction with a hand-written AppKit `NSDraggingSource`/`NSView` — a rewrite disproportionate to a cosmetic P3, and Stefano's explicit call not to take it on. No code changed this closure, only this record.
  - **Second attempt, also reverted, 2026-09-02.** Grouped into the `concept-to-code` hybrid chain with PG-029/PG-065 (SPEC.md, manifest `docs/manifests/2026-09-02-pg-089-pg-029-pg-065-outline-drag-cursor.manifest.yml`, R-01…R-04). Tried exactly the lower-level fix the first attempt's note proposed: a private `ForbiddenCursorWhileInvalidDrop` `ViewModifier`, applied to every heading row, embed row and gap, doing a manual `.onHover { NSCursor.operationNotAllowed.push()/.pop() }` gated on `draggingEntry != nil && !isValidDrag`, following `BoardHandles.swift`'s established push/pop-plus-`.onDisappear`-safety-pop pattern verbatim. `xcodebuild build` succeeded and the unit suite (2017/2017) stayed green with the change in place, so the logic itself was never in question. Stefano confirmed by hand on a throwaway vault (`Prova.md`, three headings plus a subsection): the cursor stayed the green system "+" throughout an active drag, on both an embed row and an invalid-nest heading row - no visible change at all, same failure mode as the `DropDelegate` attempt. Reverted cleanly via inverse edits (`git diff --stat` empty against the pre-attempt state). This app's AppKit/SwiftUI hybrid setup (this SDK/OS) appears not to honor a manually pushed `NSCursor` during a SwiftUI `.draggable`/`.dropDestination` session, at either the `DropDelegate` or the `.onHover` layer - two independent approaches, two silent no-ops. A third attempt would need to diagnose *why* first (e.g. instrument whether `.onHover` even fires during an active native drag session, which differs from an ordinary mouse hover) rather than trying a third mechanism blind; R-04 accepted this outcome as valid going in.

  - Found at Gate 5.06 (type-design-analyzer) for ADR-0027. Suggested fix: a `CardCommand.rendersAsSubmenu: Bool` (or small `RenderKind` enum) computed once on the type.
- [ ] `PG-076` **P3** Two more UI tests fail identically across two consecutive full `scripts/uitests.sh` runs, same class as `PG-072`'s already-documented intermittent-timing defect — `UITests/DayViewUITests.swift` (`testTheMonthMarksTheDaysSomethingIsDueOn` - "il giorno della scadenza non è segnato sul mese"), `UITests/TaskTimeUITests.swift` (`testAScadenzaWithAnHourCanAlsoBecomeABlockOnItsDay` - "manca la riga dell'orario nel pannello Scadenza") <!-- src:session opened:2026-08-29 runs:51 gh:#216 -->
  - **Scope narrowed 2026-09-07: the third test originally tracked here, `ComposerUITests.testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow`, is fixed and closed separately (see Done).** Root cause was never the "intermittent timing" this ticket named — it was a stale/wrong test assertion (`app.staticTexts["Nota composta.md"]`, an extension no view in the app ever renders), confirmed reproducing identically and deterministically, not intermittently, both on the current branch and pre-fix commit `92f7262`. The other two tests below were not re-investigated in that pass and keep the original intermittent-timing classification and evidence.
  - Found running the full UI suite at Task 8 of ADR-0027 (`unificare-nota-e-testo-in-un-solo-strume`). Confirmed unrelated: `git diff` for this chain against `Sources/Features/Composer`, `Sources/Calendar`, `Sources/Features/DayView` and the three UITest files above is completely empty, and all four failures (these three plus `PG-072`'s) reproduced byte-for-byte identically across two full runs of the suite, one before and one after this chain's own `FormattingTextView` accessibility fix (which touched only `Sources/Features/Workspace`). All four involve the Scadenza/calendar panel or note-creation timing, the same class `PG-072` already names as needing a human at the keyboard rather than another automated retry.
  - Re-run targeted (`-only-testing`, all three plus `PG-072`'s) on 2026-09-04, bookkeeping only, no root-cause investigation: `testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow` still failed, identical assertion, at 15s (not the 59s launch-timeout mark, so a real repro, not stale-instance noise). `testTheMonthMarksTheDaysSomethingIsDueOn` and `testAScadenzaWithAnHourCanAlsoBecomeABlockOnItsDay` both passed this run. One clean run of an admittedly-intermittent test is not a fix — left open, not closed; still needs a human at the keyboard per the original diagnosis, only `testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow` reproduces reliably enough right now to be worth watching first.
  - Full `scripts/uitests.sh` run on 2026-09-04 (working-tree check before the `TableGridView`/`WorkspaceController` `type_body_length` split is committed, `PG-055`/`PG-056` convention): 105 tests, 1 failure — `testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow` again, same assertion, 14s. The other two names in this ticket and `PG-072`'s test both passed on the full run too. `git diff` for the split touches only `Sources/Features/Editor/TableGridView*.swift` and `Sources/Features/Workspace/WorkspaceController*.swift`, neither reached by `Composer`/`DayView`/`TaskTime` — no new regression from that change, confirms this ticket's failure is the only one currently reproducing reliably.
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0025. Same open question as `PG-046`'s folder-side `rewrittenPaths` (there, unread anywhere in `Sources/`/`Tests/`/`UITests/`) now duplicated on the board side: either assert on it in a test (the only evidence the board's own note references were actually rewritten) or drop the field.
  - Found at Gate 5.06 (silent-failure-hunter) on ADR-0025. Worth a shared/explicit helper rather than each call site picking its own default direction for the same "store not attached" case.
  - Found at Gate 5.06 (silent-failure-hunter) on ADR-0025. Pre-existing gap; this chain touched the folder-derived-path → `workspace.board` path without fixing it.
- [x] `PG-065` **P3** No wrapper type distinguishes a board-file path from a folder path; four independently-declared board-vs-folder discriminated unions don't convert to one another — `WorkspaceItemKind`, `WorkspaceTree.Node.Kind`, `WorkspaceSelection`, `PendingWorkspaceDelete` <!-- src:review opened:2026-08-27 closed:2026-09-02 runs:50 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0025. Bare `String` everywhere. `WorkspaceBoardResolution.unique(String)` (`Sources/Core/Tasks/WorkspaceBoardResolver.swift`) carries a raw path too and is the natural first place to introduce a `BoardPath` wrapper before the pattern spreads further. Same reviewer separately called out `PendingWorkspaceRename`/`PendingWorkspaceDelete`, `WorkspaceSelection` and `WorkspaceBoardResolution` as clean, well-encapsulated sealed unions in their own right — the gap is the missing conversion between them, not any one type's own design.
  - Closed via the `concept-to-code` hybrid chain grouped with PG-089/PG-029 (SPEC.md, manifest `docs/manifests/2026-09-02-pg-089-pg-029-pg-065-outline-drag-cursor.manifest.yml`, R-08/R-09/R-10), scoped narrowly to `WorkspaceBoardResolver.swift` only per the interview - the other three union types (`WorkspaceItemKind`, `WorkspaceTree.Node.Kind`, `WorkspaceSelection`, `PendingWorkspaceDelete`) are deliberately untouched, not part of this fix. Added `BoardPath: Hashable, Sendable, CustomStringConvertible` (no `ExpressibleByStringLiteral`, deliberately - a bypass at production call sites would defeat the type safety the wrapper exists for); `WorkspaceBoardResolution.unique(String)` → `.unique(BoardPath)`, both producers (`resolve(_:in:)`, `board(inFolder:among:)`) updated. Seven consumer call sites across `TaskListOptions.swift`, `TasksView+Row.swift`, `WorkspaceView.swift`, `BoardCardMenu.swift`, `BoardChrome.swift` updated to extract `.value` at the boundary - every missed site was a compile error, not a silent bypass, by construction. `board(inFolder:)`'s `folder` parameter stays plain `String` (constructed ad-hoc via `NSString` path manipulation with no `FolderPath`-typed origin) - a deliberate lower-risk scope call, not an oversight. `xcodebuild build` succeeded, unit suite 2017/2017 green including a new wrapper equality/hashing test in `WorkspaceBoardResolverTests.swift`.
  - Found at Gate 5.06 (type-design-analyzer, CRITICAL) on ADR-0025 (`workspace-folder-board-separation`). The shared helper shipped in the same PR; a future change to the matching rule (e.g. Unicode normalization) would silently diverge between rename planning and marker resolution.
- [x] `PG-054` **P2** Double-clicking a folder card on the canvas sometimes fails to navigate into the subfolder — the breadcrumb and tree selection don't move, and no board opens — `Sources/Features/Workspace/WorkspaceController.swift` (`subfolder(for:)`) <!-- src:session opened:2026-08-26 closed:2026-09-02 runs:50 -->
  - Found by hand-check during Task 7 of the ADR-0024 chain (`workspace-board-tree-single-selection`), reported directly by Stefano: double-clicking the "Nord" folder card sometimes opened the subfolder correctly, other times left the breadcrumb unchanged with nothing indicating where the navigation went, or failed to open the file inside when the subfolder held more than one. Root-caused with Stefano at the keyboard on a throwaway vault, with temporary stderr logging on `subfolder(for:)` and `BoardCardMenu.open(_:)` (`PG054-DEBUG`, since reverted): a rename in place was confirmed *not* the trigger (repointing and the live card label both update correctly). The real trigger is a folder **move** to a different parent (drag-and-drop in the sidebar tree, ADR-0026) — `FolderFileOperations.repointBoardsPlan` correctly rewrites the moved-into card's stored path on disk (verified: `Nord/SudX` after moving `SudX` into `Nord`), but `subfolder(for:)` matched that path against `subfolderSet`, which only ever lists the *direct* children of the currently open board's own folder. Once the target folder is nested one level deeper than it used to be, it can never appear in that set again, no matter how many times the board reloads — a permanently dead card (generic-document icon, double-click falls through to the file-open branch and does nothing useful), not merely a stale-cache symptom as first hypothesized. Fixed by having `subfolder(for:)` check the filesystem directly (`FileManager.fileExists(atPath:isDirectory:)` against `store.root.appending(path:)`) instead of consulting `subfolderSet` at all — a card names a real vault path, and whether it is a folder is a property of that path, not of which folder the currently open board happens to be showing. Also fixes the folder-card icon regression for the same reason: `NodeCard`'s folder icon is driven by this same function's result. `xcodebuild build` succeeded, unit suite 2013/2013 green, Stefano confirmed by hand on the throwaway vault that the previously dead "SudX" card now opens correctly after the fix.
- [x] `PG-055` **P3** SwiftLint drift on `WorkspaceBrowser.swift` grew further under ADR-0025, still warning-level <!-- src:session opened:2026-08-26 closed:2026-09-01 runs:50 -->
  - Found in the review-triage-fix cycle on ADR-0024 (`workspace-board-tree-single-selection`) at 668 lines vs the configured 400 `file_length` threshold, up from a baseline of 442 (~226 lines added by the flat-row `WorkspaceRow`/`PendingFolderDelete` work). Grew further during ADR-0025 (`workspace-folder-board-separation`), re-verified 2026-08-27: `file_length` now 903 vs 400, and `type_body_length` newly crosses its 250-line warning threshold for the first time (257 lines) — both still warning-level, no error-level violation here (contrast `PG-056`). Same `swiftlint-drift` pattern as `PG-035`/`PG-036`; the reviewer suggested extracting `WorkspaceRow`/`PendingFolderDelete` to a sibling file if the file grows further. Left as-is at the time rather than forcing a split that wasn't otherwise needed.
  - Re-verified live before starting Phase 6 (2026-09-01): still both warning-level, but `type_body_length` had drifted to 13 lines from its 350-line *error* threshold (337 vs 250 warning) under ADR-0025/0026, and `file_length` to 870 vs 400. Fixed with the same location-only split PG-056 used for `WorkspaceController.swift`, applied here for the first time to a SwiftUI `View` (mirroring this repo's existing `WorkspaceView+Creation`/`+Drawing`/`+FolderVerbs` precedent rather than inventing a new pattern): the struct's three `// MARK:` sections moved verbatim into `WorkspaceBrowser+Verbs.swift` (toolbar target + create/rename/delete decisions), `WorkspaceBrowser+Rows.swift` (both `List`s, one row's wiring, the move a drag/drop carries) and `WorkspaceBrowser+Tree.swift` (rebuild from a scan, expand-all, and the pure statics the unit tests call directly). The one real cost, called out in the plan before starting: Swift's `private` is file-scoped, so every `@State` var and helper an extension file touches had to drop from `private` to internal (plain, unqualified) — the same trade `WorkspaceView.swift` already made for its own split. `theme` and `creating` stayed `private`, read only by `body`/`header`/`toolbar` which never left the main file. `swiftlint` re-run confirms all four files now clean: main file's struct body and both file lengths are comfortably under every threshold. Pure code motion, no behaviour change, no test edit needed (the four `nonisolated static` members the suite calls — `rows(matching:in:)`, `opening(from:to:currently:in:)`, `identifier(for:)`, `canDrop(_:onFolder:)` — stayed `static` on the same type). `tuist generate` run to register the new files, `xcodebuild build` succeeded, unit suite 1991/1991 green.
- [x] `PG-051` **P3** `WorkspaceBrowser` constructs a `Vault`-layer write type (`FolderFileOperations`) directly and runs a synchronous recursive folder walk on the main actor at click time — `Sources/Features/Workspace/WorkspaceBrowser.swift:144-146` (drifted to `:594` at close) <!-- src:review opened:2026-08-25 closed:2026-08-31 promote:declined runs:48 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. Every other folder operation in this diff goes through `VaultController` (`renameFolder`, `trashFolder`); the delete-confirmation count path instead built `FolderFileOperations(store: NoteStore(root:))` in the view and called `contentCounts` → `walk()` synchronously. Fixed exactly as the ticket's own suggestion: a read-only `nameIsAvailable(_:in:)`/`contentCounts(at:)` pair added to `VaultSession+Folders.swift` (delegating to the existing private `folderOperations` computed property) and `VaultController+Folders.swift` (delegating to the session, `true`/`nil` when no vault is open — the exact fallback the removed `@State` fields had). Scope widened by one file, deliberately: `NoteListPane.swift`/`NoteListPane+FolderVerbs.swift` carried the identical duplicate (`NoteListPane.swift:78`, doc-commented as reusing `WorkspaceBrowser`'s own field), so both views' `@State private var folderOperations: FolderFileOperations?` were deleted and every call site routed through `vault.nameIsAvailable`/`vault.contentCounts` instead. `canvasStore`/board rules left untouched (out of scope, has its own ADR-0021 §D10 precedent). The walk itself was not made async — P3, not urgent, per the ticket; async is future work if a vault ever gets large enough to matter. Four new tests in the existing `Tests/VaultSessionFolderOperationsTests.swift` (session- and controller-level, matching that file's own style); no new test file. `xcodebuild build` succeeded for the app, `perg` CLI and `pergamenum-mcp` targets; unit suite 1949/1949 green.
- [x] `PG-050` **P3** Folder verbs pass `(name, parent)` / `(folder, newName)` as bare same-typed `String` pairs instead of a small named type, so a swapped argument compiles silently — `Sources/Features/Workspace/WorkspaceFolderActions.swift:18-25`, `Sources/Features/Workspace/WorkspaceFolderSheets.swift:25` <!-- src:review opened:2026-08-25 closed:2026-08-31 promote:declined runs:48 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. `WorkspaceNameField.state(name:parent:available:)`'s doc comment has to spell out the required argument order because the compiler doesn't carry it. Stefano chose the ticket's thorough fix over the cheap one: a new `FolderName`/`FolderPath` wrapper pair (`Sources/Core/Vault/FolderIdentity.swift`, `Equatable`, `Hashable`, `Sendable`, `ExpressibleByStringLiteral` so existing string-literal call sites keep compiling unchanged) now types every closure on `WorkspaceFolderActions`, `WorkspaceNameField.state`, and both sheets (`NewWorkspaceSheet`, `RenameWorkspaceSheet`) in `WorkspaceFolderSheets.swift`, plus `WorkspaceFolderSheets.parentOptions(from:)`. Deliberately out of scope, decided during planning: `WorkspaceFolderNavigation` (same file, same swap shape, but does real path-prefix arithmetic that would need `FolderPath` to grow path operations) and the whole Vault layer (`CanvasStore`, `FolderFileOperations`, `BoardFileOperations`, `VaultSession*`, `VaultController*`) — both stay `String`-based; the wrapper types are constructed at the Workspace UI boundary and unwrapped (`.value`) immediately before crossing into either. New `Tests/FolderIdentityTests.swift` (construction, equality, hashing, `isEmpty`); one existing test (`WorkspaceCreationTests.swift`'s `parentOptions` test) needed a `[FolderPath]` type annotation, nothing else did thanks to `ExpressibleByStringLiteral`. `xcodebuild build` succeeded for the app, `perg` CLI and `pergamenum-mcp` targets (confirms the new `Sources/Core/Vault` file doesn't break either connector, ADR-0001 §D1); unit suite 1945/1945 green. Related NIT (same review, **not addressed by this fix**): renaming a folder to a different case only (`vecchio` → `Vecchio`) is refused on this machine's case-insensitive APFS, because `FolderFileOperations.swift:169`'s `exists(newFolder)` and `nameIsAvailable` both answer true for the folder being renamed — correct for availability-checking, wrong for a legitimate case-only rename. Not a wrapper-type concern; left for its own ticket if it matters.
- [x] `PG-049` **P3** A same-name (no-op) folder rename is admitted by the plan's guard but then throws "file already exists" from the performer — `Sources/Vault/FolderFileOperations.swift:169` (guard, drifted from `:152`) / `:289` (performer, drifted from `:309`) <!-- src:review opened:2026-08-25 closed:2026-08-31 promote:declined runs:48 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. `guard newFolder == oldFolder || !exists(newFolder)` lets a same-name rename through the plan stage, but unlike the note-side sibling (`NoteFileOperations.rename:203-227`, `if newPath != relativePath { move }`), the folder performer moved unconditionally, so `moveItem` threw and the user would have seen `rinomina cartella: … esiste già`. Verified before starting: not reachable today — `RenameWorkspaceSheet.canRename` (`WorkspaceFolderSheets.swift:264`) already requires `name != currentName`, both UI call sites go through the same sheet, and folder rename has no connector exposure — but the guard and the performer disagreed about whether a no-op is legal, and only the sheet kept them consistent. Fix applied: `renameFolder`'s `moveItem` call is now wrapped in `if plan.newPath != oldFolder { ... }`, mirroring `NoteFileOperations.rename`'s existing idiom exactly. One new test, `renameFolderToItsOwnCurrentNameSucceedsInsteadOfThrowingAlreadyExists`, asserts the same-name case now succeeds and returns `RenameOutcome(newPath: <unchanged>)`. `xcodebuild build` succeeded; unit suite 1940/1940 green.
- [x] `PG-041` **P2** Board content scaled via `.scaleEffect` drops out of the accessibility tree below a low-enough combined zoom — `Sources/Features/Workspace/WorkspaceView.swift` (the `.scaleEffect(workspace.zoom, anchor: .topLeading)` on `BoardContentLayer`) <!-- src:session opened:2026-08-25 closed:2026-09-02 runs:50 -->
  - Found chasing `WorkspaceBoardUITests.testACornerGripCanStillBeGrabbedWhenZoomedOut`, red on a clean `main` checkout, unrelated to ADR-0022. Two real bugs on the way were found and fixed on `fix/workspace-zoom-pan-drift`: the `−`/`+` zoom buttons left `pan` untouched so repeated clicks drifted the board away from the viewport centre (`WorkspaceController+Viewport.swift`), and macOS reported an inconsistent AX role for a card's `Text` (`AXRole = AXHeading` vs a `StaticText` automation type) that made XCUITest's snapshot query throw instead of reporting "not found" — fixed with an explicit `.accessibilityAddTraits(.isStaticText)` on `NodeCard.swift`. Neither explained the test's actual failure at the time.
  - **The original diagnosis was wrong, and re-verifying it from source before touching code was what unblocked the fix.** It is not an `NSAccessibility` transform-scaling quirk: `BoardGeometry.placeholderZoom` (introduced 2026-08-12, *before* this finding was even opened) makes `BoardContentLayer.cardBody(_:)` swap the real `NodeCard` for a bare `RoundedRectangle` below 25% zoom, for frame-rate reasons — that rectangle simply carries no `Text` and no accessibility label. No architecture change, no Accessibility Inspector session needed once the mechanism was legible from `git log -S placeholderZoom`.
  - Fixed with a small private `accessibilitySummary(for:)` helper in `BoardContentLayer.swift` deriving a short synchronous label per `CanvasNode.Kind` (text content, file/folder name via `workspace.subfolder(for:)`, link title via `LinkCardTitle.read(from:)`, group label, unknown-type fallback), applied as `.accessibilityLabel(...)` on the placeholder `RoundedRectangle` only — the real `NodeCard` branch already had one via its own `Text`. New UI test `testAZoomedOutCardStillHasAReadableAccessibilityLabel` in `WorkspaceBoardUITests.swift`, following the existing `zoomOutWithTheControl`/`element(nodeID:)` pattern. Build: succeeded. Unit suite: 2013/2013 green. New UI test alone: passed (18.1s). Branch: `fix/pg-041-placeholder-accessibility-label`, local, unpushed.
- [x] `PG-036` **P3** `bySubtasks(_:)` trips `cyclomatic_complexity` (11 vs. the configured 10) — `Sources/Core/Tasks/TaskListOptions.swift` <!-- src:session opened:2026-08-24 runs:49 -->
  - Found in the review-triage-fix cycle 2 pass on ADR-0021. Non-blocking (warning, not error); the reviewer suggested extracting the "is this task a heading" walk into its own helper if the function grows further. Left as-is for now rather than forcing a split that isn't otherwise needed.
  - Re-verified live 2026-09-01 (swiftlint 0.65.1): still 11 vs. the default 10 threshold, file otherwise clean at 395 lines. A first attempt extracting three top-level helper functions fixed the complexity but pushed the file from 395 to 415 lines, tripping a brand-new `file_length` warning on a file that wasn't part of the tracked drift — reverted. Fixed instead by replacing the `ungrouped`-loop's two `if let ... { continue }` blocks with a nested `isAlreadyGrouped(_:)` function (a nested function's branches don't count toward the enclosing function's cyclomatic score) and a `for task in tasks where !isAlreadyGrouped(task)` loop; `ProjectKey` stays nested exactly as before, no top-level declarations added, no access-level changes needed. File lands at exactly 400 lines. `swiftlint lint`: 0 violations. `xcodebuild build`: succeeded. Unit suite: 1991/1991 green, unchanged.
- [x] `PG-035` **P3** Warning-level `file_length`/`type_body_length` SwiftLint drift spread across five files by ADR-0021, none crossing the `error` threshold <!-- src:session opened:2026-08-24 runs:49 -->
  - `Sources/Features/Tasks/TasksView.swift` (file_length 445>400, type_body_length 304>250), `Sources/Features/Tasks/TaskComposer.swift` (type_body_length 252>250), `Sources/App/PergamenumApp.swift` (file_length 406>400), `Sources/Index/IndexSnapshot.swift` (file_length 412>400), `Sources/App/CommandActions.swift` (file_length 401>400, type_body_length 252→260, already a warning at base). Found in review-triage-fix cycle 2. Recurs across features (the same `swiftlint-drift` pattern already fixed once for `type_body_length` errors in `TaskParser.swift`/`WorkspaceView.swift`); worth a `swiftlint --fix`-driven pre-commit pass rather than chasing each file individually in review.
  - Re-verified live 2026-09-01 (swiftlint 0.65.1): the drift had grown since filing, and `TasksView.swift` had crossed from warning into an **error** (type_body_length 353/350). Fixed all five with pure code motion into per-concern extension files along existing `// MARK:` boundaries, the same pattern already used for `WorkspaceController`/`WorkspaceBrowser` (PG-056/PG-055): `TasksView.swift` → `+List.swift` (list arrangement/options) + `+Row.swift` (row rendering, context menu, due-date sheet); `TaskComposer.swift` → `+Footer.swift` (schedule/due chips and their popovers); `PergamenumApp.swift` → `VaultCommands.swift` (already self-contained, zero access-level cost); `IndexSnapshot.swift` → `+Search.swift` (tag usage, tag filter, fuzzy search, `ViewCorpus` conformance — zero access-level cost, all touched members already internal); `CommandActions.swift` → `+CanRun.swift` (the whole "can it run" predicate family — the class's stored properties already carried no `private`, so only `place` and `Self.rowCommands` needed a change). Final sizes: `TasksView.swift` 153 (was 517), `TaskComposer.swift` 243 (was 398), `PergamenumApp.swift` 286 (was 406), `IndexSnapshot.swift` 367 (was 422), `CommandActions.swift` 344 (was 452) — all six new extension files land well under both thresholds too. `swiftlint lint` on all 11 touched files: 0 violations. `xcodebuild build`: succeeded. Unit suite: 1991/1991 green, unchanged — no moved member is called by a test directly, confirmed by grep before starting.

- [x] `PG-011` **P3** M10 Navigazione e organizzazione: tabs, split view, tag browser, starred, the missing search operators <!-- src:session opened:2026-08-16 closed:2026-08-20 runs:49 -->
  - Closed with slice 4 (ADR-0012 D8/D9): the search operators, the extended Quick Open and the unlinked mentions.
- [x] `PG-031` **P3** Two UI tests still click `radioButtons["Modifica"]` and `["Lettura"]`, which became icons on 2026-08-19 <!-- src:session opened:2026-08-20 closed:2026-08-21 runs:49 -->
  - Closed by running them: `DesignAndReadingUITests` and `NoteImageUITests` both pass. The words survive as accessibility labels, so the change to icons cost the tests nothing. What the same run *did* find is `PG-033`.
- [x] `PG-013` **P3** M12 La settimana: the week and month scales, the drag that writes, the view controls, the rollover, the event notes <!-- src:session opened:2026-08-16 closed:2026-08-21 runs:49 -->
  - Closed in six slices: the ADR and the SPEC amendment (`d923bda`), the mockups (`f14f21d`), the week, the month, the red days and the sidebar (`553aca0`, `cfc46e3`), the drag and the editable block (`5da6838`), the per-view controls (`c272388`), the rollover (`86c6dab`), the event notes and the weekly review (`3e530c8`).
  - **The rollover amendment is spent and it is narrow.** SPEC §7.3 keeps the NotePlan rule as the default; the setting is off, bounded to a number of days, and shows without moving. Reopening it further needs its own reason.
  - The weekly review shipped as a template with four view blocks and **none of them said «this week»**: the block grammar compared `modified` against a written-out date only, so a literal week went stale the following Monday in silence. Closed by `PG-032` the same week, and the template now asks with `week-start`.
- [x] `PG-012` **P2** M11 Viste: saved queries over the index, rendered as table, board, gallery or calendar <!-- src:session opened:2026-08-16 closed:2026-08-20 runs:49 -->
  - Closed in six slices: the engine in `Core/Query`, the schema bump, the mockups, the four read-only renderers, the board and its guarded write, the connector.
  - The schema bump is **spent**: `IndexCache.schemaVersion` is 3 and carries `embedTargets`. The milestone's one allowance is gone (ADR-0009 §D2).
- [ ] `PG-014` **P3** M13 Vault completo: `note rename|move|trash` under the journal, prompts in the vault, static export, import, AppIntents <!-- src:session opened:2026-08-16 runs:49 gh:#218 -->
  - `note rename|move|trash` is done, closed as `PG-005`. What is left: prompts in the vault, static export, import, AppIntents.
- [x] `PG-019` **P2** The index's second half: drag a section to move it, which is a real text rewrite through `VaultSession.write` with the journal behind it — `Sources/Features/Editor/OutlinePane.swift` <!-- src:session opened:2026-08-17 closed:2026-09-01 runs:50 -->
  - Deferred on purpose when the index shipped: listing and jumping touch nothing, moving a section writes. Belonged with `PG-005`, now closed - ADR-0016 leaves the transaction and `moveFile` ready to use, so a future ADR for this drag does not need to redesign that part, only the gesture and its mockup.
  - Closed via the `concept-to-code` hybrid chain (SPEC.md, manifest `docs/manifests/2026-09-01-pg-019-the-index-second-half-drag-a-sect.manifest.yml`). New pure core `Sources/Core/Markdown/OutlineMove.swift` computes the two-edit move (delete source extent, insert at destination) reusing `NoteFolding.sectionRange` for the extent and `NoteOutline.headingLevel` for the ATX rewrite; applied through `NoteTextView.Coordinator.apply(_:to:)` for one `Cmd+Z` step, then `vault.saveOpenNote()` right after so the move and any pre-existing unsaved edits land in one journalled write. Level rewrite: dropping before row X makes the moved section a sibling of X (X's level, or its enclosing heading's level for an embed row), every nested heading inside it shifting by the same delta - Stefano's explicit choice against the recommended "level unchanged" option. Drag payload `Sources/App/OutlineSectionDrag.swift` (new exported UTType, `Project.swift`), request object `Navigation.moveOutlineSection` mirroring the existing `OutlineJump` pattern, insertion-line drop zones between Outline rows (`OutlinePane.swift`, new `OutlineGap`/`HeadingDraggable`). `xcodebuild build` succeeded (the many SourceKit "cannot find in scope" diagnostics throughout were confirmed stale index lag, not real errors); unit suite 2007/2007 green, including new `Tests/OutlineMoveTests.swift` covering R-01/R-02/R-04/R-05 (three test-authoring mistakes found and fixed by hand-tracing against the implementation before the first real run - two wrong expected trailing-newlines, one accidental no-op scenario). Manual R-10 hand-check done by Stefano on a throwaway vault: level adapts and nested children shift together, file on disk matches the editor, `Cmd+Z` reverts in one step, drop inside the section's own subtree is a no-op with no line, a folded section drags its hidden body, a fenced code block's `#` line is never treated as a heading. `scripts/uitests.sh` run before merge: 105 tests, 1 failure (`ComposerUITests.testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow`), confirmed pre-existing and unrelated - already tracked as `PG-076`, `git diff` for this chain touches nothing under `Sources/Features/Composer` or that test file. Two follow-ups filed from the hand-check, not fixed here: `PG-089` (system "+" cursor instead of a "not allowed" one when hovering an Outline row during the drag - fixing it means introducing a `DropDelegate`-based drop, a pattern this app doesn't use anywhere else yet) and `PG-090` (folding a heading in the Outline pane doesn't hide its nested children from the index list itself - pre-existing M8 behavior, not touched by this chain).
- [x] `PG-030` **P2** A board grouped by `status-*` draws its columns and refuses every drop, because tag.md 5.1 forbids `status-*` on a note except `status-inbox` — `Sources/Core/Conventions/Tag.swift` (`NoteCategory.allowsStatus(_:)`) <!-- src:session opened:2026-08-20 closed:2026-09-02 runs:50 -->
  - Closed in two sessions across two repos. `harness-system/convenzioni/tag.md` was raised to 1.5 in a session scoped to that repo (not this one): §5.1's Note row moved from "forbidden except `status-inbox`" to "allowed, closed vocabulary 4.6, except `status-final`" (reserved for the Deliverable row, since 4.6 defines `status-final` as a delivered *deliverable* version, not a project/note state). Daily-note (zero status) and capture-inbox (`status-inbox` only) exceptions stayed as-is, now special cases of the general limit rather than the whole rule. An ADR was recorded in `harness-system/memoria/decisioni.md` (2026-09-02) with the rationale and rejected alternatives. `Resources/vocabolari.json`'s `status` array was already the full 5-state 4.6 table (`inbox`, `active`, `waiting`, `final`, `archived`) and needed no change beyond its provenance comment — per-category enforcement was always Pergamenum's own concern, not the harness's.
  - This session did the Pergamenum side the harness-system session correctly identified as still open: `TagRules.validate` (`Tag.swift`) and `NoteCategory` still hardcoded "only `status-inbox` on a note" in Swift, independent of `tag.md`'s wording — `NoteCategory.allowsStatus` was an unconditional `false`, gated only by a `tag.value != "inbox"` special case. Replaced with `NoteCategory.allowsStatus(_:)`: an ordinary note allows any status but `final`; daily and capture keep their exact prior behavior (zero status / `inbox` only), preserved as named cases rather than re-derived, so nothing about those two exceptions moved. `VaultSession+BoardDrop.swift`'s doc comment (describing the guard for ADR-0009 §D5) updated to match; it needed no code change — the differential tag-linter guard it describes was already generic, never a `status-*` special case.
  - `Tests/BoardDropTests.swift`: `aDropThatWouldBreakTheTagRulesIsRefusedWithTheReason` repointed from `status-active` (now legal) to `status-final` (still refused, matching the new rule); new `aDropToANonFinalStatusIsAllowedOnANote` added for the positive case. Its first run failed on `vocabularyUnavailable(.status)` — this file's shared `session()` fixture built a `VaultSession` with no `bundledVocabulary:`, deliberately empty per its doc comment, which every prior test in the file routed around via the differential check rather than needing a populated table. Fixed by passing `Bundle.pergamenumResources`'s `vocabolari.json`, the same pattern `VaultSessionTests`/`TaskMarkerLintTests`/`TaskMarkerWriteTests` already use. `xcodebuild build` succeeded; unit suite 2014/2014 green (2 net new tests).
  - Manual verification of M11's acceptance criterion, on a throwaway vault with a `pergamenum-view` board (`group: tag("status-*")`) over two client notes: dragging a card from `status-active` to `status-waiting` wrote the tag, journalled it (undo offered) and re-rendered the card under the new column — refused before this fix, works now. `status-final` refusal not re-verified by hand this round; covered by the repointed unit test.
  - Upstream request `docs/upstream-requests/pg-030-harness-status-tag.md` is now fully actioned and can be archived or left as a record — nothing further pending against `harness-system` for this issue.
- [x] `PG-029` **P3** Nothing on screen says a note draft is parked — `Sources/Features/Editor/NewNoteComposer.swift` <!-- src:session opened:2026-08-19 closed:2026-09-02 runs:50 -->
  - Stepping out of the composer keeps the title, the folder, the topic and the template for the next `Cmd+N` (PG-028), and `Cmd+N` is the only way back to them. Left out of that slice on purpose: a «riprendi» row in the list or a badge on the toolbar button is a design decision, and whether one is needed at all is a question for use rather than for review.
  - Implemented via the `concept-to-code` hybrid chain grouped with PG-089/PG-065 (SPEC.md, manifest `docs/manifests/2026-09-02-pg-089-pg-029-pg-065-outline-drag-cursor.manifest.yml`, R-05/R-06/R-07): badge on "Nuova nota" appears correctly, but Stefano's hand-check on a throwaway vault found it does NOT clear after `Cmd+N` reopens the composer with the restored title, only after discard - a real bug, still being diagnosed, not yet closed.
  - **Diagnosed 2026-09-02, closed as badge-verified-correct, resume failure not reproduced.** Instrumented `hasParkedDraft` and `leaveComposer()` (`VaultController+Notes.swift`) with temporary stderr logging, since removed. Two clean hand-check cycles on the real "Pergamena" vault (`Prova`, then `Prova 2`): both times the badge appeared correctly when stepping out with a title typed, and both times `Cmd+N` resumed the parked draft correctly (`isComposingNote` went back to `true` with the title intact) - the originally-reported failure (composer not reopening) did not reproduce in either attempt. Notable finding on the way: in neither cycle was `leaveComposer()` actually called (0 hits in the log) - stepping out routed instead through one of the four sites in `VaultController+Tabs.swift` that set `isComposingNote = false` directly, bypassing `leaveComposer()` entirely. This disproves the standing theory that `leaveComposer()` re-enters right after `beginNewNote()` sets `isComposingNote = true` - that path was never on the stack in either repro. The real trigger, if the failure resurfaces, is more likely tied to *which* of the five `isComposingNote = false` call sites (one in `leaveComposer()`, four in `VaultController+Tabs.swift`) a given click routes through, and whether one of the `Tabs.swift` sites can fire immediately after `beginNewNote()` under some selection state this session didn't hit. Closing here per the chain's own R-04-style acceptance of a diagnosed-but-not-reproduced outcome; the duplicated `isComposingNote = false` assignment across five sites (one method, four inline) is a design smell worth a follow-up ticket in its own right if the intermittent failure is seen again, distinct from `PG-095` (which is a real, reproduced, separate bug found while testing this one).
- [x] `PG-032` **P2** The view grammar has no relative date, so a saved view cannot say «this week» — `Sources/Core/Query/ViewFilter.swift` <!-- src:session opened:2026-08-21 closed:2026-08-21 runs:49 -->
  - **ADR-0014, then four pieces, merged as PR #77 (`0331b0e`).** Three words - `today`, `today-N`, `week-start` - accepted only where an ISO date already is; `ViewDateBound` holds the bound and parses it; the day is a parameter of `ViewEvaluator.evaluate` and never read from the clock there, which is what lets a test fix its Friday; `onDayChange` re-evaluates the panes; and the weekly review in `SampleViews` lost the workaround and the paragraph apologising for it.
  - A forward bound stays refused until `deadline.next` becomes comparable: `+` is not in the lexer's word set, and widening the alphabet needs a question to justify it.
  - Verified on screen on a Friday, which is the day the two spellings disagree: `week-start` and `today-7` returned different sets, a bad bound named its line instead of emptying the list, and the Viste pane described the filter in the block's own words rather than in today's date.

- [x] `PG-034` **P3** Nothing retraced a step: nine panes, three day scales and tabs, and no way back — `Sources/App/NavigationHistory.swift` <!-- src:session opened:2026-08-21 closed:2026-08-21 runs:49 -->
  - ADR-0015 and the implementation, merged as PR #78. `Cmd+[` and `Cmd+]`, two arrows at the leading edge, two entries in the Vista menu. A place is derived from the state the window already holds, the way `RootView.currentItem` is, and recorded by **one** observer rather than by the eleven methods that move the window.
  - Two mechanisms are values rather than flags, and both for the same reason: the observer runs on the view update *after* the change, so anything set and cleared around the change is already false when it is read. The history holds the destination it expects back; `DayController` describes its last day move rather than flagging one in progress.
  - **Left open on purpose: the Workspace is one place, not one per canvas.** Its folder lives on a `WorkspaceController` held as `@State` inside `WorkspaceView`, so the window cannot read it without lifting the controller to the app. ADR-0015 §D1 records the way out; when that lifting happens for another reason, `case canvas(String)` is a small amendment and one `switch`.
- [x] `PG-033` **P2** The UI suite is seen only when somebody decides to look, and three of its tests were red for days — `UITests/` <!-- src:session opened:2026-08-21 closed:2026-08-21 runs:49 -->
  - Closed with `scripts/uitests.sh` plus a written rule in `CLAUDE.md`: the suite runs before every merge to `main`. A merge is the one moment rare enough to afford twelve minutes and important enough to deserve them.
  - **Refused: a pre-push hook and a fast subset.** Neither is possible, and the reason is the same one that keeps the suite out of `test-cmd`: XCUITest takes the machine, so even a single UI test closes the app in front of whoever is working. The constraint is not the twelve minutes.
  - Considered and deferred: a nightly `launchd` run, which would remove the discipline from the equation but does not run if the Mac sleeps, and a report nobody reads is noise. Worth revisiting if the merge rule turns out not to hold.
  - Found by the run `PG-031` asked for: 67 tests, 3 failures, all three failing identically at `5da6838` and so red since before M12's last slices. Two were a stale placeholder, one was the suite reading the machine's real calendar; all three are fixed in `dec6ba3`.
  - **The point is not the three tests, it is that nobody knew.** The suite is outside `.claude/test-cmd` for a load-bearing reason - with it in there, every turn ended by terminating the app the person at the keyboard was using - so the cost of that decision is a suite whose state is unknown between deliberate runs. Options worth weighing: a second `test-cmd` run only on request, a pre-push hook, or a written rule that the suite runs before every merge to `main`. None of them is free and the choice needs a reason, not a habit.
  - A full run also needs ~12 minutes of nobody touching the keyboard, and leaves several app instances alive that must be killed before the next one (`PG-026`).

- [x] `PG-015` **P3** SPEC amendments §5, §12, plus new §16 Cattura and §17 Viste — `docs/20260811_Pergamenum_SpecApp.md` <!-- src:session opened:2026-08-16 closed:2026-09-02 runs:49 -->
  - Each is applied before the milestone that depends on it, never after.
  - Done 2026-08-20: **§7.3** (rollover as an off-by-default setting), **§7.4** (the view controls) and **§8** (week and month as scales of the day view, event notes, the drag that writes), all authorised by ADR-0013 and applied before M12 starts.
  - **Closed 2026-09-02.** New **§16 Cattura** (ADR-0008) and **§17 Viste** (ADR-0009), each verified against the live implementation before writing (`Sources/Features/Capture/`, `Sources/Connector/VaultCapture.swift`, `Sources/Core/Query/*.swift`) rather than transcribed from the ADR alone. **§5** amended for ADR-0018/0028's partial WYSIWYG concealment (three constructs only: heading `#`, emphasis `*`/`_`, image/PDF embeds — the boundary is explicit, not a general rule) and for the still-current Modifica/Lettura two-mode split, plus PG-019/PG-093's outline drag-to-move/nest. **§12** amended for M10's completed search operators (ADR-0012 §D8: `-term`, `regex:`, `linked:`, `orphan:`, `modified:`, `is:starred`, deliberately no `created:`) and the extended Quick Open/unlinked-mentions (§D9). ADR-0017 §D5's filed debt cleared in the same pass: §4.1's disk-layout tree, §6.5's PDF thumbnail cache path and §12's index path all corrected from `.pergamenum/cache.db`/`.pergamenum/thumbnails/` to the real post-ADR-0017 location under Application Support.
  - Not touched, out of this entry's stated scope: §14's stale `tag.md 1.4` reference (now 1.5 after `PG-030`) and the missing top-level `## 7.` header (structural, `### 7.1`-`### 7.4` sit directly under `## 6.` with no parent) — both pre-existing, neither newly introduced by this closure.

## Roadmap Xcode

Triaged 2026-09-08 from 19 warnings Xcode's issue navigator surfaced on a rebuild (no
errors, build succeeds). 7 were mechanical one-line fixes, applied directly the same day
(`nonisolated(unsafe)`/redundant `await` in `CalendarService.swift`, tautological `as
ViewBlockError` cast in `VaultViews.swift`, unused `store` binding in
`RecordingsController.swift`, two `CalendarDate` `Text` interpolations in
`TasksView+Row.swift`, dangling `AccentColor` build setting). The three below need real
investigation or a manual check before touching code, not a reflexive silence-the-warning
edit — kept here as their own tracked items rather than folded into Backlog.

- [x] `PG-101` **P2** `dropOnRoot`'s `Bool` refuse/accept signal is silently discarded by SwiftUI's `dropDestination` overload resolution, in both sidebar trees — `Sources/Features/Editor/NoteListPane.swift:333,377`, `Sources/Features/Workspace/WorkspaceBrowser+Rows.swift:59,90` <!-- src:session opened:2026-09-08 closed:2026-09-09 runs:1 -->
  - Found triaging Xcode's "Result of call to 'dropOnRoot' is unused" warnings (4 occurrences, 2 per file). `dropOnRoot` returns `Bool` and its own doc comment says explicitly it owes `.dropDestination`'s `action` a `false` for a refused drop (ADR-0026). The current macOS SDK exposes two overloads of `dropDestination(for:action:)` — one `(_:CGPoint) -> Bool` (`@_disfavoredOverload`), one `(_:DropSession) -> Void` — and because both call sites write the closure's second parameter as untyped `_`, Swift resolves to the non-disfavored `Void` overload, discarding the `Bool` outright.
  - **Real regression candidate, not cosmetic**: if this analysis is right, a drop that ADR-0026 documents as refused (e.g. a cyclic move) may currently always animate as accepted, with no error shown. Needs a manual drag-and-drop hand-check (drop a folder into its own descendant, or another refusal case) before/after the fix to confirm current behavior is actually broken and that the fix restores it.
  - Fixed by commit `63e87bf` (`fix(sidebar): make dropDestination refusal signal reach the drop action (PG-101)`), giving each of the 4 closures an explicit `(VaultItemDrag], CGPoint) -> Bool` signature. Merged to `main` as `706d28b` via PR #182. A regression-review pass on that same PR found a fourth site with the identical bug shape, `Sources/Features/Workspace/WorkspaceView.swift:375-377` (a URL-import drop, not a sidebar tree), not covered by this fix — tracked separately as `PG-104`.

- [ ] `PG-104` **P2** File-import drop closure has the same overload-resolution bug PG-101 fixed elsewhere — `Sources/Features/Workspace/WorkspaceView.swift:375-377` <!-- src:review opened:2026-09-09 gh:#219 -->
  - `.dropDestination(for: URL.self) { urls, location in propose(import: urls, at: ...) }` is a single-expression trailing closure calling `propose(import:at:) -> Bool` (`WorkspaceView.swift:471-474`), the same shape PG-101 fixed at 4 other sites: Swift can silently resolve it to the `Void`-returning `dropDestination` overload instead of the `Bool` one, discarding the drop-refusal result. Not touched by PG-101's fix (`63e87bf`), which only covered the note/workspace sidebar tree sites.
  - Found by a reviewer subagent during PR #182's pre-merge regression check (2026-09-09), verified independently by Stefano's own read of the PG-101 fix pattern before merging.
  - Proposed fix: same shape as PG-101 — give the closure an explicit `(urls: [URL], location: CGPoint) -> Bool` signature.

- [ ] `PG-102` **P3** `TableAttachment.swift`'s TextKit2 `attachmentBounds` callback reads a `@MainActor`-isolated `NSView` property from a nonisolated context — `Sources/Features/Editor/TableAttachment.swift:74` <!-- src:session opened:2026-09-08 gh:#220 -->
  - Found triaging Xcode's "Main actor-isolated property 'intrinsicContentSize' can not be referenced from a nonisolated context" warning. `attachmentBounds(for:location:textContainer:proposedLineFragment:position:)` overrides an AppKit `NSTextAttachmentViewProvider` method that is not `@MainActor` in the SDK (TextKit2 can genuinely call it off-main during background layout), yet reads `gridView.intrinsicContentSize` where `gridView: TableGridView?` is `@MainActor`-isolated via `NSView`.
  - `nonisolated(unsafe)` on `gridView` would compile but is a real correctness risk if TextKit2 does call this off-main, since `TableGridView` mutates AppKit view state unsafe to touch off-main.
  - Proposed fix: cache the grid's measured size as a plain `Sendable` `CGSize`, updated on the main actor whenever row/column count or cell content changes, and read that cached value from `attachmentBounds` instead of the live view property.

- [ ] `PG-103` **P3** Three `QuickLookPresenter.swift` AppKit informal-category overrides are nonisolated but touch `@MainActor` state, uncovered by the file's own `@preconcurrency` — `Sources/Features/QuickLook/QuickLookPresenter.swift:51-63` <!-- src:session opened:2026-09-08 gh:#221 -->
  - Found triaging 5 Xcode "Main actor-isolated property 'X' can not be referenced/mutated from a nonisolated context" warnings (`urls` ×1, `dataSource` ×2, `delegate` ×2). `QuickLookHostView`'s `@preconcurrency` conformance to `QLPreviewPanelDataSource`/`QLPreviewPanelDelegate` covers the three formal protocol witnesses, but not `acceptsPreviewPanelControl(_:)`, `beginPreviewPanelControl(_:)`, `endPreviewPanelControl(_:)` — three overrides from AppKit's un-audited *informal* responder-chain category, `nonisolated` by default, that read/write `urls` and `panel.dataSource`/`.delegate`.
  - Proposed fix: mark the three overrides `@MainActor` explicitly (legitimate here — they're `@objc`-dynamic and AppKit only calls them synchronously on the main thread as part of the responder chain's reaction to the spacebar keyDown), with a one-line comment noting they're the same "protocol predates Swift concurrency" situation as the existing `@preconcurrency`, just reached through the informal category.

## Deep Refactor Roadmap

Derived on 2026-09-12 from `docs/deep-refactor/2026-09-11-pergamenum-audit.md` (run
`2026-09-11-2300`, branch `refactor/deep-refactor-2026-09-11`). The run fixed 100 findings
mechanically; the 164 it left open (27 security, 30 report-only/high-risk, 107 skipped by triage)
are triaged here. Every security claim below was re-verified against the source on 2026-09-12
(`assertInsideVault` still `private`, `CanvasStore.url(forBoard:)` still unguarded, no
`quarantine` anywhere under `Sources/`, `NoteExport.escape` still leaves `"` alone, and so on).
Finding ids in backticks are the report's own, so each entry can be traced back to its evidence.

Order: security first, then findings that are really missing or broken features, then P2/P3
debt grouped into chains that share one helper, one seam or one subsystem so they can be done as
one `concept-to-code` chain (or one scoped PR) each. Rules that apply to every entry: no chain
touches `Sources/Features/Pratiche` while another session is working there; every fix that
changes a protected interface (`.claude/protected-interfaces`) goes through an ADR; the UI suite
runs before each merge.

### Tier 1 — Security (all 27 findings are still open; ordered by exposure)

- [x] `PG-122` **P1** [security, chain] Vault-boundary guard is private to `NoteStore.read/write`; nine other file paths join untrusted relative paths onto the vault root unchecked — `Sources/Vault/NoteStore.swift:74`, `Sources/Vault/CanvasStore.swift:25`, `Sources/Vault/VaultSession+Journal.swift:58,106`, `Sources/Vault/ThumbnailStore.swift:43`, `Sources/Features/Workspace/WorkspaceController+Files.swift:12`, `Sources/Features/Workspace/BoardCardMenu.swift:181`, `Sources/Features/Pratiche/PraticheController.swift:775`, `Sources/Features/Pratiche/PraticaSyncEngine.swift:758` — closed by ADR-0041 §D1, the boundary check becomes `VaultBoundary.url(for:) throws -> URL`, the only door to a usable vault-relative `URL` <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-13 gh:#222 adr:0041 pr:254 -->
  - Findings: `security-NoteStore.swift-63e`, `security-CanvasStore.swift-a6f`, `security-URLSchemeTests.swift-868` (P1), `security-VaultSession+Journal.swift-077`, `security-VaultSession+Journal.swift-9b5`, `security-ThumbnailStore.swift-411` (risk high), `security-WorkspaceController+Files.swift-8c7`, `security-PraticheController.swift-96d` (risk high), `security-PraticaSyncEngine.swift-ce5`.
  - Why first: the inputs are externally invokable or externally authored — `pergamenum://canvas?file=../../x.canvas` reads *and writes* a `.canvas` outside the vault (the app is not sandboxed), the MCP `move_note`/`trash_note` tools take a folder/path with no validation beyond non-empty, a `.canvas` node's `file` value is Obsidian-round-tripped data, and a message note's attachment wikilink can be hand-edited or synced in.
  - One fix shape for all nine: promote `NoteStore.assertInsideVault` to an internal `checkedURL(for:)` (or a small `VaultBoundary` helper used by `NoteStore`, `CanvasStore`, `ThumbnailStore` and `WorkspaceController+Files`), make every `FileManager` mutation under `Sources/Vault` go through it, reduce attachment names to `lastPathComponent` where they are parsed (`attachmentFileName(fromWikilink:)`, `storePath(of:)`), and refuse a `pergamenum://` path that escapes before it reaches `routeState.pendingCanvas`. Tests: add traversal cases to `URLSchemeTests`' refusal list and to move/trash/canvas beside the existing read/write ones.
  - Pratiche files are in scope only once `feat/pratiche-*` work is merged; the two pratiche sites can land as a follow-up commit of the same chain.

- [ ] `PG-124` **P2** [security, chain] Three link-opening surfaces and the HTML exporter accept any URL scheme, while the editor's own `MarkdownAttributedText.clickTarget` already gates on http/https — `Sources/Features/Workspace/BoardCardMenu.swift:184`, `Sources/Features/Editor/MarkdownBlocksView.swift:251`, `Sources/Core/Email/HTMLTextReducer.swift:285`, `Sources/Core/Conventions/NoteExport.swift:75,273`, `Sources/App/NoteExporter.swift:62` <!-- src:deep-refactor opened:2026-09-12 gh:#224 -->
  - Findings: `security-BoardCardMenu.swift-b8d` (risk high), `security-MarkdownBlocksView.swift-3cb`, `security-HTMLTextReducer.swift-b63`, `security-NoteExport.swift-154`, `security-NoteExporter.swift-403`.
  - The chain is one input: an email `href` is written verbatim into a message note as `[text](href)` (no scheme filter, no `)` escaping), rendered clickable by `MarkdownBlocksView` (pratiche bodies), and emitted raw into `href="…"` by the HTML exporter whose `escape` never touches `"` — so an anchor can gain an attacker-chosen attribute, and the PDF export's WebKit-backed `NSAttributedString(html:)` may then fetch a subresource (the one network access CLAUDE.md principle 2 does not allow).
  - Fix shape: one `LinkPolicy.isOpenable(_ url:)` in `Sources/Core` (http, https, mailto, message, pergamenum) used by the board link card, `MarkdownBlocksView`, and `HTMLTextReducer` at write time; `NoteExport.escape` gains `"` and `'`; the exporter drops the anchor for a refused scheme. Consider `structure-NoteExport.swift-c0f` (`PG-147`) in the same chain: the exporter is a second markdown grammar and the safest place to fix escaping is the one the reading view already uses.

- [ ] `PG-155` **P3** [security, follow-up to PG-123] The `.eml` sidecar `PraticaSyncEngine.commit` writes into `email/` never gets `com.apple.quarantine`, unlike the attachments in `allegati/` (PR #268 fixed those) — `Sources/Features/Pratiche/PraticaSyncEngine+Messages.swift:660` (the `.eml` write inside `commit(_:request:folder:outcome:)`, relocated here by PR #272's file split; still the only `writeAtomically` call site `AttachmentQuarantine.apply` is not called after) <!-- src:session opened:2026-09-15 kind:fix runs:1 -->
  - Lower severity than PG-123's original half: an `.eml` opens into Mail.app on double-click rather than executing directly, so there is no Gatekeeper-equivalent bypass at stake — but it is still a file of untrusted external origin left unmarked, and `AttachmentQuarantine.apply(to:)` already exists and is a one-line call to add at this second write site. Found comparing this session's own (unmerged, closed as duplicate) PG-123 fix against PR #268's actual merged diff — the session's plan had quarantined both the attachment and the `.eml`, PR #268 quarantines only the attachment.

- [ ] `PG-125` **P3** [security] The whole `PergamenumRoute` (capture text, search query, note path) is logged to the unified log with `privacy: .public` — `Sources/Vault/VaultController+Routes.swift:24,26,93` <!-- src:deep-refactor opened:2026-09-12 gh:#225 -->
  - Finding `security-VaultController+Routes.swift-07d` (risk high). Vault content persists in a system-wide log store readable outside the app. One-line fix per call site: log the route case name publicly and the payload with the default `.private` annotation. Small enough for a direct commit, no chain.

- [ ] `PG-127` **P2** [security, test hygiene] Three `PraticheControllerTests` write `MailStoreLocation.overrideKey` into `UserDefaults.standard`, the shipping app's persistent domain, undone only by a `defer` — `Tests/PraticheControllerTests.swift:289,344` <!-- src:deep-refactor opened:2026-09-12 gh:#227 -->
  - Finding `security-PraticheControllerTests.swift-ce3`. The suite runs every turn through `.claude/test-cmd`; a run killed mid-test (this exact thing happened on 2026-09-12 01:00 when a concurrent xcodebuild SIGTERMed the host) leaves the installed app resolving its Mail store to a deleted temp fixture. Fix: inject a throwaway `UserDefaults(suiteName:)` as `SparkleUpdateControllerTests` does, or thread the fixture root into `MailStoreLocation` explicitly. Pratiche file, so after the current pratiche branch lands.

- [ ] `PG-128` **P3** [security, chain] Guard tests that claim more than they check, and the copy-paste drift in the 21 UI-test launch harnesses — `Tests/SharedSourcesPurityTests.swift:32,75`, `Tests/PlaudIsolationTests.swift:27`, `Tests/MessageDocumentTests.swift:35`, `UITests/PraticheUITests.swift:58`, `UITests/ComposerUITests.swift:26` <!-- src:deep-refactor opened:2026-09-12 gh:#228 -->
  - Findings: `security-SharedSourcesPurityTests.swift-e19` (nil enumerator silently passes both purity guards — `PraticheIsolationTests` already documents shipping this bug and fixes it with `scannedAnyFile`), `security-PlaudIsolationTests.swift-7a1` (the "fully offline" guard greps only `URLSession`; `NWConnection`, `NSURLConnection`, `CFSocket`, `Process` all pass), `security-MessageDocumentTests.swift-7cc` (no hostile-header fixture pins `MessageDocument.quoted()`'s YAML escaping; a bare CR is not escaped at all), `security-PraticheUITests.swift-29d` + `structure-PraticheUITests.swift-d06` (the one UI-test file without `-disablePlaud`), `structure-ComposerUITests.swift-866` (the harness is duplicated in all 21 files, which is how the two flag omissions happened).
  - Do the structural fix once: a `PergamenumUITestCase` base owning stateBase/mailStoreRoot creation, the five isolation flags and teardown, then the three assertion strengthenings. `security-WikilinkNavigationUITests.swift-6d4` (missing `-mailStoreRoot`) is already fixed on the run branch by the structure cluster.

### Tier 2 — Missing or broken features surfaced by the audit

- [ ] `PG-130` **P2** [feature, concept-to-code] `pergamenum://note?id=<id>` can never succeed: `RouteState.noteIDs` is read and never populated, and no stable note id exists anywhere in the index — `Sources/Vault/VaultController+Routes.swift:54,155`, `Sources/Index/IndexSnapshot.swift` <!-- src:deep-refactor opened:2026-09-12 gh:#230 -->
  - Finding `dead-code-VaultController+Routes.swift-6be`. Verified during the run: `IndexSnapshot`/`IndexCache` have no id column; SPEC's URL-scheme table says the id is "registrato nell'indice", which was never implemented on the producer side (M6). Two honest outcomes: implement the id (an index field plus a `schemaVersion` bump, a protected interface, so ADR + chain) or drop the `.noteID` route and the SPEC row so the scheme stops advertising a route that always fails. Decision is Stefano's; the interview should ask what the id was meant to survive (rename? move? both?).

- [ ] `PG-131` **P2** [feature] «Svuota cache e ricostruisci» leaves every rendered thumbnail on disk and in memory, the Impostazioni problems list can never be dismissed, and a forgotten vault's tab session lives in `UserDefaults` forever — `Sources/Vault/VaultController.swift:316`, `Sources/Vault/ThumbnailStore.swift:54,58`, `Sources/Vault/VaultSession.swift:234`, `Sources/Vault/OpenTabsStore.swift:72` <!-- src:deep-refactor opened:2026-09-12 gh:#231 -->
  - Findings: `dead-code-ThumbnailStore.swift-bdd`, `dead-code-ThumbnailStore.swift-3b0`, `dead-code-VaultSession.swift-0f8`, `dead-code-OpenTabsStore.swift-775`. Verified 2026-09-12: `VaultController.clearCache()` calls only `session.clearCache()`; `forgetAll()`/`clearCacheOnDisk()`/`clearProblems()`/`forget(_:)` have zero callers. Each method exists and is the intended affordance; the fix is wiring (`clearCache` → thumbnails, a "Svuota" button beside the problems list, `OpenTabsStore.forget` when a vault leaves the recents list). One small PR, unit tests per wiring.

- [ ] `PG-132` **P3** [feature, verify by hand] SPEC §9's "capture must not raise the app" is described by `PergamenumRoute.raisesApp` but consulted by nothing — `Sources/Core/URLScheme/PergamenumURL.swift:90`, `Sources/App/PergamenumApp.swift:17-18` <!-- src:deep-refactor opened:2026-09-12 gh:#232 -->
  - Finding `dead-code-PergamenumURL.swift-af1`. The AppDelegate comment says "opening the URL already brings the app forward", which for a capture route is exactly what §9 forbids. Hand check: fire `pergamenum://capture?text=x` from Raycast while another app is frontmost; if Pergamenum comes forward, consult `raisesApp` at both URL entry points (`application(_:open:)`, `.onOpenURL`) and re-hide/deactivate for capture. If it does not, delete `raisesApp` as unread.

- [ ] `PG-133` **P3** [feature] Two command catalogues carry an SF Symbol nothing draws: pratiche top-bar buttons and calendar day-cell menu items render title-only — `Sources/Features/Pratiche/PraticaCommand.swift:49`, `Sources/Features/Pratiche/PraticaTopBar.swift:66`, `Sources/Features/Today/CalendarDayCommand.swift:35,52` <!-- src:deep-refactor opened:2026-09-12 gh:#233 -->
  - Findings: `dead-code-PraticaCommand.swift-1b0`, `dead-code-CalendarDayCommand.swift-522`. ADR-0023 §D1's "named once, rendered twice" intends `Label(title, systemImage:)`; decide per surface (menus in this app are mostly text-only by design-system choice) and either render or delete the `symbol` half. Design-token rule applies to any icon added.

- [ ] `PG-134` **P3** [feature, pratiche, after the current pratiche branch] Pratiche polish left half-wired: the «Rigenera» sheet never shows which attachments get rewritten, the attachment chip's context menu ignores its own title catalogue, and `NoteName.sanitized` is dead while its documented callers (email subject, dropped file) went elsewhere — `Sources/Features/Pratiche/AttachmentChipModel.swift:57`, `Sources/Core/Conventions/NoteName.swift:67`, `Sources/Core/Pratiche/PraticaLedger.swift:142` <!-- src:deep-refactor opened:2026-09-12 gh:#234 -->
  - Both `PraticaSyncEngine.swift` line refs (`:537`, `:296`) went stale after the ADR-0045/PG-143 file split (the file is 213 lines now); the Rigenera-sheet and context-menu claims need re-locating in the split files before this can be worked.
  - Findings: `dead-code-PraticaSyncEngine.swift-e75` (`RegenerationPlan.attachmentFileNames`/`rewritesOriginalEML` populated, never read — surface them in the preview sheet as evidently intended), `dead-code-AttachmentChipModel.swift-580` (drive the two context-menu buttons from `contextMenuTitles` so the test stops being a tautology), `dead-code-NoteName.swift-a42` (check whether `PraticaNaming`/`ImportNaming` really cover every title path or a sanitising step is missing), `dead-code-PraticaLedger.swift-df3` (`memberMessageIDs` is dead and its body hand-duplicated in `PraticheController:1475` — call it), `dead-code-PraticaSyncEngine.swift-666` (`PreparedAttachment.digest` write-only — delete).

- [ ] `PG-135` **P3** [feature, workspace] Both board floating panels take a `viewport` they never read, so the format bar and the wikilink completion popup cannot clamp themselves on screen — `Sources/Features/Workspace/BoardFormatBar.swift:81`, `Sources/Features/Workspace/BoardWikilinkCompletionLayer.swift:42` <!-- src:deep-refactor opened:2026-09-12 gh:#235 -->
  - Findings: `dead-code-BoardFormatBar.swift-e0a`, `dead-code-BoardWikilinkCompletionLayer.swift-887`. Hand check with a card at the right/bottom edge of the window: if the bar or popup draws off-screen, implement the clamp the parameter promises (ADR-0027 board-space placement rule still applies); if it never can, delete the parameter at both call sites.

- [ ] `PG-136` **P3** [decisions] Dead state and dead tests that each need a yes/no rather than a refactor <!-- src:deep-refactor opened:2026-09-12 gh:#236 -->
  - `dead-code-VaultSettings.swift-e71`: `harnessRepositoryPath` is decoded/encoded in `settings.json` and read by no feature — either pre-fill «Importa convenzioni…» from it or drop the key (`decodeIfPresent`, so old files still load). Persisted shape, so decide deliberately.
  - `dead-code-ShortcutStore.swift-35f`: `hasConflicts` is kept alive only by `ShortcutTests` — surface it as the "shortcut conflicts" banner it reads like, or delete it with its two assertions (explain in chat first, per the never-delete-a-test rule).
  - `dead-code-AppInfo.swift-7c7` + `dead-code-PergamenumTests.swift-f31`: `AppInfo.tagline` still says "Bootstrap - under active development" at marketing version 1.4 and is asserted by one tautological test — wire it into the About panel or delete both.
  - `dead-code-VaultSession+Tasks.swift-610`: `TaskChange.link` is retained by ADR-0039 as "tested" but no test constructs it — add the one test or correct the ADR.
  - `dead-code-MailLink.swift-f00`, `dead-code-FakePlaudService.swift-0dd`, `dead-code-appcast.py-b96`, `dead-code-WorkspaceBrowser+Tree.swift-5e1`: always-true `#if canImport(AppKit)` guards, an unused fake setter, an unused `fetch` injection seam, an unread `old` parameter — delete or document, five minutes each.
  - Keep as documented: `dead-code-MarkdownReadingView.swift-8fa` (ADR-0029 §D14 seam) and `dead-code-ReminderScheduler.swift-b9e` (protocol witness, false positive).

### Tier 3 — P2/P3 debt, grouped into chains

Performance chains (each finding was skipped by the run because the fix changes behaviour,
isolation or caching semantics; each chain wants a measurement before and after, not a guess):

- [x] `PG-137` **P2** [perf, chain "main-thread I/O"] Synchronous disk work on the main actor on every save, selection and keystroke — `Sources/Vault/VaultSession.swift:196,218`, `Sources/Features/Pratiche/PraticheController.swift:325`, `Sources/Features/Recordings/RecordingsController.swift:562`, `Sources/MCPServer/VaultHost.swift:45`, `Tests/ReleasePipelineTests.swift:189` — closed by ADR-0041 §D9-§D11, disk work moves onto the `VaultDisk` actor with the hash computed before the hop and a per-path sequence number guarding write order; the tail call sites (VaultWrites, VaultHost, ReleasePipelineTests' own process-wait) adopt the same `async` pattern <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-13 gh:#237 adr:0041 pr:254 -->
  - Findings: `perf-VaultSession.swift-11e` (every save re-reads and re-parses the file just written), `perf-VaultSession.swift-fc1` (history snapshot + `thin()` directory listing on the main actor per save), `perf-PraticheController.swift-c6e` (`reloadTimeline` reads every `email/*.md` on the main actor on selection change and after each sync), `perf-RecordingsController.swift-8f0` (`saveDraft` does a full read-modify-write of `plaud-drafts.json` per keystroke), `perf-VaultHost.swift-bbb` (MCP rescans the vault before every tool call, read-only ones included), `perf-ReleasePipelineTests.swift-2cd` (pipes drained after `waitUntilExit`, so a chatty subprocess deadlocks into the 60 s watchdog — the shape of `PG-110`'s flake).
  - All six are isolation changes; the watcher's self-write hash contract (`-11e`) must be re-verified before removing the re-read.

- [ ] `PG-138` **P2** [perf, chain "index and view queries"] Whole-vault task/link aggregations recomputed per redraw — `Sources/Index/IndexSnapshot.swift:171,348`, `Sources/Features/Tasks/LinkedTasksPanel.swift:23`, `Sources/Features/Editor/VaultBrowser.swift:252`, `Sources/Features/Views/RenderedViewBlock.swift:52`, `Sources/Core/Query/ViewEvaluator.swift:232`, `Sources/Core/Query/Glob.swift:19` <!-- src:deep-refactor opened:2026-09-12 gh:#238 -->
  - Findings: `perf-IndexSnapshot.swift-2f0` (materialise `allTasks` once per `replaceAll`/`update`), `perf-IndexSnapshot.swift-c21` (`taskCounts` runs six full passes per sidebar redraw), `perf-LinkedTasksPanel.swift-744` (mirror `BoardTray`'s `.task(id:)` cache), `perf-VaultBrowser.swift-dd0` (inspector sorts every unresolved link per keystroke to show 10), `perf-RenderedViewBlock.swift-8bc` (re-parses the fence per render), `perf-ViewEvaluator.swift-97e` (`linksTo`/`linkedFrom` resolve per record instead of once), `perf-Glob.swift-de6` (pattern folded per record).
  - The two `IndexSnapshot` items change invalidation semantics on `update(_:at:)`; the rest are `@State`/`Context` memoisation with an obvious key.

- [x] `PG-140` **P3** [perf, chain "vault file operations"] Redundant reads and per-item writes in rename/move paths — `Sources/Vault/NoteStore.swift:79,93`, `Sources/Vault/VaultSession+Move.swift:79`, `Sources/Vault/VaultSession+Journal.swift:78`, `Sources/Vault/VaultSession+Starred.swift:37`, `Sources/Vault/VaultScanner.swift:178` — closed by ADR-0041 §D6 (single-parse `NoteStore.read`) and §D8 (`moveItems` computes one plan per batch: one vault walk, one `repointBoardsPlan`, one `moveStars` save, instead of one apiece per item) <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-13 gh:#240 adr:0041 pr:254 -->
  - Findings: `perf-NoteStore.swift-ce3` (search/rename callers pay a full record derivation to read `text` — add `NoteStore.text(_:)`), `perf-NoteStore.swift-bf6` (frontmatter parsed twice per read), `perf-VaultSession+Move.swift-2a6` (a batch move of N rows walks the whole vault and re-encodes every `.canvas` N times — needs a per-batch plan), `perf-VaultSession+Journal.swift-462` (moved file read twice), `perf-VaultSession+Starred.swift-4d9` (starred file rewritten once per moved note), `perf-VaultScanner.swift-a0b` (root path standardised per file). Pairs naturally with `PG-145`'s dedupe of the same code.

- [ ] `PG-141` **P2** [perf, chain "email and pratiche pipeline", after the current pratiche branch] Allocation-heavy parsing on the sync path and per-render filesystem stats in the timeline — `Sources/Core/Email/HTMLTextReducer.swift:148,352`, `Sources/Core/Email/MIMEDecoder.swift:116`, `Sources/Core/Email/MailStoreReader.swift:150,281`, `Sources/Core/Pratiche/MessageDocument.swift:185,243`, `Sources/Connector/VaultPratiche.swift:299`, `Sources/Features/Pratiche/AttachmentChip.swift:33`, `Sources/Features/Pratiche/PraticaTimelineView.swift:41` <!-- src:deep-refactor opened:2026-09-12 gh:#241 -->
  - Findings: `perf-HTMLTextReducer.swift-b66`/`-0d5`, `perf-MIMEDecoder.swift-33c` (three array allocations per line of a 10 MB base64 body), `perf-MailStoreReader.swift-53f` (N+1 conversation queries) and `-495` (a directory listing per row), `perf-MessageDocument.swift-938` (13 linear frontmatter scans per file) and `-9a2`, `perf-VaultPratiche.swift-cf2` (formatter per row), `perf-AttachmentChip.swift-09a` (three `fileExists` per chip per render), `perf-PraticaTimelineView.swift-c8b` (context-menu construction O(n²)). `CRLF` divergence was measured during the run for the reducer, so every reducer change needs the fixture corpus (`Tests/EmailFixtureCorpus.swift`) run before and after.

- [ ] `PG-142` **P3** [perf, leftovers] Board gestures, and tests that re-read the tree — `Sources/Features/Workspace/BoardContentLayer.swift:195`, `Sources/Features/Workspace/WorkspaceController+Gestures.swift:32`, `Tests/SharedSourcesPurityTests.swift`, `UITests/WorkspaceOpenStateUITests.swift:116`, `scripts/mcp-smoke.py:107` <!-- src:deep-refactor opened:2026-09-12 gh:#242 -->
  - Findings: `perf-BoardContentLayer.swift-85c` (a `fileExists` stat per visible node per frame — cache folder-ness per document load), `perf-WorkspaceController+Gestures.swift-4b4` (snap candidates rebuilt per drag tick), `perf-SharedSourcesPurityTests.swift-91f`/`-21e`, `perf-WorkspaceOpenStateUITests.swift-846`, `perf-mcp-smoke.py-a69`. `perf-support.js-1e7` is upstream (generated bundle), no action.

Structure chains (file splits and dedupes; none is regression-safe unattended, each wants its
own PR with the affected suites run before and after):

- [ ] `PG-144` **P2** [structure, chain "editor"] `NoteTextView.Coordinator` is the god object every editor feature since ADR-0018 landed on, and its decoration passes are copy-pasted twins — `Sources/Features/Editor/NoteTextView.swift:9,321`, `NoteTextView+Coordinator.swift:12`, `NoteTextView+ViewBlocks.swift:54`, `NoteTextView+Tables.swift:130`, `EditorDecorationDelegate+CheckboxRendering.swift:28`, `EditorDecorationDelegate+TableRendering.swift:30`, `CompletionPanel.swift:191`, `Sources/Core/Editor/ListContinuation.swift:228` <!-- src:deep-refactor opened:2026-09-12 gh:#244 -->
  - Findings: `structure-NoteTextView+Coordinator.swift-d95` (31 state fields, ~2,270 lines of extensions — sub-controllers per feature, not a mechanical move), `structure-NoteTextView.swift-e7a` (37 stored inputs; group into `FindInputs`/`OutlineInputs`/`EmbedInputs`), `-c97` (`updateNSView` 123 lines), `structure-NoteTextView+ViewBlocks.swift-00b` (`applyViewBlocks`/`applyTables` same 60-line skeleton), `structure-NoteTextView+Tables.swift-a5b` (three caret-rescue copies), `structure-EditorDecorationDelegate+CheckboxRendering.swift-552` and `+TableRendering.swift-a75` (paragraph/attachment substitution preambles duplicated), `structure-CompletionPanel.swift-602` (`NeverKeyPanel` + `makePanel` duplicated with `FormatBarPanel`), `structure-ListContinuation.swift-f19` (five primitives verbatim from `LineFormat`), plus the test splits `structure-MarkupHidingTests.swift-7e8` (1386 lines) and `structure-MarkdownStylerTests.swift-ee1`.
  - Order: dedupes first (they shrink the god object without touching TextKit dispatch), then input grouping, then the sub-controller design as its own ADR.

- [x] `PG-145` **P2** [structure, chain "vault layer"] Three copies of the vault walk, six copies of the apply-plan loop (already drifted), `rename` re-implementing `renamePlan`, and `import SwiftUI` in the vault layer — `Sources/Vault/CanvasStore.swift:178`, `FolderFileOperations.swift:310`, `BoardFileOperations.swift:160-288`, `NoteFileOperations.swift:226`, `VaultSession+BoardDrop.swift:66`, `VaultScanner.swift:44`, `VaultController.swift:4`, `Sources/Index/IndexCache.swift:116`, `Sources/Connector/VaultWrites.swift:123` — closed by ADR-0041: `VaultController` and its seventeen extensions relocate to `Sources/App` (Task 3, §D6), the three vault walks become one shared helper built from the boundary resolver (Task 4), and the six apply-plan copies become one, with `rename` calling `renamePlan` instead of re-implementing it (Task 5) <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-13 gh:#245 adr:0041 pr:254 -->
  - Findings: `structure-CanvasStore.swift-5cb` (one `VaultWalk.enumerate` carrying the exclusion rule), `structure-FolderFileOperations.swift-895` (verified 2026-09-12: the `BoardFileOperations` copies record failures but never append `rewrittenPaths`, so a board rename's summary under-reports what it rewrote — latent today because board rename is not exposed to the connectors, real the day it is), `structure-NoteFileOperations.swift-97f` (the run already shared the repoint step; `rename` should now compute `renamePlan` and perform it), `structure-VaultSession+BoardDrop.swift-ac4` (journal borrow-and-return block ×3 — `withJournalOnDisk`), `structure-VaultScanner.swift-c2e`, `structure-VaultController.swift-9c5` (`import SwiftUI` with no SwiftUI symbol — the ADR-0001 §D1 trap; one-line delete), `structure-IndexCache.swift-dc5` (two near-identical INSERT loops), `structure-VaultWrites.swift-dd6` (`moveNote`/`renameNote` twins), and the test-support moves `structure-VaultTests.swift-b6d`/`-3ac` (`TemporaryVault` used by 37 files lives in a topic file — `Tests/VaultTestSupport.swift`).
  - Interacts with `PG-122`: the boundary helper and the walk helper are the same seam; do `PG-122` first so the dedupe inherits the guard.

- [ ] `PG-146` **P3** [structure, chain "workspace"] Over-limit Workspace types and a drop-feedback machine duplicated with Today — `Sources/Features/Workspace/WorkspaceView.swift:11`, `WorkspaceController.swift:12`, `WorkspaceBrowser.swift:109`, `CardTextView.swift:359`, `FormattingTextView.swift:14`, `BoardHandles.swift:22`, `BoardTray.swift:89`, `Sources/Features/Views/ViewBoardRenderer.swift:38` <!-- src:deep-refactor opened:2026-09-12 gh:#246 -->
  - Findings: `structure-WorkspaceView.swift-f8e` (358-line body — `+Toolbar`/`+Board`), `structure-WorkspaceController.swift-52b` (`Tool` enum into `WorkspaceTool.swift`, editing/saving extensions), `structure-WorkspaceBrowser.swift-ac5` (sheets into `WorkspaceFolderSheets`), `structure-CardTextView.swift-05d`/`-d6d`, `structure-FormattingTextView.swift-fd1` (six responsibilities, split like `CardTextView+*`), `structure-BoardHandles.swift-5b2` (grip view duplicated by `CropHandleView`), `structure-BoardTray.swift-0f2`, `structure-ViewBoardRenderer.swift-c3e` (`Drop` struct, outcome decoding and banner duplicated with `DayController`/`TodayView` — one `WriteDropOutcome` + `DropBanner`). UI-heavy: `WorkspaceBoardUITests`/`WorkspaceOpenStateUITests` before and after.

- [ ] `PG-147` **P2** [structure, chain "core and app shell"] A second markdown grammar in the exporter, plus over-limit files and duplicated tables across Core, DesignSystem, Calendar and the app shell — `Sources/Core/Conventions/NoteExport.swift:138`, `Sources/Core/Email/HTMLTextReducer.swift:388`, `Sources/Core/URLScheme/PergamenumURL.swift:33`, `Sources/DesignSystem/Theme.swift:318`, `Sources/Calendar/CalendarService.swift`, `Sources/Features/Editor/NoteListPane.swift`, `Sources/Features/Editor/VaultBrowser.swift:270`, `Sources/App/RootView.swift:9`, `Sources/Features/Settings/EditorSettings.swift:47` <!-- src:deep-refactor opened:2026-09-12 gh:#247 -->
  - Findings: `structure-NoteExport.swift-c0f` (risk high: `MarkdownHTML.render` re-parses the dialect `MarkdownBlockParser`/`MarkdownInlineParser` already parse and has drifted — render from those parsers, golden-file test first; do it inside `PG-124` so the escaping fix lands on one grammar), `structure-HTMLTextReducer.swift-abe`/`-6ef` (entity table as data), `structure-PergamenumURL.swift-ab3` (per-host parsers — pairs with `PG-122`'s route refusal), `structure-Theme.swift-d64` (two font-weight ladders that can drift between SwiftUI and AppKit), `structure-CalendarService.swift-d72` (552 lines, five concerns), `structure-NoteListPane.swift-b4a` (`SelectionOutcome` and move machinery out of the view), `structure-VaultBrowser.swift-8ae` (`ConformanceText` consumed by six modules from a view file — move to Core), `structure-RootView.swift-c0b`, `structure-EditorSettings.swift-11d`.
  - Progress 2026-09-20 (branch `247-pg-147-…`): closed `structure-Theme.swift-d64` (one `FontWeightStop` ladder, two mappers over it), `structure-HTMLTextReducer.swift-abe`/`-6ef` (entity table as `HTMLEntities.named`; the numeric-reference parse stays code), `structure-VaultBrowser.swift-8ae` (`ConformanceText` now in `Sources/Core/Conventions`, one token changed: `VaultController.CreationError` to `VaultSession.CreationError`), `structure-NoteListPane.swift-b4a` selection half (`NoteSelectionRule`), `structure-CalendarService.swift-d72` value types (`CalendarEvent`/`CalendarReminder`/`CalendarStore`/`EventKitStore+Dates`, 543 to 381 lines). No `private` widened.
  - Still open, on purpose: `NoteExport.swift-c0f` (goes inside `PG-124`/#224 behind a golden-file test), NoteListPane's move machinery (view-coupled, widens seven members), `PergamenumURL.swift-ab3` (seven independent cases, no duplication), `RootView.swift-c0b` (line-count-only, widens twelve), `EditorSettings.swift-11d` (only the testable typography math is worth extracting). New follow-up worth its own entry: `ConformanceText` and the connector's `"\($0)"` render the same violations two different ways (`VaultPayloads.swift:142-152`); unifying them changes connector JSON.


## Blocked / Decisions Needed

- [x] `PG-002` **P2** SPEC §7.3 rollover as an off-by-default option: the reopening needs Stefano's approval, ADR-0013 records it <!-- src:session opened:2026-08-16 closed:2026-08-20 runs:56 -->
  - Approved 2026-08-20 with M12's slicing, knowing the SPEC rejects rollover by name. ADR-0013 §D1 carries the argument and the three narrowings that keep it an amendment rather than a reversal; §7.3 is amended in place. The number is 13, not the 12 the roadmap predicted: 10, 11 and 12 went to transclusion, templates and tabs.
- [x] `PG-018` **P3** Direct editing in the NotePlan sense, hiding the syntax while typing: SPEC §14 excludes it from v1, reopening needs an ADR — `docs/20260817_TextKit2_live_editing.md` <!-- src:session opened:2026-08-17 closed:2026-09-03 runs:56 -->
  - The study measured what it would cost. The mechanism exists and preserves the file; the expensive part is caret navigation over hidden characters, which folding did *not* need.
  - **Partially delivered 2026-08-22/2026-09-01 (ADR-0018, ADR-0028) — this entry is not closed by that.** Concealment shipped for exactly three constructs (heading `#`, emphasis `*`/`_`, image/PDF embeds drawn inline) plus list-marker substitution, deliberately scoped as "three named constructs, not the rule" (ADR-0018's own words) — the app still has a separate, non-editable Lettura mode (`MarkdownReadingView`/`MarkdownBlocksView`) toggled per-tab, not the single always-WYSIWYG editor this entry originally asked about.
  - **Full scope reaffirmed by Stefano 2026-09-02**, in the same session as `PG-015`'s SPEC closure: one editor, no Modifica/Lettura toggle, everything `MarkdownBlocksView` renders today (tables included) moves into the editable `NoteTextView`. Reopens three standing decisions, not one: **SPEC §14** itself ("source mode con stile è sufficiente"), **ADR-0005 §D2** (the Diario reading pane's premise that it sits beside an *unchanged* source editor), and **ADR-0018**'s explicit three-construct boundary.
  - Extending ADR-0018 §D1's paragraph-substitution mechanism (same character count in and out) plausibly covers most of the remaining gap: blockquote `>`, `---` rules, wikilink/link bracket concealment, strikethrough `~~` — each one more delimiter pair or attachment, the same shape as what already shipped.
  - **GFM tables are the real risk, not an extension of the existing mechanism.** `MarkdownBlocksView+Table.swift` draws a real grid; ADR-0018 D1's invariant (identical character count, one substituted paragraph) has no analogue for an editable multi-cell grid inside a TextKit 2 `NSTextView` — new work, not a fourth delimiter.
  - **Closed 2026-09-03 (ADR-0029, `concept-to-code` chain, manifest `docs/manifests/2026-09-02-editor-wysiwyg-unification.manifest.yml`).** Full scope delivered: the Modifica/Lettura toggle is gone everywhere (`ShortcutCommand.readingMode` included, §D13), a GFM table is now a real `NSTextAttachmentViewProvider`-hosted editable grid rather than a fourth delimiter substitution (§D-table), and all four §D16 manual probes (nested first responder, `[[` tooltip, table line-fragment height, per-cell `Cmd+Z` granularity) are confirmed by hand. Merged as PR #164/#165; unit suite 2082/2082 green. One accepted platform limitation surfaced during probe 4 and left as documented, not reopened: the caret doesn't blink right after `NoteTextView+TableCaret.swift`'s Enter/Backspace redirect next to a table, a known AppKit/TextKit 2 issue (Apple FB17103305).
  - Also in scope when this proceeds: removing the per-tab `isReadingMode` toggle (`NoteTabBar.swift:70`, `VaultBrowser.swift:132`), whatever becomes of `MarkdownReadingView`/`MarkdownBlocksView` once nothing switches to them, and the UI tests that click the mode control (same family `PG-031` already touched once for an icon change).
  - Scoped by Stefano to go through the `concept-to-code` chain (interview → ADR superseding ADR-0005 §D2 and ADR-0018, amending SPEC §14 → plan), not as a direct code change.

## PROGETTI — pratiche (derived from 2026-09-09-pratiche.manifest.yml) — COMPLETED 2026-09-10

ADR-0036 (`docs/adr/0036-pratiche.md`): a «pratica» is a vault folder that fills itself from a
published copy of Apple Mail's Envelope Index (system `SQLite3`, db+wal copied, WAL recovered,
`quick_check`, own indexes, atomic rename), one markdown file per message plus `allegati/`,
membership via `pergamenum-dossier-*` keys, two-lane timeline, `pratica.md` edited only in the
inspector, no network, connectors read-only. Plan:
`docs/superpowers/plans/2026-09-09-pratiche.md`, 10 tasks, R-01…R-41. Branch `feat/pratiche`,
planning artifacts committed as `8fb5c6b`.

**All 10 tasks implemented and merged into `feat/pratiche` (`step5-report.json`:
`tasks_completed: [1..10]`, `tasks_failed: []`). Step 6 (review-triage-fix, 5 cycles, Codex
astra/xhigh) converged and its fixes are in commit `9cd6595`. Manifest transitioned to
`completed` 2026-09-10. Not yet merged to `main` — no PR opened by explicit choice; `main` merge
still needs the full `scripts/uitests.sh` run per the Working agreements.** 4 architectural
findings from review deliberately deferred, tracked as `PG-116`..`PG-119` below — **all four closed
2026-09-10** by the follow-up plan `docs/superpowers/plans/2026-09-10-pratiche-pg105-pg108.md`
(ADR-0036 §D21, §D22, §D23; PG-119's live probe). §D23.5's tray-row work stays open as `PG-109`.

- [x] 1. Two probes on the real Mail store (index `message_id` column form, `.emlx` digit-fan
      rule — Stefano present, schema/paths only), code-built fixture store, `MailStoreReader` (R-02, R-03, R-19)
- [x] 2. `.emlx` reader, MIME decode, HTML→light markdown, quote split, one `message://` builder
      measured against Mail (R-04–R-07)
- [x] 3. Dossier line codec, `PraticaNaming`, message document, membership rule, ledger (R-01, R-08, R-12–R-14, R-37)
- [x] 4. Sync: atomic writes, attachments (100 MB threshold), `.eml` retention, pending bodies,
      deletions never delete files (R-09–R-11, R-15, R-16)
- [x] 5. `PraticaSyncEngine` actor, triggers (pane open, window key, FSEvents debounce), Full Disk
      Access probe per trigger, connector purity test (R-17–R-19, R-38)
- [x] 6. Pane (list · timeline · inspector), two lanes, `surface.received/sent/entry` tokens in both
      themes, sidebar row before Registrazioni (R-23–R-27, R-32, R-33, R-39; screens 1a, 1b, 1c, 1g)
- [x] 7. Commands (Escludi, Sposta in, Aggiungi anche a, Chiudi/Riapri), «Da smistare» tray,
      Nota/Telefonata entries via inspector caret, daily-note line (R-28–R-31, R-34)
- [x] 8. «Nuova pratica…» wizard (Cmd+Opt+P), Mail seed via AppleScript, «Aggiungi a pratica da
      Mail…» picker (Cmd+Shift+P), drop tracer bullet (R-20–R-22; screens 1d, 1e)
- [x] 9. Settings › Pratiche eleventh tab (fits 700×560, re-measure toolbar collapse), `perg
      pratiche`/`pratica` + MCP reads (R-35, R-36; screen 1f)
- [x] 10. Accessibility identifiers, SPEC §14 amendment note, `-mailStoreRoot` in all 19 UI-test
      files, regeneration diff sheet, full `scripts/uitests.sh` before merge (R-18, R-33, R-39, R-40, R-41)
      — **caveat: the full UI suite is named as done by this task's own description, but the
      full-suite run for the merge-to-`main` gate itself has not happened yet** (no PR/merge this
      session by explicit choice); still owed before `main`.

Deferred, recorded per R-41 (each needs its own chain, none blocks this one):
- [ ] ChatGPT inside Pergamenum (Stefano, 2026-09-09: "vorrei portare chatgpt dentro a pergamenum")
      — replaces the rejected «Esporta per ChatGPT» file export; no network in any feature, so the
      shape is undecided
- [ ] «Apri come board»: a pratica rendered as a `.canvas` in the Workspace
- [ ] URL-scheme entry point `pergamenum://pratica/add?message=…`
- [ ] Connector write access to pratiche (`perg`/`pergamenum-mcp` stay read-only in ADR-0036 §D19)

Open risks named by the ADR: torn index copy while Mail writes (`quick_check` + one retry, then
«Mail sta scrivendo»); 84k of 129k `.emlx` are `.partial` so first imports show many pending rows;
shortcuts Ctrl+Cmd+P / Cmd+Opt+P / Cmd+Shift+P still to be measured against `com.apple.symbolichotkeys`.

## Steps — workspace-tasks-notes-integration (derived from 2026-08-24-workspace-tasks-notes-integration.manifest.yml)

ADR-0021, three caret markers on a task line (`^[[<canvas>]]`, `^id(N)`, `^parent(N)`), no new
storage. Plan: `docs/superpowers/plans/2026-08-24-workspace-tasks-notes-integration.md`, 10 tasks.

- [x] 1. `TaskItem`/`TaskParser` gain `workspacePath`/`localID`/`parentLocalID`, `annotatedLinks`
- [x] 2. `TaskProgress`, `IndexSnapshot.tasks(assignedToWorkspace:)`/`subtasks(of:)`/`progress(ofProject:)`
- [x] 3. `VaultSession+Tasks` writes: `.workspace` change, `TaskDraft.parent`, `captureSubtask`
- [x] 4. Caret-safe note rename (workspace/id/parent markers survive a rename rewrite)
- [x] 5. `NoteViolations.taskMarkers` advisory linter (duplicate workspace, orphaned parent)
- [x] 6. `NoteTree.build(fromPaths:)`, `CanvasStore.allBoards()`, `WorkspaceBrowser` tree view
- [x] 7. `WorkspaceReferences.notes(in:)`, `BoardChrome` TASK ASSEGNATI / NOTE REFERENZIATE
- [x] 8. `TaskGrouping.subtasks` ("Progetti"), `TaskListOptions.bySubtasks(_:)`
- [x] 9. `.taskAddSubtask` shortcut, `TaskComposer` sub-task mode, `WorkspacePicker` sheet
- [x] 10. End-to-end UI test (`WorkspaceIntegrationUITests`); found and fixed two real bugs
      (`VaultController.selectedTask` staleness, macOS AX-identifier propagation on
      `DisclosureGroup`)

Status: implementation complete, 1527/1527 unit tests, 71/71 UI suite (one pre-existing
unrelated flake noted separately). Two review-triage-fix cycles run at Gate 5, zero regressions,
zero weakened tests. Gate 5.06 specialized review run, zero CRITICAL/IMPORTANT from error
handling, two IMPORTANT type-design findings filed as `PG-037`/`PG-038` (non-blocking). Awaiting
Step 7 commit.

## Steps — workspace-folder-board-separation (derived from 2026-08-26-workspace-folder-board-separation.manifest.yml)

## Steps — editor-page-typography-noteplan (derived from 2026-09-04-editor-page-typography-noteplan.manifest.yml)

## Steps — ambiguous (candidates: pg-099-views-board-renderer-orphaned-by, pergamenum-view-query-builder) — nothing derived

## Steps — ambiguous (candidates: pg-099-views-board-renderer-orphaned-by, pergamenum-view-query-builder, wikilink-click-navigation, rileva-nuove-controparti-wizard-pratiche) — nothing derived

## Steps — ambiguous (candidates: pg-099-views-board-renderer-orphaned-by, pergamenum-view-query-builder, wikilink-click-navigation, rileva-nuove-controparti-wizard-pratiche, pratiche-attachment-reliability-bugs) — nothing derived

## Not yet specified

- [ ] `PG-197` macOS showed «"Pergamenum.app" è danneggiato e non può essere aperto» (Gatekeeper, «scaricato in data sconosciuta») on 2026-09-21; which copy is unknown: `/Applications/Pergamenum.app` and the three newest DerivedData Debug builds have no `com.apple.quarantine` attribute and pass `codesign --verify --deep --strict` <!-- src:session kind:fix opened:2026-09-21 -->
- [ ] `PG-182` A copy of `/Applications/Pergamenum.app` (parent `launchd`) appeared mid-run in both full UI runs of 2026-09-19 (PID 40783 at 20:19, PID 54040 at 20:58); who launches it is unknown, and Stefano's own hands are not excluded <!-- src:session kind:fix opened:2026-09-19 -->
  - 2026-09-21: PR #364 makes the run save a launch log (`<run>.launch`, 5 s either side of the copy's start time) whenever an installed copy appears, so the next occurrence carries evidence; the cause is still unknown and the log has never yet been captured on a real occurrence.
  - 2026-09-20, third time: the full run started at 11:22 ended with `/Applications/Pergamenum.app` (PID 62920) running, left open by the script; the run's 12 reds were the external monitor (`PG-188`), not this.
  - 2026-09-20, fourth time: `/Applications/Pergamenum.app` (PID 60195) appeared during the full run started at 13:23 and the script left it open.
- [ ] `PG-183` The Stop hook runs `.claude/test-cmd` at each turn end, collides on `build.db` with a UI run started in the background («database is locked»), and afterwards keeps reporting the record of the first collision (19:59) at every later turn end with nothing building; the hook lives in vibe-coding-lean, outside this repo · 2026-09-20: the `build.db` collision half is closed by #335 (a UI run now builds into `build/uitests-dd`, and the header of `scripts/uitests.sh` names PG-183), the stale-record half is untouched and was not re-verified today <!-- src:session kind:fix opened:2026-09-19 -->
  - 2026-09-20, second observation: after `tuist generate --no-open` the hook kept reporting the unit build failure of 12:43 (`CanvasTemporaryRoot` not in scope) at three consecutive turn ends while a direct run of the same command was green (3270/3270); `tuist generate` changes only gitignored files, which the tree fingerprint (`git status` + `git diff HEAD` + untracked hashes) does not see, so the record is replayed until the 300 s TTL expires.
- [ ] `PG-186` A 25 s UI run's focus log caught Mail and GitKraken taking frontmost; candidate cause for the unexplained `PG-180` red and related to `PG-182`, not confirmed <!-- src:session kind:fix opened:2026-09-20 -->
  - 2026-09-21: PR #364 records a non-empty focus log as the `focus-taken` signal, which makes a red run `contaminated` and reruns only its failed tests once; the cause of the focus theft is not confirmed and no real run has yet shown the signal.
  - 2026-09-20: full run at 13:23 ended 98/120, 22 reds in Workspace classes (`WorkspaceOpenStateUITests` 10, `WorkspaceBoardUITests` 5, `WorkspaceIntegrationUITests` 3, `WikilinkNavigationUITests` 2, `SectionToolbarsUITests` 1, `TaskTimeUITests` 1), GitKraken frontmost 24 times and the installed app running (`PG-182`). The same 22 tests alone at 14:12, installed app closed and other apps minimised: 22/22 green in 376 s, focus taken 4 times (Finder). Cause not confirmed; `PG-188` (external monitor) not excluded, the display setup was not checked.
- [ ] `PG-187` Spike: run the UI suite in a Tart macOS 27 VM so it stops taking the person's machine, starting with `TaskCategoriesUITests` and `WorkspaceBoardUITests` and stopping on any red <!-- src:session kind:roadmap opened:2026-09-20 -->
- [ ] `PG-193` `ViewQueryOutOfScopeTests.untouchedDefaultsCommitAsTheMinimalHandWrittenFence(body:explicitDefaults:)` failed once in the Stop hook during the PG-172 build, then passed alone (8/8) and in two full unit runs (3314/3314); cause not investigated, no ledger code on its path <!-- src:session kind:fix opened:2026-09-20 -->

## Project Map

- **Entry point**: `Sources/App/PergamenumApp.swift` · CLI `Sources/CLI/main.swift` · MCP `Sources/MCPServer/main.swift`
- **Modules**: `Core` (pure, no SwiftUI, compiled by all three binaries) · `Vault` `Index` (files and the rebuildable cache) · `Connector` (the one vault API behind `perg` and `pergamenum-mcp`) · `Features` `DesignSystem` `App` `Calendar` (app only)
- **Build & test**: `tuist generate --no-open` after editing `Project.swift` or after any git op that adds or removes a file · test-cmd: `xcodebuild -workspace Pergamenum.xcworkspace -scheme Pergamenum -destination 'platform=macOS' -only-testing:PergamenumTests test` — the Stop hook runs it every turn, so the UI suite is deliberately not in it (see CLAUDE.md)
- **Design notes**: `docs/20260817_TextKit2_live_editing.md` — what TextKit 2 allows, measured against the installed SDK, and the two mechanisms for making the display differ from the file
- **Key ADRs**: 0001 architecture · 0002 naming and shortcuts · 0003 composers · 0004 hours on task dates · 0005 the diary pane · 0006 timeline hours as a setting · 0007 the AI connector · 0008 the global capture panel · 0009 views are queries · 0013 the week and the tasks that slip
- **Invariants**: no network call in any feature · every piece of content is a readable file · the index is rebuildable and never the source of truth · frontmatter is exactly `date`, `tags`, `related`, `aliases` · tags are flat and namespaced, no `/` · no colour or font in a view without a token · never commit to `main`, never force-push · a new connector capability goes in `Sources/Connector/`, never in one front end · CI (ADR-0044) builds all three targets and runs `PergamenumTests` on every PR and push to `main`, advisory only, not required — the merge gate (PG-196, 2026-09-22) is `PergamenumTests` plus the in-process tests (`Tests/HostedViewPrototypeTests.swift` and siblings); the 17 surviving GUI tests run via `scripts/uitests.sh --affected` at merge and don't block it, a full run is required only before a release

## Done

- [x] `PG-129` **P3** [security, scripts] Release/install scripts trust their inputs — closed 2026-09-23 by PR #441 (merge commit on `main`, GitHub issue #229 closed): `scripts/appcast.py` rejects non-https `--feed-url`/`--download-url` and validates every republished enclosure against the trusted `github.com/istefox/pergamenum-updates/releases/download/` prefix (self-test extended 10 → 13 checks, all green); `scripts/install-cli.sh` picks the newest Release binary via `ls -dt` and fails naming every candidate when DerivedData holds more than one, instead of `find | head -1`'s glob-order pick — `scripts/appcast.py:67`, `scripts/install-cli.sh:63` <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-23 gh:#229 pr:441 runs:1 -->
- [x] `PG-215` -> #429 **P4** [chore] `docs/plans/ui-suite-determinism-handoff.md` and `docs/plans/pg-187-tart-vm-ui-suite-handoff.md` proposed options for the UI-suite merge gate as if the decision were still open, while `SPEC.md` and `docs/adr/0053-test-seams-for-the-in-process-merge-gate.md` had already implemented it. Closed 2026-09-23: issue no longer open upstream (#429 closed 2026-09-23T12:20:40Z), and both handoff docs are gone from `main` <!-- src:session kind:chore opened:2026-09-23 closed:2026-09-23 runs:4 promote:2026-09-23 -->
- [x] `PG-139` **P3** [perf, chain "editor hot path"] Per-keystroke costs in the styler and the slash/emoji menu — closed by PR #435: `ListNesting.levels(in:)` replaces the backward per-line walk with one forward pass, `MarkdownAttributedText.StyleContext` memoizes per-span attribute dictionaries within one styling pass, `EntryRanking.matching` precomputes each entry's lowercased/word-split form once per call instead of once per ranking tier — `Sources/Features/Editor/MarkdownStyler.swift`, `Sources/Core/Editor/ListNesting.swift`, `Sources/Features/Editor/MarkdownAttributedText.swift`, `Sources/Features/Editor/RankableEntry.swift`, `Sources/Core/FuzzyMatch.swift` <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-23 gh:#239 pr:435 -->
  - Findings: `perf-MarkdownStyler.swift-c80`, `perf-MarkdownAttributedText.swift-64e`, `perf-RankableEntry.swift-e05` — all three closed. Measured after the fix only (500-line nested list: 0.0076s, 2000-line: 0.0300s, ~linear); "before" was not measured in place. Manual GUI smoke test (list/heading rendering, both completion menus in a running build) deferred, tracked separately as `PG-217`.
- [x] `PG-222` -> #430 **P4** [chore] Duplicate promotion of `PG-215`: two concurrent `/project-tasks` sessions raced the same ledger entry and each opened its own GitHub issue for it (`PG-216`'s own race shape) — #430 ("SPEC.md already approves the merge gate two handoff docs still treat as open") is the same `docs/plans/ui-suite-determinism-handoff.md`/`docs/plans/pg-187-tart-vm-ui-suite-handoff.md` fix as `PG-215`/#429, already closed there. Renumbered from a duplicate `PG-215` local id 2026-09-23 (found by `ledger-merge.sh`'s self-check refusing to write with two `PG-215` entries) — `docs/plans/ui-suite-determinism-handoff.md`, `docs/plans/pg-187-tart-vm-ui-suite-handoff.md` <!-- src:session kind:chore opened:2026-09-23 closed:2026-09-23 runs:4 promote:2026-09-23 duplicate-of:PG-215 -->
  - Closed 2026-09-23: both handoff docs now carry a head "Scope note" naming `SPEC.md`/ADR-0053 as the settling artefacts, plus inline markers on the spent sections (determinism §0/§4/§9, Tart §2/§11 Q1) so a reader cannot mistake them for open work. Nothing was deleted or rewritten: the live analysis sections (determinism §2/§3/§5/§6, Tart §4) stand untouched, and `PG-187` (the Tart spike's own question) stays open in `Not yet specified` — this closure settles only the merge-gate question, which was never PG-187's to answer. Folded in on the same commit: `CLAUDE.md:144`'s `scripts/uitests.sh` comment still described the retired before-every-merge rule, contradicting the merge-gate paragraph 137 lines below it in the same file — corrected to match.
- [x] `PG-185` -> #343 **P3** [fix] The shared UI verdict of #338 was proven on a real run for one `partial` verdict only: writing a `full` verdict, skipping a second run on a verified tree, a red verdict, the launch rerun and the lock across two sessions were exercised with a stand-in `xcodebuild` and `ps`, not for real — `scripts/uitests.sh` · Real run 2026-09-20: `TaskCategoriesUITests`, 2 tests, 0 failures, 26.3 s, tree `556758b`; the `Executed N tests` line parsed on Xcode 27 and `--status` then showed the `partial` verdict. The baseline for `b74d561` was imported by hand from PG-184's full run, marked as imported. · Real runs 2026-09-20, tree `d0c81b0`: a `red (full)` verdict was written and `--status` showed it; the script refused to start while `/Applications/Pergamenum.app` was open; a partial rerun of 22 tests on a dirty tree wrote no verdict. <!-- src:session kind:fix opened:2026-09-20 closed:2026-09-23 runs:5 promote:2026-09-20 -->
  - Closed 2026-09-23: a real full run went red on 2 tests with signal `focus-taken` (Finder 6, GitKraken 3), the automatic rerun of just those 2 went green, and `--status` recorded `green (full)` for tree `4d11903` (commit `17114f56`). Re-running immediately after was skipped with "questo albero è già verificato", proving the verified-tree skip for real. The refusal to start while another session's `xcodebuild` was running (observed twice, unstaged) is the real cross-session contention this ticket asked to prove, though not the exact "two `uitests.sh` runs racing for the same lock" shape. Incidental fix along the way: this worktree's generated Xcode project was stale (last `tuist generate` 2026-09-20, missing two files added 2026-09-22 17:22), causing a build failure misread as a zero-test run on the first attempt — `tuist install && tuist generate --no-open` resolved it, no code change.
- [x] `PG-206` -> #403 **P4** [chore] `NoteFileOperations.rename(_:to:knownPaths:)` and `move(_:toFolder:)` have no caller anywhere in `Sources/` — `VaultSession+Files` builds a `NoteFileOperations` but calls only `renamePlan`/`movePlan`/`danglingLinks` on it, so this is a second, unguarded implementation of note rename reachable only from `Tests/NoteFileOperationTests.swift` and `Tests/NoteRenameCharacterizationTests.swift` — `Sources/Vault/NoteFileOperations.swift` · Found while adopting the ADR-0054 §D6 `writeRepoint` guard across every `.canvas` writer (this dead code was in scope for the guard, per the plan's Task 6 file list, even though nothing calls it). Named, not deleted, by ADR-0054 §D8: those characterization tests pin bytes deliberately, and deciding whether to delete the dead implementation or give it a real caller is its own decision, not this chain's to take. <!-- src:session kind:chore opened:2026-09-22 closed:2026-09-22 runs:4 adr:0054 promote:2026-09-22 -->
  - Closed by `docs/adr/0055-note-write-guard-and-the-dead-rename-performer.md` and its plan (`docs/plans/pg-205-pg-206-note-write-guard.md`): `NoteFileOperations.rename(_:to:knownPaths:)`, `move(_:toFolder:)`, the private `repointBoards` and `trash(_:knownPaths:)` are deleted — none had a production caller, and the app's one note performer stays `VaultSession.renameNote`/`moveNote`/`trashNote`, which already read a plan from this type and perform it inside a transaction. Every assertion the eighteen characterization/operation tests made is re-pointed at a live surface (`renamePlan`/`movePlan`/`danglingLinks` at the plan level, or the session-level performer for a disk-level outcome), none dropped or disabled.
- [x] `PG-205` -> #402 **P3** [fix] The **note** half of the board/folder rename verbs still writes through an unguarded `store.write`, so a folder rename can clobber a dirty editor buffer and `syncOpenNote`'s conflict prompt never sees it — `Sources/Vault/BoardFileOperations.swift:160-162`, `Sources/Vault/FolderFileOperations.swift:398-400`, `Sources/Vault/NoteFileOperations.swift:231-233` · Named as a deliberate scope boundary by ADR-0054 §D8 (`docs/plans/pg-213-workspace-autosave-race.md`, Task 6/7): fixing it needs the same `refusals` plumbing through these outcomes' note-write half that this chain gave the board half, a different buffer (the editor, not a Workspace board), a different conflict surface, and roughly doubles the chain — deliberately left for its own pass rather than folded in. <!-- src:session kind:fix opened:2026-09-22 closed:2026-09-22 runs:4 adr:0054 promote:2026-09-22 -->
  - Closed by `docs/adr/0055-note-write-guard-and-the-dead-rename-performer.md` and its plan (`docs/plans/pg-205-pg-206-note-write-guard.md`): the note half of `BoardFileOperations.renameBoard` and `FolderFileOperations.renameFolder` now writes through the one guarded note door, `NoteStore.writeGuarded` (ADR-0055 §D1), exactly as the board half already writes through `CanvasStore.writeRepoint` — a note whose bytes moved on since the plan was read is refused, not clobbered. Correction to this ticket's own text, recorded rather than silently fixed: «`syncOpenNote`'s conflict prompt never sees it» is true only for a dirty tab that is not the focused note — a `.md` write does reach `VaultWatcher`, `store.write` records nothing in `selfWrittenHashes`, so the change does reach `VaultSession.reconcile` as external and does reach the editor for `openNote`. The remaining gap — `VaultController.reconcile` only handling `openNote`, dropping an external change to a dirty tab that is not the focused note — is filed separately as `PG-211` (ADR-0055 §D5/§R-10; renumbered twice at merge — first from `PG-209`, which `main` had already assigned to an unrelated CI-trigger ticket, then from `PG-210`, which a separate concurrent `main` entry already held in `Done`).
- [x] `PG-208` **P3** [fix] `PraticaConversationRenumberingTests.syncRenumbersAFollowedConversationWhoseIdMailHasChanged()` failed once during a full `PergamenumTests` suite run — `dossier.conversations` stayed `[112409]` instead of renumbering to `[550001]` per §D23.4 — but passed clean in isolation (`-only-testing:PergamenumTests/PraticaConversationRenumberingTests`) and in an immediate full-suite rerun (3502/3502 green): order- or shared-state-dependent, not a deterministic regression from the concurrent PG-213/ADR-0054 merge landed the same session — `Tests/PraticheControllerTests.swift:335` <!-- src:session kind:fix opened:2026-09-22 closed:2026-09-22 runs:4 promote:2026-09-22 gh:closed#406 pr:408 -->
  - Fixed 2026-09-22 (PR #408, `fe071b8`): root cause was `MailStoreLocation.overrideKey`, a process-global `UserDefaults.standard` key six test files set and `defer`-remove around a sync. Each suite is `@Suite(.serialized)`, which only serializes tests *inside* one suite — Swift Testing runs different suites in parallel, so a concurrent suite's `set`/`removeObject` could land inside another's sync window. The failing test's own 50ms sleep between two syncs (deliberate, to move a fixture's mtime) was exactly the exposure window. New file `Tests/MailStoreOverride.swift` gives the key one door: a process-wide async gate (`NSLock` + `CheckedContinuation` waiters, modelled on `ReleasePipelineTests.ProcessOutcomeBox`) that only one test at a time may hold, replacing all 14 direct `UserDefaults` set/`defer` pairs across `Tests/PraticheControllerTests.swift`, `Tests/PraticaLiveSyncRelocatedMidRunTests.swift`, `Tests/PraticaLedgerFolderTrashEdgeTests.swift`, `Tests/PraticaLiveSyncTrashedMidRunTests.swift`, `Tests/PraticaLiveSyncUnloadedLedgerReadTests.swift` and `Tests/MailStoreReaderTests.swift` (the last file's two "no override" tests take the gate too, without setting it, since a concurrent suite's override must not leak into them). A real secondary bug found in the same sweep: `anAbandonedRegenerationPreviewNeverClobbersASupersedingOne`'s first early-`return` released the gate while its `@MainActor` task was still resolving against it — fixed by awaiting that task before returning. Production code untouched (`Sources/Core/Email/MailStoreLocation.swift`'s process-global read is the correct design for `-mailStoreRoot`, ADR-0036 §D7). Verified: the six affected suites green in isolation, then the full unit suite green 5 consecutive times (3506/3506, 5 known issues, unchanged across runs). Not fixed by this: **PG-120** (`publishReportsMailIsWritingOnATornCopyAfterOneRetry`), which passes `fixture.root` explicitly and never touches the global override — a different flake, still open. Deduplicated at merge with a locally-numbered duplicate `PG-208` opened by this same branch independently (identical topic, both `runs:` histories folded into this entry).
- [x] `PG-204` **P3** [fix] `pollingStopsAndMarksExpiredAfterTheInjectedTimeout` failed once in the Stop hook's unit run while the UI suite was running: a 25 ms injected timeout checked after a real 150 ms sleep, which a loaded machine does not honour — `Tests/RecordingsControllerTests.swift:339` <!-- src:session kind:fix opened:2026-09-21 closed:2026-09-22 runs:2 gh:closed#393 -->
  - Fixed 2026-09-22: replaced the fixed 150ms sleep with the existing `waitUntil` polling helper (`Tests/DiaryControllerTests.swift`), converging as soon as `pollExpired` contains the recording id instead of depending on a fixed wall-clock margin. PR #397 (`a9849e90`), merged. Unit suite green.
- [x] `PG-201` **P3** [fix] A Swift Testing issue recorded outside a test's task context (`Test «unknown»`) does not fail `xcodebuild`: one run printed "failed with 11 issues" and still ended `** TEST SUCCEEDED **`, exit 0, so a `NeverKeyWindow` refusal raised from a run-loop callback would show only in the log — `Tests/HostedViewSupport.swift` <!-- src:session kind:fix opened:2026-09-21 closed:2026-09-22 runs:2 gh:closed#392 -->
  - Fixed 2026-09-22: `NeverKeyWindow` now tracks every refused call in a plain array, exposed as `HostedView.refusals` and checked with an ordinary `#expect` inside the test's own task, independent of `Issue.record`'s out-of-task attribution. PR #397 (`a9849e90`), merged. Unit suite green.
- [x] `PG-195` -> #367 **P2** [fix] `UITests/TaskCategoriesUITests.swift` `testLinkingANoteFromTheCategoryViewMakesItTheHome` was merged with PR #359 without ever being run, and the picker and the inspector's «Assegna una categoria…» menu were never checked by hand: `scripts/uitests.sh --status` had no verdict for the tree and the merge skipped the UI suite. Run `scripts/uitests.sh --affected` (or the full suite) and confirm the test, then a hand check on a throwaway vault: link, link a second note and confirm the first lost its key · `UITests/TaskCategoriesUITests.swift` <!-- src:session opened:2026-09-20 closed:2026-09-22 kind:fix runs:4 promote:2026-09-21 -->
  - Issue no longer open upstream: #367 no longer listed as open on GitHub, confirmed at `/project-tasks` Step 6c on 2026-09-22.
- [x] `PG-210` **P2** [fix] Category view didn't show which note a category was linked to, and back-navigation from "Vai alla nota" (opened from the category view) lost the category and landed on "Oggi" instead — fixed in PR #401 (`6b0253d`): the linked-note button now shows the note's title, and `Navigation.taskSelection` plus a new `Destination.taskSelection` case (ADR-0015 §D1 amendment) anchor the Attività pane in the navigation history the same way `.workspaceBoard` already does. Unit suite green (3471/3471 including 4 new tests in `WindowPlaceTests.swift`/`NavigationHistoryTests.swift`). <!-- src:session kind:fix opened:2026-09-22 closed:2026-09-22 runs:1 pr:401 -->
  - Originally numbered `PG-205`/`PG-206` at the time these bugs were found and fixed; both numbers were independently reused by a concurrent `chore/todo-sync` run for an unrelated file/chore pair before this entry reached `main` (see `PG-205`/`PG-206` above), so this entry was renumbered to `PG-210` on the next `/project-tasks` run to clear the collision.
- [x] `PG-202` -> #374 **P3** [fix] `Sources/Features/Workspace/WorkspaceView.swift` `type_body_length` is an error at HEAD: 351 lines after #371 against the 350 limit (358 before it); SwiftLint is not on CI, so nothing shows it — `Sources/Features/Workspace/WorkspaceView.swift` <!-- src:review kind:fix opened:2026-09-21 runs:2 promote:2026-09-21 closed:2026-09-22 -->
  - Fixed 2026-09-22: `toolbar(previewURLs:)` extracted verbatim into `WorkspaceView+Toolbar.swift`, the same `extension WorkspaceView` split this file already uses for drawing/folder-verbs/creation. Struct body 351 → 308 lines, error cleared (`swiftlint lint --quiet` on the file shows only the pre-existing 250-line and `file_length` warnings). Unit suite 3462/3462, `perg` and `pergamenum-mcp` builds unaffected.
- [x] `PG-200` -> #373 **P3** [fix] Follow-ups of the review of #371 (ADR-0053 seams 1 to 3): the `enter(folder:)` tests call `enter` and not `BoardCardActions.open`, so `WorkspaceOpenStateUITests` `:370`/`:383` lose their call-site cover when deleted; ADR-0053 §D3 says the empty-path difference is unobservable, but the browser converges to nil on the next rescan (`WorkspaceBrowser.swift:132-135`); three test headers still read "RED: the seam is a placeholder" — `Tests/WorkspaceEnterFolderTests.swift`, `Tests/WorkspaceRowLabelTests.swift`, `Tests/BoardCardSummaryTests.swift` <!-- src:review kind:fix opened:2026-09-21 runs:2 promote:2026-09-21 closed:2026-09-22 -->
  - Fixed 2026-09-22: the two folder-card double-click tests now call `BoardCardActions(workspace:vault:).open(card)`, the real production path, instead of replicating its internals; two new tests restore the `.ambiguous`/`.notFound` resolution coverage that change would otherwise have dropped. The §D3 empty-path question is answered, in `WorkspaceEnterFolderTests.swift`'s doc comment and in the ADR itself: a hand-written `.canvas` can carry the empty path, and the old/new spelling difference was never user-visible because `WorkspaceBrowser.rebuild()` drops a selection naming no row on the next rescan. All three stale "RED: the seam is a placeholder" paragraphs removed. Unit suite 3462/3462.
- [x] `PG-126` **P3** [security] `PlaudHTTPClient` interpolates a service-supplied recording/proposal id into the request path with `appending(path: id)`, which preserves `/` and `..` — `Sources/Features/Recordings/PlaudHTTPClient.swift:53-55`, `Tests/PlaudHTTPClientTests.swift:36`
  - Issue no longer open upstream: #226 closed by PR #385 (`9704323e`), merged 2026-09-22. Fixed with a single resolver, `PlaudHTTPClient.endpoint(_:_:_:)`, validating a service-supplied id against an explicit charset and rejecting `.`/`..` before the URL is built; new tests confirm nothing is sent over the wire for a malicious id, and a real 32-hex id still reaches the intended path unchanged.
  <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-22 gh:#226 pr:385 -->
- [x] `PG-194` **P3** [fix] `scripts/uitests.sh` records a `red (full)` verdict for a run that executed 0 tests (the UI runner failed to initialise: «Timed out while enabling automation mode», 74 s, 2026-09-20 14:38) and that verdict replaces the informative one in `--status` (98/120 with 22 reds, 13:23); the cause of the timeout was not investigated — `scripts/uitests.sh`
  - Fixed by PR #364 (`060ba94`), merged 2026-09-21: a run that executed no test is `error` (`run_outcome`, `scripts/uitests.sh:350-352`), exits 1, and `write_verdict` refuses to store it, so the tree's existing verdict is left as it was (`:419-424`); pinned by `--self-test` (63 checks). The UI runner's «Timed out while enabling automation mode» is now a `launch-failure` signal; its own cause was still not investigated.
  <!-- src:session kind:fix opened:2026-09-20 closed:2026-09-21 -->
- [x] `PG-190` -> #350 **P3** [fix] `private let praticaNote` is declared four times in four test files, contents not compared; the same smell as `PG-177`, left out of #340 on purpose — `Tests/PraticheConnectorTests.swift:24` · The four: `PraticheConnectorTests.swift:24`, `PraticaLedgerFolderTrashTests.swift:19`, `DossierWriterTests.swift:14`, `PraticheLinksConnectorTests.swift:15`. Recorded in the ADR-0051 amendment of 2026-09-20 as out of scope.
  - Issue no longer open upstream: #350 closed by PR #361, merged 2026-09-21. The four `praticaNote` constants renamed by content (`praticaNoteWithTagsAndConversation`, `praticaNoteWithForeignKey`, and `minimalPraticaNote` for the two byte-identical ones, kept as separate private constants); rename only, unit suite 3350/3350.
  <!-- src:session kind:fix opened:2026-09-20 closed:2026-09-21 -->
- [x] `PG-166` -> #297 **P3** [roadmap] `linkCategory`/`unlinkCategory` had no UI entry point: the write primitive existed and was tested but nothing in the app could create the link · `Sources/Vault/VaultSession+Categories.swift`
  - Issue no longer open upstream: #297 closed by PR #359 (`dde3778`), merged 2026-09-20. Entry points on both sides (category view and row menu via `CategoryNotePicker`, note inspector via `AssignCategoryMenu`), one home per category through `VaultSession.setCategoryHome`; decisions in `docs/plans/pg-166-category-link-note-entry-point.md`. The new UI test was not run at merge time, tracked as PG-195.
  <!-- src:session opened:2026-09-17 kind:roadmap closed:2026-09-20 -->
- [x] `PG-174` -> #321 **P3** [fix] `Tests/MailStoreReaderTests.swift` carries three SwiftLint warnings that predate PG-120's diagnostic change and were left alone there: `file_length` (447 lines, limit 400), `type_body_length` (276, limit 250) and `line_length` at line 336 (123 characters, limit 120). Same class as PG-143's error-level debt in the pratiche files, but warning-level here; a split into `MailStoreReaderTests+Copy.swift` / `+Queries.swift` extensions would clear the first two → `Tests/MailStoreReaderTests.swift:17,336` (2026-09-20) <!-- src:session opened:2026-09-19 kind:fix runs:4 promote:2026-09-19 -->
  - Issue no longer open upstream: #321 closed by PR #352 (`b91b9c6`), merged 2026-09-20. `MailStoreReaderTests.swift` is now 347 lines (limit 400) with its fixtures in `MailStoreReaderTests+Fixtures.swift` (105 lines); `swiftlint` printed nothing on either file when checked on 2026-09-20.
- [x] `PG-172` -> #312 **P2** [correctness] `PraticheController`'s ledger writers save without checking the ledger was ever loaded: `ledger` starts as `.empty`, only `load(from:)` fills it (from `PratichePane`'s `.task`, the settings tab, `AddToPraticaSheet`, `NuovaPraticaWizard`; the app never calls it at launch), and `moveLedgerState`, `recordSyncOutcome` and `updateTray` each write it back straight after mutating memory, so a folder move or sync outcome before the pane was opened can overwrite `…/vaults/<id>/pratiche/ledger.json` with an empty or partial ledger and lose every pratica's `importedMessageIDs`, `entries`, `notInStore` (not recoverable from disk), `lastOpenedAt` and `trayCount`. Second variant, reviewer confidence ~55, not traced: a ledger loaded for vault A and written into vault B's `ledger.json` after a vault switch without opening the pane. Fixed for `forgetLedgerState` only (PG-169's review round 1). Closing it properly means a «ledger loaded for URL X» marker, a design decision, not a per-call guard — `Sources/Features/Pratiche/PraticheController+Ledger.swift` · `docs/plans/pg-169-pratica-ledger-forgotten-on-folder-trash.md` §"Follow-ups decided at /build close" (2026-09-20) <!-- src:review opened:2026-09-19 kind:roadmap runs:3 promote:2026-09-19 closed:2026-09-20 -->
  - Issue no longer open upstream: #312 closed by PR #346 (`ce7f10f`), merged 2026-09-20. `PraticheController.ledger` is `private(set)` with a `LedgerOrigin` marker, all six save sites go through `updateLedger(_:_:)`, an unreadable ledger is never saved over, and a vault change resets the vault-scoped state (ADR-0052). Follow-ups: `PG-191`, `PG-192`.
- [x] `PG-178` -> #332 **P3** [fix] `tripleWidth` was 213 in `TabBarMockup` and 208 in `HistoryMockup` and `TemplateMockup`, so the same three-part row rendered at two widths across the gallery — `Sources/Features/DesignGallery/MockupGalleryView.swift` (2026-09-20) <!-- src:session kind:fix opened:2026-09-19 runs:4 promote:2026-09-20 -->
  - Issue no longer open upstream: #332 closed by PR #340 (`ca4d6ec`), merged 2026-09-20. 208 was the wrong value: `MockupPage` pads 24 a side before clamping, so content gets 672, and 208 gave a row of 704. `rowWidth`, `pairWidth` and `tripleWidth` now derive once in `MockupGalleryView`; hand-checked in the gallery on Cronologia, Template, Tab, Tag e preferiti and Menzioni.
- [x] `PG-177` -> #331 **P3** [fix] `private let taskNote` was declared twice with different content, in `TaskViewTests.swift:32` and `TaskDropTests.swift:47` (2026-09-20) <!-- src:session kind:fix opened:2026-09-19 runs:4 promote:2026-09-20 -->
  - Issue no longer open upstream: #331 closed by PR #340, merged 2026-09-20. Renamed `fiveViewsNote` and `dropTargetNote`, not merged: `TaskDropTests` reads its fixture by line offset. The same smell on `praticaNote` is `PG-190`.
- [x] `PG-176` -> #330 **P3** [fix] Near-identical `~Copyable` temp-vault fixtures under `Tests/`; `TaskVault` was a strict subset of `TemporaryVault` (2026-09-20) <!-- src:session kind:fix opened:2026-09-19 runs:4 promote:2026-09-20 -->
  - Issue no longer open upstream: #330 closed by PR #340, merged 2026-09-20. Re-measured at 20 duplicates, not 22: 21 struct declarations became 10, `~Copyable` ratchet 12. Recorded in the ADR-0051 amendment of 2026-09-20. `PergamenumTests` 3267/3267; the UI suite ran 108/120 green, and the 12 reds were the external monitor (`PG-188`) and passed 12/12 on rerun, so not one clean run of 120.
- [x] `PG-179` -> #333 **P3** [fix] `MockupGalleryView.Screen` kept three parallel `switch`es in step by hand — `Sources/Features/DesignGallery/MockupGalleryView.swift` (2026-09-20) <!-- src:session kind:fix opened:2026-09-19 runs:4 promote:2026-09-20 -->
  - Issue no longer open upstream: #333 closed COMPLETED 2026-09-20 08:22Z, fixed by `8953466` (three `switch`es merged into one `Page` table), on `main`.
- [x] `PG-184` **P2** [fix] PR #329 (`fix(uitests): retry a lost drag and rerun a failed launch`) was merged with CI green and without the full `scripts/uitests.sh`, by Stefano's choice at the merge gate after the gate had named the rule; the drag retry (`PG-180`) and the launch rerun (`PG-181`) were only exercised on single-test selections and, for the script, with a stand-in `xcodebuild`, so the first full run on `main` (`bd18e82`) is still owed — `scripts/uitests.sh:1` (2026-09-20) <!-- src:session kind:fix opened:2026-09-20 runs:2 regression-of:PG-157 -->
  - Paid 2026-09-20: full `scripts/uitests.sh` on `main` at `b74d561` (after #329 and #334 merged), 120 tests, 0 failures, verde, 1439 s. No launch failure occurred, so the `PG-181` rerun was not exercised in a real run. A copy of `/Applications/Pergamenum.app` started during the run and was left open by the script; the run was not affected. Closed as a paid debt like `PG-157`, not as a solved pattern: the rule (full suite before every merge) is still not enforced by anything.
- [x] `PG-181` **P3** [fix] `TaskTimeUITests.testAScadenzaWithAnHourCanAlsoBecomeABlockOnItsDay` failed at 0.7 s in a full run with «Failed to get launch progress ... The LaunchServices launcher has returned an error»: the app never started, so this is a launch failure, not the «manca la riga dell'orario» assertion `PG-076` tracks for the same test — `UITests/TaskTimeUITests.swift:45` (2026-09-20) <!-- src:session opened:2026-09-19 kind:fix runs:1 -->
  - Seen 2026-09-19 in the full run of 20:42, test started 20:56:44. The installed copy that appeared in that run started at 20:58:16, after it, so it does not explain the failure. Not seen elsewhere: the other 119 tests launched.
  - Cause not proven, and the log narrows it to a race in the launch machinery rather than a test: launchd did spawn the new process (pid 53629, 20:56:44.451) and Xcode still reported the launch as failed, 2 ms after the previous instance's coalition was evicted, 0.13 s after its teardown. It is the first test of a suite and 1 launch failure in about 120. It cannot be provoked on demand.
  - Fixed in `scripts/uitests.sh`, by treating it as what it is, a launch that never happened and not a test outcome: a test whose only message is «Failed to get launch progress» is run once more, alone, after the debris of the failed launch is closed, and the report says so. Any other failure is never retried, and a run with a real failure beside a launch failure still exits non-zero. Checked with a stand-in `xcodebuild` in three cases (launch failure then pass: exit 0 and one rerun naming the test; launch failure plus a real failure: exit 65; a real failure alone: no rerun), not with a real launch failure.
- [x] `PG-180` **P3** [fix] `SidebarMoveUITests.testDraggingASingleBoardRowMovesTheFile_R15` failed once in a full `scripts/uitests.sh` run (17.3 s, «il drag di una singola riga board non ha spostato il file») and passed in the 14-test drag subset and 3/3 alone, so its cause is unknown; not the `press` defect of `PG-162`, which the drag helper fixed — `UITests/SidebarMoveUITests.swift:317` (2026-09-20) <!-- src:session opened:2026-09-19 kind:fix runs:1 -->
  - Seen 2026-09-19 in the full run of 20:03, on `main` with the drag helper. An installed `/Applications/Pergamenum.app` started at 20:19 during that run; whether this test had already run by then is not readable from the log, so interference is neither proven nor ruled out. Not seen in the 20:42 run.
  - Cause measured 2026-09-20, not the app and not the drag helper: the pointer is shared with whoever is at the machine. The same test 20 times each way: cursor moved by another process during the run 13 failures, every one at ~17 s with the source row still in place and the same message; machine untouched 0 in 30 (and 0 in 20 after the fix); 18 busy-loop processes on 18 cores 2 failures in 20, no different from an idle machine, so CPU load is not it. The `/Applications` interference guessed above is ruled out by the log: the installed copy started at 20:19, the failing test at 20:14:54. Traces of a failing and a passing iteration are identical up to the drop, and preselecting the row first (the multi-row shape) changed nothing (4 in 40).
  - Fixed in `UITests/SidebarMoveUITests.swift`: the test repeats the drag once when the file has not moved after 4 s and the source row is still there (a lost drag leaves it in place). Under the same cursor interference 1 failure in 20 (was 13), and the one left lost both attempts; machine untouched 20 in 20. Only this test got the retry, the other drag tests have not shown the failure; the same three lines apply to any that does.
- [x] `PG-162` **P3** [fix] `scripts/uitests.sh` full run on `fix-159-160-bugs` (118 tests, 13 failures, 12 reproducible in isolation) shows no code regression: every failure is a drag/press gesture producing no effect or an element rendered-but-not-hittable, none a wrong-value logic failure, and PG-159/PG-160's own changed files (Pratiche backend only) share zero overlap with any failing test's area — `UITests/DayViewUITests.swift`, `UITests/DiaryUITests.swift`, `UITests/SidebarMoveUITests.swift`, `UITests/WorkspaceBoardUITests.swift` (2026-09-19) <!-- src:session opened:2026-09-15 kind:fix runs:19 promote:declined closed:2026-09-19 -->
  - The 13: `DayViewUITests.testTheMonthFollowsTheDividerAndHasNoGripOfItsOwn` (240.0/240.0), `DiaryUITests.testABlockIsDeletedFromItsSheet`/`testABlockIsMovedByDragging`/`testDraggingOverEmptyTimeBlocksItOut` (sheet/section never opened), `SidebarMoveUITests.testDraggingAFolderRowMovesItAndItsContents_R15`/`testDraggingAMultiRowSelectionMovesAllOfThem_R15`/`testDraggingASingleBoardRowMovesTheFile_R15`/`testUndoOfADragMoveRestoresTheFile_R15` (element not hittable mid-drag), `WorkspaceBoardUITests.testACardInsideAGroupCanBeSelected` (260.0/320.0), `testACornerGripCanStillBeGrabbedWhenZoomedOut` (240.0/260.0), `testACornerGripResizesTheCard` (240.0/340.0), `testAGroupStillMovesWhenDraggedByItsFrameAndCarriesWhatItHolds` (0.0/100.0), `testTheArrowToolConnectsTwoCards` (nil connection id). 105/118 non-gesture tests in the same run passed clean.
  - Personal-app-interference theory is **ruled out**: an isolated rerun of just these 13 tests (`-only-testing`, ~4 minutes, 2026-09-16 07:56, personal `/Applications/Pergamenum.app` confirmed not running throughout) reproduced 12/12 failures at the identical numeric values. Deterministic, not flaky.
  - TCC/Accessibility-permission theory is **ruled out**: this Mac took a macOS 27.0 (26A428) major upgrade on 2026-09-15 06:57:29, hours before the first failing run, and a major upgrade is a known trigger for Accessibility/Automation permission resets — but adding Xcode to the renamed pane ("Controllo del dispositivo e accesso ai dati") and enabling it, then rerunning the same 13-test subset, still reproduced 12/12 failures at identical numeric values (`uitests-run3-after-permission.log`, 232.4s). Not yet tried: a full machine restart (one external source notes `AXIsProcessTrusted` can return a stale cached trust state after a major macOS upgrade until reboot) — Stefano will restart when convenient and we'll rerun this same subset once more before closing this line of investigation for good.
  - Code-side causes are **ruled out** by direct search (2026-09-16): no custom `CGEvent`/`CGEventPost`/`IOHIDEvent` code anywhere in `Sources/` or `UITests/` — every failing test uses the standard `XCUIElement.press(forDuration:thenDragTo:)` API, including `SidebarMoveUITests.swift:22-24`'s own comment noting this exact API previously passed 5/5 against a real drag; no `240`-valued clamp exists in production resize code either — `BoardGeometry.swift`'s only floor is `CGSize(width: 40, height: 30)`. The `240.0` values in every failure are the card's untouched starting size: the synthesized drag delivers no `translation` to SwiftUI's `DragGesture` at all.
  - Current leading explanation (external evidence, not an Apple-official source): independent projects (e.g. `mac-mouse-fix`, GitHub issues #2026/#2036) report that macOS 27 (same build 26A428) stopped honoring the undocumented `CGEvent` fields historically used to synthesize drag gestures, now requiring a real `IOHIDEvent` — plausible root cause for "drag delivers no effect" across unrelated apps. No official Apple release note for macOS 27 or Xcode 27 (installed: Xcode 27.0, build 27A266a, no update pending) confirms this for XCTest specifically.
  - Does **not** block merges: zero file overlap with any Pratiche/backend PR, reproducible before and after PG-159/PG-160 was ever committed. Not `regression-of:PG-108` — that ticket (`docs/adr/0044-github-actions-ci.md:98-99`) is a separate, already-diagnosed, pre-existing `460.0`/`460.0` assertion issue, unrelated to these `240.0`-valued failures.
  - New occurrence, 2026-09-17, branch `289-bug-vaultsessionwritefile-pre-hop-jo` (PG-161/#289 fix): a plain `xcodebuild test` (not `scripts/uitests.sh` — no stale-instance kill, no `-disableCalendar`/`-disableUpdater`/`-mailStoreRoot` isolation flags) reproduced 12 of the same 13 failures verbatim, same "drag/press produces no effect" signature, zero overlap with the changed files (`Sources/Vault/VaultDisk.swift`, `Sources/Vault/VaultSession+Journal.swift`). Weaker evidence than the source run above given the missing isolation, so this does not by itself confirm the macOS 27/XCTest theory further. One test not previously listed: `TaskCategoriesUITests.testTheCategoriesSectionShowsARegisteredAndAnImplicitRowAndOpensTheCategoryView`.
  - **Real-app hand-check, 2026-09-16 (Pergamenum 1.6 release session):** the same drag gestures verified by a human directly in a freshly built Debug app, not XCUITest — Workspace card drag, corner-grip resize, group-frame move, sidebar row drag between folders, DayView month-column divider drag. All work correctly. This confirms the failing 12 are a test-harness/OS symptom (macOS 27's `CGEvent` synthesis), not a real product defect.
  - **Pushed further back, 2026-09-16:** the same 4 representative tests (one per affected UITest class) reproduced identically, byte-for-byte on the assertion values, at build 903 (`f9dc424`, 2026-09-12, PG-149's own checkpoint) and at build 946 (`cecdfc7f`, 2026-09-13, the last *published* release) — isolated in a separate `git worktree`, clean environment, no personal app instance running. Not introduced anywhere in the ADR-0041/0043/0046 write-ordering chain; predates it.
  - New occurrence, 2026-09-19, `main` at `7ab1aa6` (after PR #311 and #314 merged), full `scripts/uitests.sh` run (isolation flags on, no stale instance): 120 tests, 12 failures, all in the same set listed above, none in Pratiche or the sidebar's delete path. Same signature (gesture with no effect, 11-30 s, no 60 s launch timeout) and the same values for the four `WorkspaceBoardUITests` (260/320, 240/260, 240/340, 0/100); `DayViewUITests.testTheMonthFollowsTheDivider…` now reads 386.5/386.5 where the first run read 240/240. One fewer than the 13: `DiaryUITests.testABlockIsDeletedFromItsSheet` passed this time. Reproduces on `main`, so it is not something a branch introduced.
  - New occurrence, 2026-09-19, `main` at `745f849` (after PR #320 merged; `tuist generate` first, since the generated project still listed the deleted `MockupScreens.swift`), full `scripts/uitests.sh` run: 120 tests, 12 failures, the same set and the same values as the `7ab1aa6` run above, `testABlockIsDeletedFromItsSheet` passing again. 1538 s of test time, about 26 minutes, twice what the script announces.
  - Also the reason the rule «run `scripts/uitests.sh` before every merge» keeps being skipped (`PG-157`, closed 2026-09-19 as a paid debt, five occurrences): a suite that always ends with the same 12 red tests, in about 26 minutes, decides nothing, so no enforcement (a stamp of the tested commit, a pre-merge check against a known-failures baseline) is worth building until this is fixed or the baseline is written down.
  - Fixed by PR #327 (`e5ef7aa`, merged 2026-09-19): `press(forDuration:thenDragTo:)` was the cause, not macOS 27's drag synthesis. Measured on the corner grip: it, `press(...withVelocity: .slow, thenHoldForDuration:)` and a window-relative start coordinate all fail (240.0 of 340.0); `click(forDuration:thenDragTo:)` passes. All 14 call sites now go through `dragTo(_:pressing:)` in `UITests/DragSupport.swift`, which records the table, and CLAUDE.md carries the working agreement. The «leading explanation» above (the `CGEvent`/`IOHIDEvent` theory) is superseded and stays as history.
  - After the fix: the 14 drag tests 14/14 green in isolation, `PergamenumTests` 3267/3267, full `scripts/uitests.sh` 119/120 twice with a different red each time (`PG-180`, `PG-181`) and all 14 drag tests green in the second run. `testDraggingTheMiddleOfAGroupLeavesItWhereItIs` passed vacuously with `press` and now measures what it names. Not 120/120, so the drag set is fixed and the suite is not yet proven green.
- [x] `PG-157` **P2** [fix] `scripts/uitests.sh` not run before merging PR #273 (`fix(pratiche): retry pending attachments and bodies from disk, not the filtered candidate set`) — skipped per explicit instruction; owed per this repo's own convention (CLAUDE.md: "the UI suite runs before every merge to main") (2026-09-19) <!-- src:session opened:2026-09-15 kind:fix runs:19 gh:#273 -->
  - Second occurrence, 2026-09-16: PR #286 (`chore(release): raise marketing version to 1.6`) was also merged before the full UI suite ran — merged directly on GitHub, outside the assistant's control. Low risk this time (one-line version-string change, no logic touched, drag behavior separately hand-verified in the real app), but it is the same gap recurring: nothing mechanical enforces the convention.
  - Third occurrence, 2026-09-17: PR #291 (`feat(tasks): add category drag-to-reorder and finish chain docs`) was merged with `scripts/uitests.sh` explicitly skipped, per Stefano's own instruction at the merge gate ("Proponi il merge subito, senza uitests.sh") rather than an out-of-band GitHub merge — the first occurrence where the skip was a deliberate in-session choice, not something outside the assistant's control. PergamenumTests (3089/3089) and swiftlint were green; the new drag-to-reorder/reparent behavior was hand-verified in the real app, not through the UI suite.
  - Fourth occurrence, 2026-09-17: PR #296 (`fix(tasks): disable Crea on invalid slug and pin sidebar row height`) was also merged with `scripts/uitests.sh` skipped, per Stefano's explicit instruction ("mergia senza uitest per ora") — both fixes in that PR are themselves UI-layer bugs (`CategoryEditor.swift`'s disabled-«Crea» state, `CategorySidebarSection.swift`'s row height), the exact class of regression the UI suite exists to catch, and neither has been hand-verified in the real app either. PergamenumTests (3093/3093) green. Highest-risk occurrence yet of this recurring pattern — worth running `scripts/uitests.sh` before further Tasks-area work stacks on top.
  - Fifth occurrence, 2026-09-19: PR #311 (`fix(pratiche): forget path-keyed state when a pratica folder is trashed`) was merged with `scripts/uitests.sh` not run, by Stefano's explicit choice at the merge gate ("Merge commit adesso") after the gate had named the rule. CI `build-and-test` green and `PergamenumTests` 3257/3257, but the change is in the Pratiche/vault layer that the UI suite's pratiche and sidebar tests exercise, and no UI-suite result exists for it.
  - Run made good after the fact, 2026-09-19: `scripts/uitests.sh` on `main` at `7ab1aa6`, after #311 merged. 120 tests, 12 failures, the exact `PG-162` set (gesture failures, no new test, none in Pratiche), so #311 introduced no UI regression the suite can see. The rule was still not followed as written (merge first, run later); this occurrence stays counted.
  - Closed 2026-09-19 as a paid debt, not as a solved pattern. Two full `scripts/uitests.sh` runs on `main` (`7ab1aa6` and `745f849`) contain every skipped merge (#273, #286, #291, #296, #311, and #320 in the second, checked with `merge-base --is-ancestor`): 120 tests, 12 failures, all the `PG-162` set, none in Pratiche or Tasks, so no UI regression from the skipped runs is visible. A run after the merge shows `main` is sound today, not that each PR was sound going in. The recurring pattern (the rule is skipped and nothing enforces it) is not fixed: its cause is that the suite is never green and takes about 26 minutes, so its result decides nothing, and that lives in `PG-162`.
  - 2026-09-19, after `PG-162` closed (PR #327): its stated reason for building no enforcement, a suite that always ends with the same 12 red tests, no longer holds; the suite now ends 119/120 with a different unexplained red each run (`PG-180`, `PG-181`). A pre-merge check (a stamp of the tested commit, a known-failures baseline) becomes worth considering once those two are explained. Reopening this entry is a separate decision.
- [x] `PG-175` **P3** [fix] `scripts/uitests.sh`'s post-run cleanup had two defects: it targeted a `/Applications/Pergamenum.app` launched during the run, which the pre-run check refuses to touch, and it never closed the last instance it listed, because `$(running_instances)` drops the trailing newline that `read` needs (2026-09-19) <!-- src:session kind:fix opened:2026-09-19 runs:1 -->
  - Fixed by `4353fa9` (PR #324, merged 2026-09-19): one `partition_instances()` serves the pre-run check and the cleanup, so an installed copy is named and left alone and only debris is closed, and both strings keep the newline `read` needs. Verified with a stand-in `xcodebuild` that launches an `/Applications`-path and a DerivedData-path fake mid-run (new script: the first survives, the second is closed; old script: the reverse), not with a real UI run.
  - Correction: the entry first read that the cleanup had closed PID 5760 during the 2026-09-19 run. It had not: the process was still alive afterwards, which is the second defect showing itself.
- [x] `PG-148` **P3** [structure, chain "tests, gallery, scripts"] Test files past 500 lines with duplicated fixtures, the design gallery's 17 copies of one helper, and script prologues copy-pasted four times (2026-09-19) <!-- src:deep-refactor opened:2026-09-12 gh:#248 -->
  - Fixed by PR #320 (`745f849`, merged 2026-09-19; ADR-0051, plan `docs/plans/pg-148-tests-gallery-scripts.md`): #248 closed upstream. The scope left open on purpose is now `PG-175`..`PG-179`.
  - Tests: `structure-ConventionsTests.swift-4b8` (810 lines, eight subjects), `structure-EmbedDrawingTests.swift-048` (fixtures duplicated despite `EmbedEditorTestSupport`), `structure-TaskTests.swift-6f0`, `structure-PraticaSyncTests.swift-53d` (`makeEngine` declared twice), `structure-SharedSourcesPurityTests.swift-903` (four diverged copies of the repo-root resolver — one `Tests/RepoRootSupport.swift`).
  - Gallery: `structure-MockupScreens.swift-4d0`, `structure-TabBarMockup.swift-dcb` (one `MockupPage` + shared `scene`).
  - Scripts: `structure-install-cli.sh-11b` (`scripts/lib/common.sh`), `structure-release.sh-627` (single `gh api` call with a built arg list; `-fd5`'s full phase-function rewrite stays report-only because the script cannot be dry-run), `structure-uitests.sh-2fa` (`kill_instances` helper), `structure-appcast.py-d86`/`-0a8` (`AppcastItem` dataclass, `_case_N` helpers), `structure-mcp-smoke.py-314`/`-6f1` (`main()` guard, no module-global `failures`), `structure-support.js-0ab` (record the `dc-runtime` origin and version beside the vendored bundle, or drop it).
- [x] `PG-169` **P3** [fix] `PraticaCommandActions.confirmDeletion` trashes the pratica folder but never removes the pratica's ledger key: recreating a pratica with the same name inherits its stale `importedMessageIDs` and re-skips messages never actually imported into the new folder — `Sources/Features/Pratiche/PraticaCommandActions.swift:106` · Named as a deliberately separate defect in `docs/plans/pg-pratica-ledger-orphaned-by-folder-move.md` §"Deliberately out of scope", filed there as a TODO comment only <!-- src:marker opened:2026-09-18 closed:2026-09-19 kind:fix runs:1 -->
  - Fixed by `87f62c3` (PR #311, merged 2026-09-19): `VaultController.trashFolder(at:)` publishes the trimmed path through the new `didTrashFolder` hook, and `PraticheController.forgetLedgerState(under:in:)` removes every ledger key, tray proposal and count and watcher under it, clears a selection inside it and discards an in-flight run's outcome through a tombstone. Amends ADR-0026 §D7, no new ADR. The ticket's stated symptom did not reproduce as written: `MailStoreReader.rows` builds rows with `messageID: nil`, so stale `importedMessageIDs` alone never made `workItems` skip a message; what a recreated pratica really inherited was the old one's history (`notInStore`, bridge `entries`, `lastOpenedAt`, tray count). Follow-ups: `PG-172`, `PG-173`.
- [x] `PG-152` -> #281 **P3** [correctness] `VaultSession.transaction(_:_:)` sets `currentOperation` on a `@MainActor` object for the duration of its body; ADR-0043's own implementation (chain "vault write ordering") makes that body `async`, so the scope now spans a suspension — two transactions started from two `Task { }`s can interleave, tripping the existing `assertionFailure("transazione annidata…")` in Debug and, in Release, joining the wrong journal gesture. Pre-existing consequence of ADR-0041 §D9, widened in reach by ADR-0043, not fixed by it. Closing it properly means an actor-owned operation stack — a design decision that belongs in its own ADR, not a blind patch → `docs/adr/0043-vault-write-ordering-concurrency-races.md` §"Implementation notes", item 3 — issue no longer open upstream (closed by ADR-0050, PR merged as `8d8c997`, 2026-09-18) <!-- src:session opened:2026-09-13 closed:2026-09-18 kind:feature runs:4 adr:0043 promote:2026-09-16 -->
- [x] `PG-161` -> #289 **P3** [correctness] `VaultSession.writeFile` still reads its journal "before" on the main actor, before the actor hop, for every board write in the app — ADR-0043 §D5's Race 2 shape, moved inside the actor for `write` only, surviving on the `writeFile` door — issue no longer open upstream <!-- src:session opened:2026-09-15 closed:2026-09-17 kind:feature runs:5 adr:0046 promote:2026-09-16 -->
  - Fixed by `c69501e` (PR #302, merged 2026-09-17): `VaultDisk` gained a second `writeFile` overload, mirroring `write(_:to:precomputedHash:expecting:journalDescriptor:...)`'s own ADR-0043 §D5 fix — it reads `textBefore`/`hashBefore` and records the journal entry inside the actor, immediately before the write, instead of on the main actor before the hop. `VaultSession.writeFile` now only builds the `JournalDescriptor` (command, operation id, entry id, timestamp) before the hop. The original journal-less overload stays for `Tests/VaultWriteOrderingTests.swift`. `PergamenumTests` 3093/3093 green; CI green.
- [x] `PG-163` -> #292 **P3** [fix] `scripts/release.sh` has no preflight check for an already-published release tag — it discovers the collision only at the final `gh release create` step, after paying the full build+notarization cost (~10 min) — issue no longer open upstream <!-- src:session opened:2026-09-16 closed:2026-09-17 kind:fix runs:4 promote:2026-09-17 -->
- [x] `PG-164` **P2** [fix] `ListMarkerRendering.paragraphStyle(level:font:basedOn:)` derived its indent step from the font of the character right after the list marker instead of the page's own body font — a bold/heading/badge-sized leading run produced a visibly inconsistent indent at a given nesting level compared to sibling items — `Tests/ListIndentFontInvariantTests.swift:8` <!-- src:marker opened:2026-09-16 closed:2026-09-17 kind:fix runs:4 gh:closed#293 -->
  - Fixed by `ef7c915` (2026-09-04, "fix(editor): reconcile transclusion width reservation and stale list-indent test"), already on `main` before this issue was promoted: `EditorDecorationDelegate.listParagraph(at:storage:)` now always passes `proseFont` (the page's own body font, pushed in via `ProseTypography.prose(_:)`) to `ListMarkerRendering.paragraphStyle`, instead of resolving the font from the character right after the marker. Verified green in-session: `ListIndentFontInvariantTests.aBoldLeadingListItemIndentsTheSameAsAPlainSiblingAtTheSameLevel` passes end to end. Issue #293 closed with this note.
- [x] `PG-167` **P3** [chore] `.claude/test-cmd`'s uncommitted diff turned out to be gone on re-check: the working tree matched `HEAD` exactly, `-derivedDataPath` pin from `ccb9a45` intact, `git status --short` clean. No change needed <!-- src:session opened:2026-09-17 closed:2026-09-17 kind:chore runs:1 -->
- [x] `PG-165` **P2** [fix] Due stash residui non droppati, `task4-lint-check-rootview` e `review-worktree-scratch-swiftlint-check`, lasciati durante il chain task-categories — il branch `chore-question-activity` è già stato mergiato in `main` (PR #291, 2026-09-17), quindi non bloccano più nulla ma vanno ancora droppati a mano <!-- src:session opened:2026-09-17 closed:2026-09-17 kind:fix runs:6 gh:#291 -->
  - I due stash nominati non esistevano più nello stack condiviso al ricontrollo (probabilmente droppati da un'altra sessione concorrente sullo stesso repo). Trovato invece uno stash senza nome, `stash@{0} WIP on chore-question-activity` (contenuto obsoleto: vecchia versione di `TODO.md` e la stessa modifica a `.claude/test-cmd` già chiusa come non necessaria in `PG-167`), droppato con conferma esplicita di Stefano.
- [x] `PG-159` **P2** [structure] `Tests/PraticaSyncTests.swift` carries its own error-level SwiftLint violation, left deliberately standing by PG-143's refactor (ADR-0045 §D8's own recommendation: fix it as its own ticket, not folded into a pure-structure PR) — `Tests/PraticaSyncTests.swift` <!-- src:session opened:2026-09-15 closed:2026-09-16 kind:fix runs:4 adr:0045 gh:closed#282 -->
  - Fixed by PR #278 ("fix(pratiche): split oversized files below SwiftLint thresholds"), merged 2026-09-16, which split `Tests/PraticaSyncTests.swift` into twelve fixture-scoped test files. Issue #282 (promoted from this entry by the previous `/project-tasks` run) was closed as a duplicate once #278 was found already addressing it — #278 predates the promotion and its "Closes #159/#160" wording refers to these local IDs, not GitHub issue numbers.
- [x] `PG-160` **P3** [structure] Residual out-of-scope `file_length`/`type_body_length` SwiftLint warnings surfaced by PG-143's refactor, none crossing the error threshold — `Sources/Core/Pratiche/MessageDocument.swift`, `Sources/Features/Pratiche/PraticaFileOperations.swift`, `Sources/Features/Pratiche/PraticaLiveSync.swift`, `Sources/Features/Pratiche/PraticaSyncEngine+Messages.swift` (the last kept deliberately as one file per ADR-0045 §D4) <!-- src:session opened:2026-09-15 closed:2026-09-16 kind:fix runs:4 adr:0045 gh:closed#283 -->
  - Fixed by the same PR #278, which split `MessageDocument`, `PraticaFileOperations` and `PraticaLiveSync` into `+Aspect.swift` extensions. `PraticaSyncEngine+Messages.swift` deliberately left over the warning line per ADR-0045 §D4, unchanged. Issue #283 closed as a duplicate for the same reason as PG-159 above.
- [x] `PG-143` **P1** [structure, pratiche, after the current pratiche branch] `PraticheController.swift` is 1575 lines, past the project's own SwiftLint *error* threshold, and the pratiche layer holds five more over-limit types <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-15 kind:fix runs:1 gh:closed#243 -->
  - Findings: `structure-PraticheController.swift-7e1` (P1: split out `PraticaRowModels.swift` and `PraticaLiveSync.swift` with `SyncRunQueue`), `structure-PraticheController.swift-95b` (`runExclusive` 151 lines, complexity 18), `structure-PraticaSyncEngine.swift-186` (actor body 549 lines — split along its MARKs) and `-6c9` (`prepare` 184 lines), `structure-PraticaCommandActions.swift-7cd` (483-line struct mixing dispatch with filesystem plumbing — `PraticaFileOperations.swift`) and `-697` (`praticaNotePath` belongs in `PraticaNaming`), `structure-PraticheController.swift-bea` (Mail-store open block duplicated in `MailSeedPicker`), `structure-NuovaPraticaWizard.swift-ec2`, `structure-PratichePane.swift-bb8`, `structure-MessageDocument.swift-3ab`/`-27f` (two copies of the quote-aware splitter), `structure-PraticaNaming.swift-942` (`truncated` is a verbatim copy of `ImportNaming.truncatedAtWordBoundary` — both feed protected file names, so the shared helper needs an ADR note), `structure-MailStoreReader.swift-27d`, `structure-RecordingsController.swift-bef`.
  - Pure moves first (types into files), signatures untouched, `PraticaSyncTests`/`PraticheControllerTests`/`PraticheUITests` green before and after each step.
  - Merged 2026-09-15 via PR #272 (commit `644ec1e`, merge `18de8c3`): "refactor(pratiche): split oversized files across the pratiche/recordings feature" — split `PraticheController.swift` and `PraticaSyncEngine.swift` into the extension-file structure this issue described (`PraticaLiveSync.swift`, `PraticheController+Ledger.swift`, `PraticaSyncEngine+Messages.swift`, `PraticaSyncEngine+Payloads.swift`, `PraticaSyncEngine+Attachments.swift`, `+Decoding.swift`, `+Folder.swift`, `+Paths.swift`, `PraticheController+TimelineRead.swift`, `+Triggers.swift`, and others). GitHub issue #243 confirmed CLOSED.
- [x] `PG-154` **P3** [correctness, chain "batch-scope write guard"] `VaultSession+TagRename.renameTag` and `VaultSession+Files.renameNote`/`moveNote`'s batch writers overwrote a file whose bytes had moved on mid-batch, silently, with nothing said — the ADR-0043 §D8 `expecting:` precondition existed but none of the four highest-fan-out writers in the app had adopted it → `docs/adr/0046-batch-rename-stale-write-refusals.md` (now accepted), `docs/plans/pg-154-tag-rename-note-rename-batch-scope-write-guard.md`, `gh:#263` <!-- src:session opened:2026-09-14 closed:2026-09-15 kind:feature runs:1 adr:0046 gh:#263 -->
  - Implemented on branch `263-tag-rename--note-rename-batch-applie`, all 7 tasks: `VaultPlanApplication.Outcome`/`TagRenameOutcome`/`NoteFileOperations.Outcome` gained a `refusals` channel distinct from `failures`; `VaultDisk.writeFile`/`VaultSession.writeFile` gained the same opt-in `expecting:` precondition `write` already had; `renameTag`, `renameNote` and `moveNote` all adopt it through two new named writer seams (`VaultSession.writeGuarded`/`writeFileGuarded`); every consumer (`VaultController+Files`, `VaultSession+Move.moveItems`, `VaultAPI.renameNote`/`moveNote`, `TagBrowserView`) reports the new channel, folded into `FileMoveSummary.failures` on the wire (no new JSON key) and shown as its own count in the tag-rename banner, whose «Annulla la rinomina» is now hidden when nothing was written. `WriteRefusal` moved to `Sources/Core/Vault/VaultWriteRefusal.swift` as a top-level type, kept reachable as the unqualified `VaultSession.WriteRefusal` typealias both connector targets need. Acceptance is deterministic per ADR-0046 §D11 throughout — every forced refusal is a hand-built `VaultFileChange`/`NoteFileOperations.Outcome` driven at the writer seam, never a raced concurrent `Task`. R-01 through R-09 all covered. Verification: full `PergamenumTests` green at every task boundary (3030/3030 on the last run), `perg`/`pergamenum-mcp` connector builds green (Tasks 1, 2, 4), `swiftlint lint --quiet` clean on every touched file (all warnings found were confirmed pre-existing against `git show HEAD:<file>`). Merged 2026-09-16 via PR #279. GitHub issue #263 confirmed CLOSED. Filed three follow-ups this chain surfaced and deliberately did not fix: `PG-152` (already existed, since promoted to gh:#281), `PG-153` (gh:#262), `PG-161`.
- [x] `PG-150` **P1** [correctness, chain "vault write ordering"] ADR-0043 (follow-up to ADR-0041) — the per-path write-ordering guard (§D11) covered one of the vault's six index writers, two overlapping async writes could capture the same journal "before", and a `canOperate` check did not survive the `await` it guards → `docs/adr/0043-vault-write-ordering-concurrency-races.md`, `gh:closed#259` <!-- src:session opened:2026-09-13 closed:2026-09-14 kind:feature runs:0 adr:0043 gh:closed#259 -->
  - Stale checkbox found and corrected 2026-09-15 during a branch-cleanup pass: implementation was already complete and merged via PR #265 (commit `11fe567`), with the ADR itself marked accepted in `7b04f4a`. GitHub issue #259 confirmed CLOSED. Code verification: `writeSynchronously` no longer exists anywhere under `Sources/`, `IndexMutation` is the sole currency through `VaultDisk.swift`/`VaultSession+Journal.swift`/`VaultSession.swift`. The entry's own implementation note (all 10 tasks, 4 batches, full suite green, `scripts/adr-0043-interleaving-check.sh` passing) already documented this; only the `[ ]`→`[x]` flip and Done-section move were missing.
  - Three follow-ups this chain surfaced and deliberately did not fix, filed separately: `PG-152` (already existed), `PG-153`, `PG-154`.
- [x] `PG-123` **P1** [security, chain] Email attachments landed in `allegati/` without `com.apple.quarantine` and «Apri»/double-click handed any of them to `NSWorkspace.open` with no type restriction — `Sources/Features/Pratiche/PraticaSyncEngine.swift`, `Sources/Features/Pratiche/AttachmentChip.swift` <!-- src:deep-refactor opened:2026-09-12 closed:2026-09-14 kind:fix runs:2 gh:closed#223 -->
  - Findings: `security-AttachmentChip.swift-4ce` (P2, risk high), `security-PraticaSyncEngine.swift-a15`.
  - A hostile `.command`/`.app`/`.dmg` attachment therefore launched from the chip with none of the Gatekeeper first-run prompt the same file gets from Mail or a browser. Verified 2026-09-12: no occurrence of `quarantine` under `Sources/`.
  - Fixed 2026-09-14 on PR #268 (commit `7e0efcf`, merged to `main`): both halves as prescribed. `Sources/Core/Email/AttachmentQuarantine.swift` (Foundation + CoreServices, compiles into both connectors) stamps `com.apple.quarantine` as `LSQuarantineTypeEmailAttachment` with the app's bundle id as agent, applied in `PraticaSyncEngine.commit` *after* the atomic rename (the xattr must land on the final file, not the temporary sibling). `AttachmentChipModel.refusesToOpen(_:)` rejects `.executable`, `.applicationBundle`, `.diskImage`, `.shellScript`, `.script` and `com.apple.installer-package-archive`, judged on both the extension and the filesystem's reported type (catches an extensionless file with its execute bit set); `openURL` is `nil` for those, `revealURL`/`copyItems` untouched, and the chip's double-click falls back to «Mostra nel Finder» with a tooltip saying why. Tests: `AttachmentQuarantineTests` (4), `AttachmentChipModelTests` (+4, 12 parametrised cases), quarantine assertion added to `PraticaSyncTests.attachmentIsCopiedAsDateUnderscoreName…`.
  - Not covered by PR #268, filed separately as `PG-155`: the `.eml` sidecar write is not quarantined, only the attachment write is. Not done, deliberately: no retroactive sweep of an `allegati/` filled before this fix — the UTI check covers those files; a sweep would be a write-on-read in every sync and wants its own decision.
  - A duplicate fix for this same finding was independently produced and committed in a separate, parallel session (branch `fix/pratica-attachment-quarantine-and-open-gate`, PR #269) without visibility into PR #268 having already merged first — closed unmerged once the overlap was discovered, no functional loss since PR #268's approach was equivalent or better on every point except the `.eml` gap it also missed.
- [x] `PG-108` **P3** Two pre-existing UI test failures found during issue #188's final full-suite run, unrelated to any file that chain touched — `UITests/DayViewUITests.swift` (`testTheMonthFollowsTheDividerAndHasNoGripOfItsOwn`, `XCTAssertGreaterThan` "460.0" not greater than "460.0") and `UITests/WorkspaceBoardUITests.swift` (`testAZoomedOutCardStillHasAReadableAccessibilityLabel`, `XCTAssertEqual` "" not equal to "CARD A") <!-- src:session opened:2026-09-09 closed:2026-09-14 kind:fix runs:3 gh:closed#205 -->
  - Diagnosed 2026-09-14: both tests measured a change inside a clamp already saturated by the app's inherited window size (`DayMonthSection.calendarWidth`'s `widest = 460` clamp; `WorkspaceController+Viewport`'s `zoomToFit` landing near 1.0), not a real defect — confirmed by verifying first that all three implicated tests (plus `testACornerGripCanStillBeGrabbedWhenZoomedOut`) already passed before any code change, an incidental side effect of commit `01a5a1b` (Debug/Release bundle-id split) changing which saved window frame Debug UI runs inherit. Fixed the tests to stop depending on window geometry rather than leaving them "accidentally green": a self-correcting Workspace zoom-out loop (replacing a fixed 6-click loop with a silently-swallowed missing control) with stable `accessibilityIdentifier`s on the board's zoom controls, an explicit DayView precondition naming the saturated-clamp case, and a real `NodeCard` accessibility label at every zoom level (previously only below the placeholder LOD threshold, PG-041) — excluded for a group node, where wrapping the whole card as one accessibility element was found during this session's own verification to reparent the group's header `Text` and regress `testDraggingTheMiddleOfAGroupLeavesItWhereItIs` (caught and fixed before merge, not left as a follow-up).
  - Deliberately abandoned within scope: making the UI-test app window itself deterministic. Two independent approaches (launch-argument `-NSWindow Frame main`, a production-code `NSWindow.setContentSize` hook) both failed identically against this machine's window-tiling — likely WindowServer-level state restoration outside the reach of either `UserDefaults` or an app-side resize call. Stefano's explicit call: don't chase it further here: closing on the merit — the tests no longer depend on the constants that broke — outweighs also pinning the geometry itself.
  - Merged to `main` via PR #264. Verified: `PergamenumTests` 2964/2964; `DayViewUITests` + `WorkspaceBoardUITests` (full files, not just the named tests) 17/17; `WorkspaceOpenStateUITests` (the only other UI file touching the same accessibility identifier) 1 failure on first pass reproduced green in isolation, concluded to be resource-contention flakiness from concurrent sessions on the same machine, not a regression; `perg`/`pergamenum-mcp` connector builds green; CI `build-and-test` pass. The full `scripts/uitests.sh` suite (all 21 files) was **not** run end to end before merge — two attempts were killed by macOS for low memory from other concurrent sessions/worktrees — Stefano is running it manually post-merge; see the handoff comment on PR #264.
- [x] `PG-072` **P3** `ComposerUITests.testTheScadenzaPanelIsACalendarAndWritesTheDueMarker` failed intermittently, root-caused and fixed — `UITests/ComposerUITests.swift:205-215` <!-- src:session opened:2026-08-28 closed:2026-09-13 runs:5 gh:#217 -->
  - Root cause found: picking a calendar day in the Scadenza panel calls `onDone()`, dismissing the popover, and the test's immediate `task-composer-create` click could land on the still-animating-out popover instead of the button underneath, silently dropping the click — explaining the previously-unexplained `"---"`-only captured note content. Fixed by waiting for `due-panel-field` to actually disappear (`waitForNonExistence`) before the create click, same pattern already used elsewhere in the suite. Verified: 5 consecutive isolated runs plus the full `ComposerUITests` suite (10/10), all green. Test-only change. PR #258, merge commit `790bbca`, issue #217 closed.
- [x] `PG-110` **P3** `ReleasePipelineTests.appcastSelfTestExitsZeroWithOutput` failed reliably when run as part of the full `PergamenumTests` suite via `xcodebuild test`, but passed every time run in isolation — `Tests/ReleasePipelineTests.swift:24` <!-- src:session opened:2026-09-10 closed:2026-09-11 runs:3 -->
  - Root-caused on 2026-09-11 using `xcrun xcresulttool get test-results test-details --path <xcresult> --test-id ...` on the actual failing/passing xcresult bundles from a full-suite run: the true failure was a 60s timeout ("did not finish within 60.0s"), not an assertion failure as previously suspected, and a passing run of the same test carried a captured Xcode Runtime Warning: "Thread running at User-initiated quality-of-service class waiting on a lower QoS thread running at Utility quality-of-service class. Investigate ways to avoid priority inversions." This confirmed a GCD QoS priority inversion in the shared `run()` test helper, which dispatched `process.waitUntilExit()` onto `DispatchQueue.global(qos: .utility)` — starved under the full suite's much higher concurrent `.userInitiated` load.
  - Fixed by replacing the `DispatchQueue.global(.utility).async` + semaphore pattern with `process.terminationHandler = { _ in finished.signal() }`, set before `process.run()` — driven by the process's own dispatch source rather than a shared global-queue worker slot, so it isn't subject to that starvation. Verified: isolated `ReleasePipelineTests` suite (6/6 passed), then the full suite via `.claude/test-cmd`'s exact command twice independently (2799/2799 passed in 14.6s and 15.1s), both including this test — the first-ever confirmed full-suite passes of a test that had previously failed reliably in that context.
- [x] `PG-107` **P2** A wikilink whose visible text was made bold (`[[**Prova**]]`) never navigated on Cmd+click, and selecting it whole lit both the Bold and Italic buttons in the floating format bar — `Sources/Core/Conventions/Wikilink.swift`, `Sources/Core/Editor/InlineFormat.swift`, `Sources/Features/Editor/MarkdownStyler.swift` <!-- src:session opened:2026-09-09 closed:2026-09-09 runs:1 -->
  - Found by Stefano hand-testing issue #188's click-navigation fix: "se faccio diventare bold il wikilink, il cmd+click che porta alla nota non funziona più... mi segnala che è anche italic quando invece non lo è". Two independent root causes, one report. (1) `MarkdownStyler`'s `.linkTarget` span carried `link.target` verbatim as its navigation payload, so a bold-wrapped target (`**Prova**`) looked for a note literally titled `**Prova**` and found none — fixed with a new `Wikilink.resolvedTitle` computed property that strips one wrapping pair of emphasis markers for navigation/resolution consumers only; `target`/`rendered` deliberately stay the literal bracket interior, since `NoteRename.rewritingLinks` and `TaskParser`'s link-removal matching both rebuild source ranges from `rendered` and would silently drop the markers on the next rename otherwise. (2) `InlineFormat.isLongerMarker` widened in the wrong direction for the "markers inside the selection" shape (`**amplifica**` selected whole, as happens when a click selects a wikilink's target and its bold markers together), so its own "is this really an italic run inside a longer bold marker" check never matched and the bold-only selection reported as italic too — fixed by splitting the widening direction with an explicit `inward` parameter, matching the two shapes `wrapping(_:in:over:)` already distinguishes. `xcodebuild build` succeeded, unit suite 2545/2545 green (4 new regression tests: `Tests/ConventionsTests.swift`, `Tests/InlineFormatTests.swift`, `Tests/MarkdownStylerTests.swift`), and `scripts/uitests.sh -only-testing:PergamenumUITests/WikilinkNavigationUITests` (5 tests, including a new bold-wikilink Cmd+click case) passed green.
- [x] `PG-106` **P3** CommonMark `[text](url)` links carried no `.linkTarget` span for their label, so the click-navigation feature (issue #188) covered wikilinks but not this second link form — `Sources/Features/Editor/MarkdownStyler.swift` (`markdownLinkSpans`) <!-- src:session opened:2026-09-09 closed:2026-09-09 runs:1 -->
  - Completed as part of the issue #188 chain's follow-up commit: the label between a link's `[` `](url)` delimiters now carries `.linkTarget(href)`, resolved through the same `MarkdownAttributedText`/`CardTextAttributes` payload wikilinks already use, so `Cmd+click` navigates a CommonMark link exactly as it does a wikilink.
- [x] `PG-105` **P3** `OutOfScopeDiffFence.noOutOfScopeFileOrDirectoryHasChangedSinceTheChainBegan()` failed permanently — it diffed HEAD against the ADR-0037 chain's frozen baseline SHA (`533172d959e518417ab20c40f471f471aed8524d`) over a fixed file list, but two of those fenced paths had since changed via unrelated commits merged from `main` (`Sources/Features/Editor/EditorDecorationDelegate+CheckboxRendering.swift` via `e902e77`, `Sources/Connector/VaultViews.swift` via `d7e0ff7`), both confirmed via `git merge-base` to postdate the baseline and be unrelated to ADR-0037 or issue #188 — `Tests/InlineSpanRevealFenceTests.swift` <!-- src:session opened:2026-09-09 closed:2026-09-09 runs:1 -->
  - Fixed by removing the suite entirely rather than advancing the baseline: it compared `git diff --stat` against a fixed point in the past, once, during ADR-0037's Task 7 — a point-in-time scope check has no business becoming a permanent build gate keyed to a commit that predates every future contributor's work. Matches `fix/remove-obsolete-out-of-scope-diff-fence`'s prior resolution of the same issue (a pre-existing local sibling branch that had already reached the same conclusion). Unit suite green afterward, 2545/2545.
- [x] `PG-098` **P2** No test exercises `PlaudHTTPClient` directly — `Sources/Features/Recordings/PlaudHTTPClient.swift:56-60` <!-- src:review opened:2026-09-06 closed:2026-09-06 runs:1 -->
  - Found verifying the Plaud API client (ADR-0032) field-by-field against the plaud-service v0.2 contract: `RecordingsControllerTests.swift` always went through `FakePlaudService`, `PlaudIsolationTests.swift` only checked the base URL literal parses. Fixed with a new `Tests/PlaudHTTPClientTests.swift`, a `URLProtocol` stub that intercepts the client's real `URLSession` and reads the request back (including `httpBodyStream`, since `URLSession` moves `httpBody` there before `URLProtocol` sees the request) - no network, no `127.0.0.1:3777` service required. Five new tests cover query-string construction, the conditional `force=1`, GET/POST headers, and `ImportedBody`'s snake_case JSON encoding. Commit `c2702ac` (not pushed). Full `PergamenumTests` suite green.
- [x] `PG-097` **P3** `PlaudError` does not distinguish the 400 sub-codes of `POST /proposals/{id}/imported` — `Sources/Features/Recordings/PlaudError.swift:59-69` <!-- src:review opened:2026-09-06 closed:2026-09-06 runs:1 -->
  - Found in the same verification pass as PG-098. Fixed by adding `.invalidImportBody`/`.invalidTaskIDs` cases to `PlaudError`, mapped from the route's `invalid_json`/`invalid_task_ids` 400 sub-codes the same way the route's other documented errors (404 `proposal_not_found`, 413 `payload_too_large`) already are, each with a dedicated Italian message. `Tests/PlaudPayloadTests.swift`'s exhaustive case list and mapping tests extended to cover both. Commit `d5bf2e9` (not pushed). Full `PergamenumTests` suite green.
- [x] `PG-099` **P3** `DesignAndReadingUITests.testAThemeFileInTheVaultReachesTheThemePicker` failed: the toolbar "Tema" popup menu it expects was never implemented — `Sources/App/HistoryToolbar.swift` (`themeToggleToolbarItem`) <!-- src:session opened:2026-09-07 closed:2026-09-07 runs:1 -->
  - Found investigating a UI-suite regression report from a TasksView chain; confirmed unrelated to that work. Reproduced deterministically, not intermittently: `app.windows["Pergamenum"].popUpButtons["Tema"]` never appeared within 5s because it didn't exist — the toolbar's "Tema" control was a plain three-state cycling `Button` (Sistema/Chiaro/Scuro), exposed to accessibility as `AXButton`, not `AXPopUpButton`, and its tap handler only cycled `engine.selection` through those three cases; it had no branch that ever surfaced or selected a `.named(...)` vault-discovered theme. `ThemeEngine`'s backend was fully functional and not at fault: `attach(vaultRoot:)`/`loadUserThemes(in:)` (`Sources/DesignSystem/ThemeEngine.swift:86-95`, `:270-301`) correctly scan `.pergamenum/themes/` and build a `Theme` per file — the test asserted a feature that was never built, not a regression.
  - Fixed by replacing the cycling `Button` with a real `Picker("Tema", selection:)` in `.pickerStyle(.menu)`, listing Sistema/Chiaro/Scuro plus every vault-discovered theme by name (mirroring `ThemeCommands`' existing Vista > Tema menu-bar submenu and Settings' own theme list — `MenuCommands.swift:293-311`). One extra fix needed after the first attempt still failed the test: a captured accessibility-hierarchy snapshot (`xcrun xcresulttool export attachments`, per this repo's own UI-test debugging convention) showed the resulting `AXPopUpButton` had no accessibility label at all — the toolbar strips a `Picker`'s own title string from the popup button's AX label, leaving only its current value ("Sistema") — so `.accessibilityLabel("Tema")` was added explicitly. Single-file change (`HistoryToolbar.swift`), every one of the ten call sites (`TasksView`, `WorkspaceView`, `VaultBrowser`, `DayToolbar`, `DiaryToolbar`, `TagBrowserView`, `ViewsPane`, `StarredPane`, `RecordingsPane`, `ConformanceView`) unchanged. `xcodebuild build` succeeded, unit suite green (2175+ tests), and `DesignAndReadingUITests.testAThemeFileInTheVaultReachesTheThemePicker` confirmed passing in isolation (10.7s). Full `scripts/uitests.sh` not run this session (would have closed a Pergamenum window Stefano had open) — owed before the next merge to `main` per this repo's own convention.
- [x] `PG-076` (partial) **P3** `ComposerUITests.testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow` failed — `UITests/ComposerUITests.swift:72` <!-- src:session opened:2026-08-29 closed:2026-09-07 runs:4 -->
  - Split off `PG-076`, which still tracks its other two tests (`DayViewUITests`, `TaskTimeUITests`) as open. This one's root cause was never the "intermittent timing" `PG-076` originally named for all three: confirmed reproducing identically and deterministically, not intermittently, both on the current branch and pre-fix commit `92f7262`, so unrelated to any TasksView work in flight. Not a race: the creation/open flow (`NewNoteComposer.swift` `create()` → `VaultController+Notes.swift` `createNote` → `VaultController+Tabs.swift` `openNoteInNewTab`/`readForEditing`) is fully synchronous on the main actor, no debounce, no index-refresh dependency. The test asserted `app.staticTexts["Nota composta.md"]` (filename with `.md` extension), but no view in the app ever renders a note's title with its extension — `NoteName.title(fromFileName:)` (`Sources/Core/Conventions/NoteName.swift:82-85`) always strips `.md` before the title reaches `NoteTabBar`, `NoteListPane` or the breadcrumb, so the exact string the test looked for never existed anywhere, regardless of how fast the note opened.
  - Fixed by changing the assertion to the stripped title (`app.staticTexts["Nota composta"]`), the string the app actually renders — a one-line test change, no app code touched. Unit suite unaffected (test-only change); `ComposerUITests.testANewNoteIsNamedInTheEditorAndNotInAFloatingWindow` confirmed passing in isolation (10.4s).
- [x] `PG-094` **P3** A section move/nest computed from the Outline pane could be applied twice against the same drop, corrupting the note — `Sources/Features/Editor/NoteTextView.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by Stefano testing `PG-093`'s new nesting gesture: a single drag left the note with a heading's body duplicated and a neighbouring heading's text partially overwritten. Root-caused with temporary stderr logging (`PG093-DEBUG`, since reverted) around every step of the move pipeline: `replacementsApplied()` fired twice for one `Navigation.moveOutlineSection` call, because `NoteTextView.swift`'s `updateNSView` applied `replacements` unconditionally on every SwiftUI update pass it was still non-nil - unlike `focusRequest`/`scrollRequest`/`matchJump`, which already guard against exactly this with a `lastFocusRequest`/`lastScrollRequest`/`lastMatchLocation` comparison. Between `onReplacementsApplied()` setting `pendingReplacements = nil` and that reaching this view on the next render, at least one more `updateNSView` pass saw the same non-nil `replacements` and reapplied it - correct ranges the first time, meaningless ones (computed against text the first application had already changed) the second. Fixed by giving `NoteTextView.Coordinator` the same idiom: `alreadyApplied(_:)` compares the incoming batch's ranges and texts against the last one actually applied, and `updateNSView` only calls `apply(_:to:)` when it differs. Pre-existing latent bug in the PG-019 move/replace infrastructure (shared by find/replace-all and the sibling-drag move), only surfaced once nesting made a second drag on the same note common enough to hit the race. `xcodebuild build` succeeded, unit suite 2013/2013 green, Stefano confirmed by hand that a repeated nest/move on a throwaway vault no longer corrupts the file.
- [x] `PG-093` **P2** The Outline pane's drag-to-move only reordered siblings - dropping a section before or after another, never *into* one as a subsection — `Sources/Core/Markdown/OutlineMove.swift`, `Sources/Features/Editor/OutlinePane.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by Stefano testing `PG-090`: he could move "Sezione B" above or below another heading but never nest it as that heading's child. Asked to analyze the existing drag design and propose a better one with `AskUserQuestion`; two decisions confirmed: (1) the nesting gesture is a drop directly on a heading's own row, distinct from dropping in the gap between rows (sibling reorder, unchanged); (2) a nested section always lands as the target's LAST child, right before whatever already ends the target's section. Implemented as a new `OutlineMove.replacements(in:moving:nestingUnder:)`, sharing the existing `toPrecede` entry point's edit-assembly logic (`assembled(in:extent:sourceLevel:insertionPoint:targetLevel:before:)`, factored out of both) rather than duplicating it: refuses nesting a section under itself or one of its own descendants, refuses a non-heading target, clamps the new level at 6 instead of overflowing past ATX's `######`. New `HeadingNestDropTarget` row modifier in `OutlinePane.swift` gives a heading row a rounded-outline "drop into this" affordance, matching the convention every other container drop in this app already uses (`TaskDropTarget`, `NoteTreeRow`, `WorkspaceRow+Move`) rather than `OutlineGap`'s between-rows line for a sibling reorder. `xcodebuild build` succeeded; unit suite 2013/2013 green, including six new `OutlineMoveTests` cases (nest under an empty vs. non-empty target, refuse self/descendant/non-heading, level clamp at 6 - two expectation typos in the tests themselves, both missing/extra trailing newlines, caught and fixed by hand-tracing before the first real run). Manual hand-check on a throwaway vault surfaced a real, unrelated corruption bug in the underlying apply path, filed and fixed separately as `PG-094`; once fixed, Stefano confirmed the nesting gesture itself works correctly.
- [x] `PG-090` **P3** Folding a heading in the Outline pane (INDICE) doesn't hide its nested child entries from the index list itself — `Sources/Features/Editor/OutlinePane.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by Stefano during PG-019's manual R-10 hand-check. Pre-existing M8 behavior, not touched by PG-019: `OutlinePane.body`'s `ForEach` rendered every entry in `entries` unconditionally, and `vault.foldedEntries` only ever drove `NoteFolding.hiddenParagraphs` for the editor's own text, never the sidebar index. Fixed with a new `hiddenByFold` computed property applying the same "next heading of the same or a higher level ends the section" rule `NoteFolding.sectionEnd` uses for text offsets, applied instead to entry indices (an embed never ends a section, having no level of its own, so it stays hidden along with whatever section contains it); the `ForEach` now skips an index in that set. `xcodebuild build` succeeded, unit suite green, Stefano confirmed folding a heading now hides its nested children from INDICE too.
- [x] `PG-091` **P2** Pasting a screenshot (Cmd+V) into a note silently falls back to plain text instead of embedding the image, when the source app also puts a `.fileURL` on the pasteboard alongside the image bytes (CleanShot X does) — `Sources/Features/Editor/CompletingTextView+Pasteboard.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by Stefano during PG-006's manual verification. `pastedImagePNG()`'s `guard pasteboard.data(forType: .fileURL) == nil else { return nil }` unconditionally bailed whenever a file URL type was present, on the wrong assumption that a file-URL pasteboard always meant "a Finder-copied file, leave it to drag&drop" — CleanShot X's paste puts both a `.fileURL` and real PNG/TIFF bytes on the same pasteboard. Fixed by removing the `.fileURL` guard entirely: it was redundant once PNG/TIFF presence is checked directly (a bare file reference with no image bytes already falls through to `nil` on its own). `xcodebuild build` succeeded, unit suite 2007/2007, and Stefano confirmed the embed now appears on a real CleanShot paste.
- [x] `PG-092` **P3** Selecting an embedded image in the editor shows the text format bar (bold/italic/strikethrough/lists/headings), which does nothing meaningful on an attachment — `Sources/Features/Editor/CompletingTextView+FormatBar.swift` <!-- src:manual opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by Stefano during PG-006's manual verification. `refreshFormatBar` showed the bar for any non-empty selection outside a code fence, with no check for an embed selection. Fixed with a new `isEmbedMarker(_:)` guard reusing `Attachment.embed(inLine:)` — the same pure predicate `EditorDecorationDelegate` already applies to a marker line — against the trimmed selected substring: `selectEmbed(at:in:)` (ADR-0018 slice 3) selects the whole `![[foto.png]]` marker as ordinary text, so a full-marker selection is what the bar now recognises and hides for. `xcodebuild build` succeeded, unit suite 2007/2007, and Stefano confirmed the bar no longer appears on an embed selection while it still appears on ordinary text.
- [x] `PG-006` **P2** Build 89: block deletion and images in notes never confirmed by hand — `docs/20260811_Pergamenum_SpecApp.md` <!-- src:manual opened:2026-08-16 closed:2026-09-01 runs:2 -->
  - Verified by Stefano on a throwaway vault (Debug build). Block deletion (select a paragraph, Backspace): works, confirmed on disk. Image paste: confirmed broken, two real defects found and filed separately as `PG-091` and `PG-092` rather than fixed inline, since this item's own scope was verification, not a fix.
- [x] `PG-082` **P3** `NoteListPane`'s selection set discriminates note vs. folder ids by `id.hasSuffix(".md")` rather than a typed lookup — `NoteName.validate` does not forbid `.` in a name, so a folder literally named `Reunion.md` would be misclassified as a note-open request; no test covers this — `Sources/Features/Editor/NoteListPane.swift:53,457-461` <!-- src:session opened:2026-08-29 closed:2026-09-01 runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) for ADR-0026. Fixed in the Batch 1 fast-cleanup round: `opening(from:to:currentlyOpen:isComposingNote:)` now takes a `tree: [NoteTree.Node]` parameter and resolves the id's real kind via `NoteTree.node(withID:in:)`, replacing the `.hasSuffix(".md")` guess. One call site inside `NoteListPane`'s body updated, plus ~8 fixture-passing call sites in `Tests/NoteTreeTests.swift`. `xcodebuild build` succeeded, unit suite green. PR #150 (merged), but the TODO.md closure was missed at the time — closed now on re-triage.
- [x] `PG-063` **P2** `BoardFileOperations.renamePlan`'s note-rewrite loop swallows the real error — `try? store.read(path)` replaced with a flat "non leggibile" string <!-- src:review opened:2026-08-27 closed:2026-09-01 runs:2 -->
  - Found at Gate 5.06 (silent-failure-hunter) on ADR-0025. Fixed in the Batch 1 fast-cleanup round: the `try?`/flat-string fallback replaced with a `do`/`catch` that surfaces `NoteStore.StoreError`'s real `CustomStringConvertible` description. `xcodebuild build` succeeded, unit suite green. PR #150 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-062` **P2** `WorkspaceController` keeps stale board data after `select(.folder(f))`, and `board: String` duplicates what `current: WorkspaceSelection?` already carries — `Sources/Features/Workspace/WorkspaceController.swift:81-102` <!-- src:review opened:2026-08-27 closed:2026-09-01 runs:2 -->
  - Found at Gate 5.06 (type-design-analyzer, CRITICAL) on ADR-0025. Fixed as its own batch (the one item touching `WorkspaceController.swift`, run separately from the worktree-parallel batch): `select(_:)`'s non-`.board` branch now resets `document`/`contents`/`board` the same way `detach()` does, before setting `current = new`; `mutate(creatingOnDisk:_:)` gained `guard current?.hasBoard == true else { return }`. The guard broke ~29 existing test call sites that mutated a controller right after `attach()` with no board open — Stefano explicitly chose the full fix over a partial one ("Fai anche (2), riscrivi i test necessari"), so every affected site across six test files was updated to open a board first, via a new shared `openedWorkspaceController(rootURL:)` fixture helper in `Tests/CanvasTests.swift` (kept file sizes under the 400-line SwiftLint warning threshold). `xcodebuild build` succeeded, unit suite green. PR #151 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-045` **P2** `FolderFileOperations.validate()` does not reject `.`/`..` as a folder name; the rename/delete write path bypasses `NoteStore`'s vault-escape guard — `Sources/Vault/FolderFileOperations.swift:46-48` <!-- src:review opened:2026-08-25 closed:2026-09-01 promote:declined runs:1 -->
  - Found at review-triage-fix cycle 1 (ADR-0022). Fixed in the Batch 1 fast-cleanup round: `validate(_:)` rejects `"."`/`".."` explicitly before delegating to `NoteName.validate`, reusing the existing `NoteName.Violation` vocabulary rather than adding a new case. Covers both `createFolder` and `renamePlan`. `xcodebuild build` succeeded, unit suite green. PR #150 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-037` **P2** `TaskGroup.parent`/`progress` are parallel optionals standing in for a discriminated union — `Sources/Core/Tasks/TaskListOptions.swift` <!-- src:review opened:2026-08-24 closed:2026-09-01 promote:declined runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0021. Fixed in the Batch 1 fast-cleanup round: the parallel `parent`/`progress` optionals replaced with one `Kind: Equatable, Sendable` enum (`.plain` / `.project(parent:progress:)`) on `TaskGroup`, making the invariant compiler-checked instead of comment-only. The one construction site (`bySubtasks(_:)`) and the two read sites in `Sources/Features/Tasks/TasksView+List.swift` updated to pattern-match on `group.kind`. `xcodebuild build` succeeded, unit suite green. PR #150 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-040` **P3** Malformed `^id(abc)`/`^parent(abc)` is silently dropped with no linter finding — `Sources/Core/Tasks/TaskParser.swift` `caretInteger(in:name:)` <!-- src:review opened:2026-08-24 closed:2026-09-01 promote:declined runs:1 -->
  - Found at Gate 5.06 (silent-failure-hunter, confidence ~50) on ADR-0021. Descoped from a full linter finding (would need a new `TaskMarkerViolation` case threaded through `VaultSession+Search.swift`, four-plus files for a P3 finding at ~50% confidence) to a doc comment on `caretInteger` naming the silent-drop as deliberate, consistent with the parser's existing permissive style — matching how `PG-039` documents its own `try?` fallback. Full linter integration stays open as a separate, larger ticket if it's ever wanted. PR #150 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-039` **P3** `CanvasStore.allBoards()`'s `try?` fallback isn't documented as deliberate — `Sources/Vault/CanvasStore.swift:118` <!-- src:review opened:2026-08-24 closed:2026-09-01 promote:declined runs:1 -->
  - Found at Gate 5.06 (silent-failure-hunter, confidence ~55) on ADR-0021. Fixed with a one-line doc comment naming the `lastPathComponent` fallback as deliberate (display name only, low impact), matching this codebase's habit of narrating other `try?` fallbacks nearby. PR #150 (merged); TODO.md closure missed at the time, closed now on re-triage.
- [x] `PG-060` **P3** `WorkspaceController.folder`/`current` split still relies on caller discipline, not the type system <!-- src:review opened:2026-08-26 closed:2026-09-01 runs:2 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0024. Closed with no code change, per the ticket's own framing ("not urgent... if a third unguarded caller ever appears"): re-verified live that the guard-discipline premise still holds — `grep -rn "\.folder\b" Sources/Features/Workspace` shows the same guarded readers cited when filed (`BoardCardMenu.swift`, `BoardChrome.swift`, the rename/delete flows in `WorkspaceView.swift`/`WorkspaceFolderActions.swift`/`WorkspaceView+FolderVerbs.swift`), each still behind `isShowingBoard`/`current != nil`. No third unguarded caller has appeared since filing. Reopen if one does.
- [x] `PG-058` **P2** `WorkspaceSelection`'s own discriminant is discarded at the `WorkspaceController` → `WorkspaceBrowser` boundary and reconstructed from a second, independently-refreshed source — `Sources/Features/Workspace/WorkspaceView.swift:64`, `Sources/Features/Workspace/WorkspaceBrowser.swift:232-247` <!-- src:review opened:2026-08-26 closed:2026-09-01 runs:2 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0024. Already fixed by other work since filing, never closed: `WorkspaceView.swift:140` now passes `selection: workspace.current` — the whole `WorkspaceSelection`, not a bare `String?` — into `WorkspaceBrowser`, per the fix ADR-0025 §D8 shipped for a different reason. Re-confirmed live this session by reading the call site directly; no code change needed.
- [x] `PG-053` **P2** `WorkspaceBoardUITests.openWorkspace()` can no longer open any board, so 6 of its tests fail with "la board non si è aperta" — `UITests/WorkspaceBoardUITests.swift:203-213` <!-- src:session opened:2026-08-26 closed:2026-09-01 runs:2 -->
  - Found running `scripts/uitests.sh` at Task 7 of the ADR-0024 chain. Already fixed by other work since filing: `openWorkspace()` now clicks the fixture board's own row by its `workspace-board-<folder>/<file>.canvas` accessibility identifier after navigating to the Workspace pane, exactly the fix the ticket itself proposed. Confirmed live with a targeted run of the affected test class rather than closing on a code read alone: `scripts/uitests.sh -only-testing:PergamenumUITests/WorkspaceBoardUITests` — first two attempts failed identically with "Timed out while enabling automation mode" (an Accessibility/Automation permission issue, not a code defect: the code itself was already read and confirmed correct), retried after killing stale `Pergamenum.app`/`PergamenumUITests-Runner` processes, third attempt green: "Executed 6 tests, with 0 failures (0 unexpected) in 115.058 seconds."
- [x] `PG-038` **P2** Case-insensitive `workspacePath` matching is independently re-derived at multiple call sites — `Sources/Features/Tasks/WorkspacePicker.swift:84`, `Sources/Index/IndexSnapshot.swift:187-188` <!-- src:review opened:2026-08-24 closed:2026-09-01 promote:declined runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0021. Already fixed by other work since filing: both cited call sites now route through the shared `WorkspaceBoardResolver.matches(_:workspacePath:)` instead of an inline `.lowercased() ==` at each — `WorkspacePicker.swift:84` and `IndexSnapshot.swift:198` (drifted from the ticket's original `187-188` citation by unrelated line churn, not a sign the fix moved). Re-confirmed live this session by reading both call sites directly; no code change needed.

- [x] `PG-057` **P3** A failed board load leaves `current` claiming the board opened, with the only error record in a `problems` array nobody reads — `Sources/Features/Workspace/WorkspaceController.swift` `load(folder:)`/`open(folder:)` <!-- src:review opened:2026-08-26 closed:2026-09-01 runs:2 --> — **already resolved on main, no code change**
  - Found at Gate 5.06 (silent-failure-hunter) on ADR-0024, when `load(folder:)` still existed and folders could themselves fail to load. Both premises are gone: ADR-0025's folder/board separation deleted `load(folder:)`/`open(folder:)` outright — a folder-less selection reads nothing from disk any more (`select(_:)`'s `.folder`/`nil` branch, `WorkspaceController.swift:307-327`, is pure state reset, no I/O), so there is no folder-load failure left to misreport. The one remaining read, `load(board:)`, already does exactly what the ticket asked for: on a throw it calls `recordProblem(_:)` and returns `false` *before* touching `current`/`board`/`document` (`WorkspaceController.swift:255-283`), so `open(board:)` (`:293-296`) never marks a failed board as selected. `recordProblem` forwards to `vault.recordProblem`, and `vault.problems` is read and shown in Settings → Problemi (`SettingsView.swift:280-285`) — not "nothing reads it" any more. Covered by an existing test, `WorkspaceOpenStateTests.swift:114` `openingAMissingBoardPathSelectsNothingAndRecordsAProblem`, which asserts both `current`/`isShowingBoard` are untouched and `vaultController.problems` is non-empty. Verified live this session by reading both the current code and the test; no code change needed.
- [x] `PG-026` **P3** `CLAUDE.md` is missing three things measured on 2026-08-18 — `CLAUDE.md` <!-- src:session opened:2026-08-18 closed:2026-09-01 runs:2 -->
  - Doc-only fix, no code or test risk: added three bullets to the "Working agreements"
    section next to the existing "A UI-test instance outlives its run" note. (1) A stale
    instance left alive poisons the *next* full UI run wholesale — 18 failures observed, every
    one at exactly 60.2 s, the launch timeout rather than a real defect; kill instances first
    and read the per-test timings before trusting a red run. (2)
    `firstRect(forCharacterRange:)` returns a zero rectangle for a text range TextKit 2 hasn't
    laid out yet (reliably true at the end of a long note), which silently clamps popup/panel
    placement to the screen's bottom-left corner instead of failing loudly. (3) The UI-test
    runner's own temp directory is unreadable from outside the sandbox either way; attach
    files with `XCTAttachment` + `-resultBundlePath`, pull them back with `xcrun xcresulttool
    export attachments`. No build/test run needed — nothing but `CLAUDE.md` changed.
- [x] `PG-088` **P3** `Tests/CanvasTests.swift` trips the error-level `file_length` threshold — 1177 lines vs the configured 1000-line error threshold, the repo's only `serious` swiftlint finding — `Tests/CanvasTests.swift` <!-- src:session opened:2026-09-01 closed:2026-09-01 runs:1 -->
  - Found by a full-repo `swiftlint lint` sweep right after PG-035 merged, not previously tracked anywhere. `.swiftlint.yml` applies `file_length` (warning 400/error 1000) with no per-directory override, so a test file is held to the same bar as a source file. Same `swiftlint-drift` pattern as `PG-035`/`PG-036`/`PG-055`/`PG-056`, applied for the first time to a Swift Testing free-function file rather than a type. Fixed with pure code motion along the file's own `// MARK:` boundaries into `Tests/CanvasStoreTests.swift` (`CanvasStore` path-addressing + `allBoards()`), `Tests/WorkspaceControllerTests.swift` (breadcrumb navigation, board open/close, node CRUD, title editing) and `Tests/WorkspaceControllerToolsTests.swift` (dragging, the Freccia tool, crop mode transitions) — main file keeps the JSON Canvas decode/round-trip tests and the shared `TemporaryRoot` fixture. One access-level change plus one rename, not just a demotion as planned: `TemporaryRoot` needed to go from `private` to internal since three new files construct it, but that collided with an unrelated, pre-existing `private struct TemporaryRoot` in `Tests/WorkspaceCreationTests.swift` — Swift does not allow two same-named types in one module once either loses file-scoped `private`. Renamed the moved fixture to `CanvasTemporaryRoot` to disambiguate; caught only by `xcodebuild test`'s "invalid redeclaration" error, not by `swiftlint`, which does not see cross-file name collisions. Final sizes: `CanvasTests.swift` 216 (was 1177), `CanvasStoreTests.swift` 274, `WorkspaceControllerTests.swift` 392, `WorkspaceControllerToolsTests.swift` 310 — all four comfortably under both thresholds. `swiftlint lint`: 0 violations on all four files, repo-wide serious count 1→0. `xcodebuild build`: succeeded. Unit suite: 1991/1991 green, unchanged count confirms no `@Test` was lost in the motion.

- [x] `PG-056` **P2** `type_body_length` on `WorkspaceController.swift` was error-level, not warning-level as first filed — 383 lines vs the configured 350-line error threshold — `Sources/Features/Workspace/WorkspaceController.swift`, `WorkspaceController+Nodes.swift` <!-- src:session opened:2026-08-26 closed:2026-09-01 runs:1 -->
  - Re-verified live before starting: `swiftlint` confirmed the error still current, grown further since filing — 495 lines vs the 350-line error threshold. Fixed by extracting the node CRUD/mutation cluster (`resize`, `setColor`, `setTextColor`, `setTextAlignment`, `setText`, `setTitle`, `toggleTask`, `delete`, `addNode`, `connect`, `placeFile`, `addStickyNote`, `addFreeText`, `addLink`, `createFolder` — none holds stored state, all already go through `mutate`) into a new `WorkspaceController+Nodes.swift`, matching this codebase's existing per-concern extension-file split (`+Drawing`, `+Duplicate`, `+Gestures`, `+Import`, `+Tools`, `+Viewport`, `+Crop`) rather than inventing a new pattern. `swiftlint` re-run confirms the error is cleared: `type_body_length` now 344 lines, under the 350 error threshold (trips only the pre-existing 250-line warning tier, same as the `file_length` warning already accepted elsewhere in this file for `WorkspaceBrowser.swift`). Pure code motion, no behaviour change: `xcodebuild build` succeeded (app target), unit suite 1991/1991 green.

- [x] `PG-042` **P3** No app-wide right-click context menu exposing every command, including shortcut-only ones — currently toolbar/menu-bar/shortcut only, inconsistently — `Sources/Features/Workspace/WorkspaceRow.swift`, `CardCommand.swift`, `Sources/Features/Today/CalendarDayCommand.swift`, `Sources/Features/Editor/NoteRowMenu.swift` <!-- src:session opened:2026-08-25 closed:2026-09-01 runs:1 -->
  - Requested by Stefano during ADR-0022 manual QA, refined to: every command reachable both from the toolbar/menu-bar and from a right-click context menu, app-wide, including shortcut-only commands. Verified against live code before starting an interview/design chain: **already fully implemented and merged** by ADR-0023 (universal-command-surface-parity, PR #105) — ADR-0023's own References section literally names its SPEC "Universal command surface parity (PG-042), R-01…R-14", and its Context states "a read-only survey of every command surface in Pergamenum found six clusters" — the same six surfaces this ticket's refined text names (Workspace folder row, note row, task row, canvas card, editor embed, calendar day cell), plus a net-new Duplica card command. The ticket's own cited blocker, "ADR-0022 §D8 deliberately chose the toolbar as the action surface", is explicitly superseded by ADR-0023 §D2-§D4 on exactly that point. Confirmed in live code, not inferred: `WorkspaceRow.swift:277-286`'s `menu` computed property has Rinomina/Elimina gated by `WorkspaceBrowserToolbar.canMutate(_:)`, the same rule the toolbar buttons use (ADR-0023 §D1/§D3); `CardCommand.swift` names/symbols/applicability once for the canvas-card context menu and the card's floating format bar, including `Duplica`; `Sources/Features/Today/CalendarDayCommand.swift` is the shared catalogue for all three calendar surfaces (Month/Week/MiniCalendar); `NoteRowMenu.swift` covers the note row; `CommandActions.swift:408`'s `TaskDraft.subtask(of:)` covers the task row; `CompletingTextView+Pasteboard.swift:67`'s `menu(for event:)` override covers the drawn-embed context menu in the editor. The one remaining shortcut-only command in the app, «Cattura rapida» (ADR-0008), is deliberately excluded by that ADR, not an oversight this ticket should reopen. No code change, no manual hand-check performed before closing (Stefano's call, given the mechanism — including the exact file/line citations above — is confirmed in the code, not inferred). A `concept-to-code` standard-path chain was opened for this ticket, then aborted at Step 1 (before any SPEC.md was written) once this was discovered during the interview's own preliminary research; `docs/manifests/2026-09-01-pg-042-add-an-app-wide-right-click-conte.manifest.yml` records the aborted chain.
- [x] `PG-043` **P3** An intermediate Workspace folder that lost its only board used to vanish from the sidebar tree, reading as if it too were deleted — `Sources/Features/Workspace/WorkspaceBrowser.swift` <!-- src:session opened:2026-08-25 closed:2026-09-01 runs:1 -->
  - Found during ADR-0022 manual QA, against the then-current `WorkspaceBrowser.rebuild()`, which built the tree exclusively from `CanvasStore.allBoards()` paths — a folder with zero boards left inside had no node and disappeared. Verified against live code, no fix needed: ADR-0025's rework of the sidebar (folders and boards as distinct tree rows, ADR-0025 §D2) already fixed this exact defect as a side effect, not deliberately re-addressed for this ticket. `WorkspaceBrowser.rebuild()` (`:583-584`) now populates `folders` from `canvasStore?.allFolders()`, a real filesystem walk (`CanvasStore.swift:166-186`) independent of which folders hold boards. `WorkspaceTree.build(folders:boards:)` (`WorkspaceTree.swift:48-61`) inserts every real folder as its own row via `insertFolder`, board-less or not — ADR-0025 §D2's own stated consequence ("an empty folder is visible for the first time") is precisely this ticket's fix. No manual hand-check performed before closing (Stefano's call, given the mechanism is confirmed in the code across three files).
- [x] `PG-044` **P3** Workspace sidebar folder selection was sticky, so «Rinomina»/«Elimina» never visually disabled after the first click — `Sources/Features/Workspace/WorkspaceBrowser.swift` <!-- src:session opened:2026-08-25 closed:2026-09-01 runs:1 -->
  - Found during ADR-0022 manual QA (R-09), against the then-current `selectedFolder`/`onOpen`/`onDeselect` model. Verified against live code, no fix needed: ADR-0024/0025's rework of the sidebar (`WorkspaceSelection?` enum, `List(selection:)`'s own multi-selection binding) already fixed this exact defect as a side effect, not deliberately re-addressed for this ticket. `WorkspaceBrowser.swift:454-457`'s own comment states it: clicking the blank area below the rows deselects, `List(selection:)`'s native behaviour under ADR-0024 §D7. `opening(from:to:currently:in:)` (`:750-752`) returns `.some(nil)` for an empty selection set, and `WorkspaceBrowserToolbar.canMutate(_:)` (`:129-135`) returns `false` for `.none` and for an empty/root folder path, so «Rinomina»/«Elimina» now correctly disable. No manual hand-check performed before closing (Stefano's call, given the mechanism is confirmed in three places in the code, not inferred).
- [x] `PG-077` **P3** CardFormatBar's format buttons carried `accessibilityIdentifier` for UI tests but no `accessibilityLabel`, so VoiceOver read only the SF Symbol's default (often none) rather than a meaningful name <!-- src:session opened:2026-08-29 closed:2026-08-31 runs:1 -->
  - Found at Gate 5.05's accessibility/i18n checklist (ADR-0066) for ADR-0027. Fixed with `.accessibilityLabel(entry.help)` on both bars' buttons, reusing the string already carried on `Entry.help` — same pattern as `BoardCardControls.swift:88-90`. Scope widened deliberately, confirmed with Stefano before implementing: (1) added `.accessibilityAddTraits(entry.isOn/isOn ? [.isSelected] : [])` on both bars, since a toggle's on/off state was conveyed only by a background colour swap and was equally unannounced — same defect class, same fix shape as `WorkspaceRow.swift:185`'s existing `.isSelected` precedent; (2) applied the identical two-modifier fix to `Sources/Features/Editor/FormatBar.swift` (the note editor's own format bar, not named in the ticket) — ADR-0027 §D6 lets the two bars drift in button catalogue, not in accessibility hygiene, and fixing only the Workspace card bar would have left the editor's bar with the identical hole for no defensible reason. No test file: both bars are SwiftUI views with no unit tests by this repo's own documented convention (`Tests/BoardFormatBarTests.swift:9-14`); no `accessibilityIdentifier` changed, so no UI test query is affected. VoiceOver hand-check on both bars still owed, batched with the session's other pending hand-checks. `xcodebuild build` green; unit suite 1991/1991 green (unchanged count, as expected).
- [x] `PG-085` **P3** List nesting depth was computed by this app's own indent classifier, not CommonMark's content-column rule — a list indented three spaces, or whose nesting depends on the parent bullet's content column, rendered at a level this classifier computed rather than the one CommonMark (and Obsidian) would assign <!-- src:session opened:2026-08-30 closed:2026-08-31 runs:1 -->
  - Declined follow-up from ADR-0028 (wysiwyg-markdown-in-workspace), Consequences/Negative. New shared pure function `ListNesting.level(in:lineStart:indent:)` (`Sources/Core/Editor/ListNesting.swift`, Foundation-only) walks the document backward from a line's start to reconstruct the stack of open ancestor list items' content columns (marker start + marker width + trailing space), then attaches at 1 + however many still fit (CommonMark's tie-break), capped at 6. Both duplicate call sites — `MarkdownStyler.listMarkerSpan` (styling pass) and `EditorDecorationDelegate+ListRendering.stillSpellsAListMarker` (layout-time re-read) — now call this one function instead of restating `1 + columns/2` inline; each still recomputes it fresh from live characters on its own schedule, preserving `HiddenMarker.Kind.list`'s deliberate no-cached-level invariant. `MarkdownStylerTests.swift`'s old fixed-column assertions rewritten to real ancestor-chain scenarios; `MarkupHidingTests.swift`'s `nested` fixture gained a real parent line (isolated indented items are level 1 with no ancestor under CommonMark) — both helper functions there gained an `at:`/`offset` parameter to target a non-first paragraph. New `Tests/ListNestingTests.swift` covers the algorithm directly (3-space nesting under narrow vs. wide markers, tie-break, level cap, a top-level paragraph closing every open list). `ListContinuation.swift` (Enter-key continuation/renumbering, a distinct mechanism) untouched, as scoped. `xcodebuild build` green for all three schemes; unit suite 1991/1991 green.
- [x] `PG-084` **P3** `MarkdownStyler`'s inline-span parser (`inlineSpans`/`emphasis`) was non-recursive: a matched `**...**` span consumed its entire matched range as one atomic `.bold` span, including any `~~...~~` nested inside it, so `**~~testo~~**` never rendered (or concealed) as bold+strikethrough — only as bold, with the `~~` markers left visible and unconcealed — `Sources/Features/Editor/MarkdownStyler.swift` (`inlineSpans`, `emphasis`) <!-- src:manual opened:2026-08-29 closed:2026-08-31 runs:1 -->
  - Found by hand-check during the ADR-0028 (wysiwyg-markdown-in-workspace) ship gate, reproduced on a Workspace `.text` card. `inlineSpans(in:absolute:)` now recurses into a matched run's own inner content (the substring between its opening/closing markers) through the same entry point, offsetting the recursive `absolute` closure back into the outer line's coordinates; a new `markerLength(for:)` helper answers the delimiter width (2 for `.bold`/`.strikethrough`, 1 for `.italic`, `nil` for the atomic spans that have no inner content to recurse into). No change needed to attribute application (`MarkdownAttributedText`/`CardTextAttributes` already merge overlapping spans' attribute keys via `addAttributes`, not replace) or to concealment (`~~` was never concealable by design and stays that way — only `.bold`/`.italic` markers conceal, unaffected). Fix lands in the single shared parser both the note editor and the Workspace card already read (ADR-0028 §D1), so both surfaces are fixed together. Five new unit tests in `Tests/MarkdownStylerTests.swift`: strikethrough nested in bold, in italic, the reverse nesting (bold in strikethrough, including its own `.emphasisMarker` pair), a non-nested-run regression check, and an absolute-offset correctness check with leading text before the match. `xcodebuild build` green for the app, `perg` CLI and `pergamenum-mcp` targets; unit suite 1981/1981 green.
- [x] `PG-075` **P3** A Workspace text card never concealed markdown markers (`**`, `*`, `#`) the way the note editor does — `Sources/Features/Editor/EditorDecorationDelegate.swift`, `Sources/Features/Workspace/CardTextAttributes.swift` <!-- src:session opened:2026-08-29 closed:2026-08-31 runs:1 -->
  - Recorded as needing its own interview/ADR when opened. That ADR shipped separately and closed this gap without the entry being marked done: ADR-0028 (`docs/adr/0028-wysiwyg-markdown-in-workspace.md`, 2026-08-30) ported the note's WYSIWYG concealment into the Workspace card by reusing `EditorDecorationDelegate` directly rather than forking it (a deliberate, narrow reopening of ADR-0027 §D1). Verified live in this session: `StickyTextCard.swift` threads `workspace.hidesMarkup` into `CardTextView`, which constructs its own `EditorDecorationDelegate()` and calls `decorations.apply(hiddenMarkers:hidingMarkup:)` (`CardTextView.swift:168,324`) — the exact mechanism this ticket asked for. Closed as already resolved, no new implementation needed.
- [x] `PG-073` **P3** SPEC §6.5's editable title on a Link card was never implemented — creation sets the URL once via the sheet, and no affordance exists afterward to rename it — `Sources/Features/Workspace/WorkspaceController.swift:579` (`addLink`), `Sources/Features/Workspace/NodeCard.swift` (`linkCard`) <!-- src:session opened:2026-08-28 closed:2026-08-31 runs:1 -->
  - Found in the same audit as `PG-074`. Implemented via the `concept-to-code` hybrid chain: title stored as a new `pergamenum-title` prefixed key on `CanvasNode.unknown` (`LinkCardTitle.swift`, same pattern as `CanvasCrop`/`CardTextStyle`), no title set falls back to displaying the URL as before, empty commit removes the key rather than writing `""`. A new `CardCommand.renameLink` ("Rinomina") triggers inline editing — double-click stays reserved for `NSWorkspace.open(URL)` per SPEC §6.4 row 7 — via `WorkspaceController.beginTitleEdit`/`endTitleEdit`/`setTitle`, the same transient-controller-owned-editing-state shape `editingTextNodeID`/`editingTextDraft` already used. `NodeCard.linkTitle(_:_:)` swaps a `Text` for a `TextField` bound to `editingTitleDraft`, committing on submit/blur (`@FocusState` + `.onChange`) and discarding on Esc (`.onExitCommand`). Unit tests cover the property read/round-trip (`LinkCardTitleTests.swift`), the `CardCommand` catalogue (`CardCommandTests.swift`, 13→14 commands), and the controller's begin/commit/discard/non-link-guard/duplicate-carries-title behavior (`CanvasTests.swift`). `xcodebuild build` green for the app, `perg` CLI and `pergamenum-mcp` targets (no `Project.swift`/`sharedSources` change needed — `Sources/Features/**` has no connector surface); unit suite 1976/1976 green. Not yet hand-checked on a live vault by Stefano (inline edit interaction, empty-clear fallback, Obsidian round-trip, double-click-still-opens) — owed together with the other outstanding hand-checks at end of session.
- [x] `PG-074` **P2** SPEC §6.4 tool 8 (To Do) draws only a static sticky containing the literal `"- [ ] "` — no checkbox, no interactive toggling, no task indexing as the spec requires — `Sources/Features/Workspace/WorkspaceController+Tools.swift:33` (`createSticky("- [ ] ")`), `Sources/Features/Workspace/StickyTextCard.swift` <!-- src:session opened:2026-08-28 closed:2026-08-31 runs:1 -->
  - Found auditing why Nota/Testo/To Do canvas cards were not editable. Folded together with `PG-086` below (its own checkbox-glyph ticket) into one plan, since both call sites colour the same `MarkdownStyler.Span.taskMarker(done:)` and one substitution rule serves both surfaces. Implemented as a five-part chain: (1) `MarkdownStyler.taskMarker(in:)` now returns which of the four SPEC §7.1 states a line carries, not just `done: Bool`; (2) a new `EditorDecorationDelegate+CheckboxRendering.swift` substitutes the state character (offset 3 of the five-character `- [x]` marker) with a drawn glyph, shared verbatim by the note editor and the Workspace card — the four other marker characters stay in the string, collapsed via font, so the paragraph's length is invariant (TextKit 2's `NSTextContentManager.h:120` constraint); (3) `FormattingTextView.claimsCheckbox(at:)` hit-tests the drawn glyph and toggles the task whether the card is at rest or being edited — `WorkspaceController.toggleTask(atLineIndex:forNodeID:)` writes through `setText(_:forNodeID:)` at rest, or mutates `editingTextDraft` and commits via the existing `endTextEdit(commit:)` while editing, so the toggle is one undo step; (4) `VaultScanner.scan()` gained a `.canvas`-scanning branch: a `.text` node is scanned for tasks only when its own first line already parses as a task line (no `CanvasNode` field distinguishes a purpose-made To Do card from a freehand Testo card), yielding a new `BoardTaskRecord` (`Sources/Vault/BoardTaskRecord.swift`) carried in `IndexCache`'s new `boardTasks` table (schema version unchanged at 3, additive) and merged into `IndexSnapshot.allTasks` — the single point every task query, the Attività views, the reminder scheduler and both connectors read, so board tasks are indexed for free; a new `TaskItem.nodeID: String?` disambiguates which node a board task's `lineIndex` is relative to; (5) `VaultSession.apply(_:to:)` — the one resolution point every task mutation funnels through — now redirects a `.canvas`-sourced task's read/write through `CanvasStore` and the owning node's own text, never the whole board file, with the existing staleness guard (`.stale` on a line that moved under it) applying identically. `xcodebuild build` green for the app, `perg` CLI and `pergamenum-mcp` targets (`BoardTaskRecord.swift` added to `Project.swift`'s `sharedSources`); unit suite 1964/1964 green, including new coverage for the four-state glyph substitution, the click-hit-test's marker offset, board-scan fixtures, `IndexSnapshot.allTasks`'s merge, and `VaultSession.apply`'s board write-back and staleness guard on a `.canvas` node. `scripts/uitests.sh` run before merge: 105 executed, 4 failures, all four pre-existing and already tracked (`PG-072`, `PG-076`), none touching the Workspace tools or task-editing surfaces this chain changed. Not yet hand-checked on a live vault by Stefano (glyph states, click-toggle at rest/while editing, `@done(...)` stamping in the `.canvas` on disk, Attività round-trip, Obsidian round-trip) — owed separately.
- [x] `PG-086` **P3** No real checkbox glyph on a task line (`- [ ] fai`) — a To Do card/note draws its own characters, coloured through `.taskMarker(done:)`, with nothing substituting a box or a tick — `Sources/Features/Editor/MarkdownAttributedText.swift:126`, `Sources/Features/Workspace/CardTextAttributes.swift:191` <!-- src:session opened:2026-08-30 closed:2026-08-31 runs:1 -->
  - Declined follow-up from ADR-0028 (wysiwyg-markdown-in-workspace) §C4 turned out not to need its own separate implementation: folded into `PG-074` above, since drawing the checkbox glyph is exactly what that ticket's To-Do-tool work needed too, and both surfaces (note editor, Workspace card) colour the same `MarkdownStyler` span. See `PG-074`'s entry for the implementation.
- [x] `PG-059` **P3** `WorkspaceBrowser.targetFolder` wraps a plain `String` into a fabricated `.folder(...)` case just to satisfy a signature that never branches on it — `Sources/Features/Workspace/WorkspaceBrowser.swift:145-147` <!-- src:review opened:2026-08-26 closed:2026-08-31 runs:2 --> — **already resolved on main, no code change**
  - Verified while driving PROJECT.md Phase 8. The ticket's claim (`selectedFolder: String?` re-wrapped into a fabricated `WorkspaceSelection.folder(...)` to feed `target(for:)`) was true when opened on 2026-08-26, but the very next day's ADR-0025 chain (`42a36e9`, "distinguish board and folder paths with newtypes") replaced `WorkspaceBrowser.selectedFolder: String?` with `selection: WorkspaceSelection?` throughout, as a byproduct of the folder/board separation the ticket had nothing to do with. `target(for:)` (`WorkspaceBrowser.swift:265`) now takes the real `WorkspaceSelection?` directly — no fabrication, nothing simulated. Confirmed no other `.folder(...)`-fabrication site exists (`grep` across `Sources/Features/Workspace/`). Same shape as PG-057/PG-058's closures: an unrelated, larger chain already fixed the underlying architecture before this ticket's own fix could land.
- [x] `PG-052` **P3** `WorkspaceView.renameWorkspace` recomputes the renamed folder's destination path by hand instead of reading it from the rename outcome — `Sources/Features/Workspace/WorkspaceView.swift:182` (drifted to `WorkspaceView+FolderVerbs.swift:112-125` at close) <!-- src:review opened:2026-08-25 closed:2026-08-31 promote:declined runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. Verified live before starting: the claim held exactly (the verbs had been extracted into `WorkspaceView+FolderVerbs.swift` since the ticket was filed, but the hand arithmetic, the discarded `RenameOutcome.newPath`, and the `Bool`-returning `VaultController.renameFolder` were all unchanged). Scope widened by user's explicit choice to `renameBoard`, the identical duplicate two functions below it, hand-building `"\(folder)/\(newName).canvas"` while discarding `BoardFileOperations.RenameOutcome.newPath` the same way — same root cause, its only two occurrences, same reasoning as PG-051's `NoteListPane` widening. Fix: `VaultController.renameFolder`/`renameBoard` now return `String?` (the outcome's `newPath`, or `nil` on refusal) instead of `Bool`. `WorkspaceView+FolderVerbs.swift`'s `performFolderVerb`/`performBoardVerb` brackets became generic over the value the mutation answers with, so `renameWorkspace`/`renameBoard` feed their landing rule straight from the vault call instead of a second, hand-computed variable beside it — both hand-arithmetic blocks deleted outright. `deleteWorkspace`/`deleteBoard` adapted to the same generic shape by answering with the path they already had (`vault.trashFolder(at: folder) ? folder : nil`); `trashFolder`/`trashBoard` themselves deliberately kept their `Bool` — a delete has no computed destination to read back. `BoardFileOperations.RenameOutcome` gained `Equatable, Sendable` for parity with its `FolderFileOperations` sibling (PG-046 covered that file's `MovePlan`/`MoveOutcome` but not this type) — pure additive conformance, every field already conforming. One existing test (`vaultControllerRefusesToRenameAFolderWithAnUnsavedNoteUnderIt`) updated from `renamed == false` to `renamed == nil`; two new tests in the same file assert both `renameFolder`/`renameBoard` now answer with the outcome's `newPath` — the assertion the hand arithmetic previously made untestable. `xcodebuild build` succeeded for the app, `perg` CLI and `pergamenum-mcp` targets; unit suite 1951/1951 green.
- [x] `PG-048` **P3** `contentCounts` couldn't distinguish "folder is empty" from "folder could not be read", and the destructive delete dialog showed the result as if it were always accurate — `Sources/Vault/FolderFileOperations.swift:66` (`walk`), `Sources/Features/Workspace/WorkspaceBrowser.swift:338` (`confirmDelete`) <!-- src:review opened:2026-08-25 closed:2026-08-31 runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. Verified live before starting: every claim held exactly as written, only the line numbers had drifted (`walk` at `:79`, `confirmDelete` at `:338`) — and a second call site the ticket didn't name, `Sources/Features/Editor/NoteListPane+FolderVerbs.swift:79` (`deletingFolderCounts`), had the identical bug for the Note sidebar's own delete dialog. Fix: `walk` now returns `(notePaths: [String], subfolders: Int)?`, nil when the target isn't a real, existing directory or its enumerator can't be built; `contentCounts` maps that through as `(notes: Int, subfolders: Int)?`. Both dialogs (`PendingWorkspaceDelete.message` in `WorkspaceBrowser.swift`, `deletingFolderCounts` in `NoteListPane+FolderVerbs.swift`) drop their `?? (notes: 0, subfolders: 0)` fallback and word the dialog without counts when unreadable, naming the uncertainty rather than claiming zero — chosen over refusing the delete outright, since an unreadable enumeration doesn't mean the directory can't be trashed, and `trashFolder` already reports its own failure separately if the delete itself then fails. The three internal `walk` callers that run only after a plan/existence check already confirmed the folder is there (`renameFolder`, `trashFolder`, `FolderFileOperations+Move.moveFolder`) unwrap with `?? []`, documented as unreachable in practice. New tests in `Tests/FolderFileOperationTests.swift`: `contentCountsReturnsNilForAFolderThatDoesNotExist`, `contentCountsReturnsZeroNotNilForARealEmptyFolder` (the assertion that gives the change its meaning — a real empty folder must still read as `(0, 0)`, not `nil`). 1939/1939 unit tests green.
- [x] `PG-047` **P3** The "create workspace" verb (as originally named) is now three call sites, all calling `CanvasStore.createFolder(named:in:)` directly, which only checks `fileExists` — no name-rule validation of its own — `Sources/Vault/CanvasStore.swift:220` <!-- src:review opened:2026-08-25 closed:2026-08-31 runs:1 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0022. Verified live before starting: `createWorkspace` no longer exists under that name (folder creation was reshaped since the ticket was opened). Of the three current call sites, two are already safe — `WorkspaceView+FolderVerbs.swift:103` (Workspace toolbar) and `NoteListPane+FolderVerbs.swift:90` (Note sidebar toolbar) both run through `WorkspaceFolderSheets`'s `NewFolderSheet`, which already calls `FolderFileOperations.validate` and disables "Crea" until the name is clean. The third, `WorkspaceController.swift:716` (the canvas "Nuova cartella" *tool*, via `WorkspaceView+Creation.swift:64`), is driven by `NewCanvasItemSheet` (`BoardSheets.swift`), whose "Crea" only disables on an empty trimmed string — no `NoteName`/`FolderFileOperations` check at all. The ticket's "not reachable today" premise no longer holds for this one surface: a name containing `/` or another forbidden character reaches `CanvasStore.createFolder` today, unguarded. Fix: added a validating `FolderFileOperations.createFolder(named:in:)` (matches the existing `renamePlan` idiom — `Self.validate` then `FileOperationError.invalidTitle` on failure) and routed `WorkspaceController.createFolder` through it instead of `store.createFolder` directly. The two already-safe call sites were left unchanged on purpose — routing them too would be a redundant second validation pass on an already-known-good name, not what this ticket's live gap required. Related NIT (`FolderFileOperations.OperationError` duplicating `NoteFileOperations.OperationError`) was already resolved by PG-066's `FileOperationError` unification — confirmed live, nothing left to do. New test `Tests/FolderFileOperationTests.swift` (`createFolderRejectsAnInvalidNameAndCreatesNoDirectory`) covers the new validation. 1937/1937 unit tests green.
- [x] `PG-061` **P2** `BoardFileOperations.renamePlan` re-derived `WorkspaceBoardResolver.matches`'s lowercased-path comparison independently instead of calling the shared helper — `Sources/Vault/BoardFileOperations.swift` (the ADR-0022 §D4 relocated ambiguity guard) <!-- src:review opened:2026-08-27 closed:2026-08-31 runs:2 -->
  - Found at review. Verified live before starting: the guard's `sharing` filter still re-derived `($0 as NSString).lastPathComponent.lowercased() == oldName.lowercased()` inline, byte-identical to what `WorkspaceBoardResolver.matches(_:workspacePath:)` already does. Fix: the filter closure now calls `WorkspaceBoardResolver.matches($0, workspacePath: oldName)` directly — no new type, no signature change, `WorkspaceBoardResolver` already visible from `Sources/Vault/` (single module). Existing test `Tests/BoardFileOperationsTests.swift` (the "two boards share a name" ambiguity-guard case) already exercises this path and needed no change. 1936/1936 unit tests green.
- [x] `PG-070` **P3** `RenameOutcome.rewrittenPaths` was computed on every successful board rename but never consumed — `Sources/Vault/BoardFileOperations.swift`, `Sources/Vault/VaultController+Folders.swift` (`renameBoard` only reads `.failures`) <!-- src:review opened:2026-08-27 closed:2026-08-31 runs:2 -->
  - Found at review. Verified live before starting: only caller (`VaultController+Folders.renameBoard`) still reads only `.failures`; board rename is not exposed to connectors (ADR-0022 §D11, a deliberate decision not reopened here), so no reader exists or could exist without inventing new UI feedback the app's rename convention (note/folder/board alike, all silent-success/loud-failure) does not have anywhere else. Fix: dropped `rewrittenPaths` from `RenameOutcome` and its two append sites. Scope extended (user choice, `AskUserQuestion`) to the identical dead field found during verification on the neighboring `BoardFileOperations.MoveOutcome.rewrittenPaths` (`moveBoard`, consumed by `VaultSession+Move.swift`'s `.board` case which also reads only `.failures`) — same file, same defect, dropped too rather than left inconsistent. Left out of scope on purpose: `FolderFileOperations.RenameOutcome`/`+Move.MoveOutcome` carry the identical unread pattern (folder rename/move is also not exposed to connectors, ADR-0022 §D9) but neither is named by this ticket and both already have live test assertions on the field — a separate ticket's decision. No test changes: zero existing assertions on Board's `rewrittenPaths` anywhere (unlike the Note/Folder siblings), nothing to update for a field removal with no behavior change. 1936/1936 unit tests green.
- [x] `PG-067` **P3** Closed as declined — the suggested fix cannot be expressed in Swift without a larger, arguably regressive restructuring. Claimed: `WorkspaceController`'s gesture-transient state is plain `var`, not `private(set)` — `Sources/Features/Workspace/WorkspaceController.swift:428-490` (drag/resize/marquee/pan cluster: `draggingIDs`, `dragTranslation`, `dragAnchorID`, `activeGuides`, `panOrigin`, `marqueeStart`, `marqueeRect`, `resizingNodeID`, `resizeHandle`, `resizeOriginalFrame`, `resizedFrame`) and `:498-504` (crop cluster: `croppingNodeID`, `cropHandle`, `cropOriginal`, `cropDraft`, `cropDrawnSize`) <!-- src:review opened:2026-08-27 closed:2026-08-31 runs:2 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0025. Verified live before starting: both clusters are confirmed exclusively mutated by their own extension file (`WorkspaceController+Gestures.swift`, `WorkspaceController+Crop.swift` respectively — grepped across all of `Sources/` and `Tests/`, no other writer anywhere), so the ticket's underlying observation holds. But Swift extensions cannot declare stored properties — a stored property must live in the primary type declaration, `WorkspaceController.swift` itself. `private(set)` restricts write access to code in the *same file* as the declaration, so applying it there would block the very extension file that needs to mutate the state every frame of a drag, exactly as much as it blocks every other file. `WorkspaceController+Crop.swift`'s own doc comment already names this constraint: "owned here for the same reason `WorkspaceController+Gestures`'s resize state is - an extension cannot hold stored properties." The only way to make `private(set)` work would be moving the mutating methods out of `+Gestures.swift`/`+Crop.swift` and back into `WorkspaceController.swift` itself — which directly fights PG-056 (bring `WorkspaceController.swift` under the `type_body_length` limit, Phase 6) and undoes this codebase's deliberate per-concern file-per-extension split (`+Drawing`, `+Duplicate`, `+Gestures`, `+Import`, `+Tools`, `+Viewport`, `+Crop`). A third, superficially similar pair immediately below the crop cluster, `editingTextNodeID`/`editingTextDraft` (`:508-512`), was checked and confirmed genuinely out of scope regardless of this constraint: `editingTextDraft` is a live two-way SwiftUI `Binding` target (`StickyTextCard.swift:54`, `Bindable(workspace).editingTextDraft`) and is written directly by a test (`Tests/CanvasTests.swift:577`) to simulate typing — an actual external-write requirement, not caller-discipline debt. No code change. User confirmed via `AskUserQuestion`: decline with full reasoning rather than implement a fix that regresses a sibling ticket, or force the file split apart.
- [x] `PG-069` **P3** Closed as not-a-bug — the entry's premise did not hold up. Claimed: `allBoards() ?? []` degrading conservative vs. `boardNameIsAvailable ?? true` degrading optimistic is an inconsistency to unify — `Sources/Features/Workspace/BoardCardMenu.swift`, `BoardChrome.swift`, `WorkspaceBrowser.swift` <!-- src:review opened:2026-08-27 closed:2026-08-31 runs:2 -->
  - Found at review. Analyzed fully against live code rather than reflexively implementing or rejecting (per the standing "an unaddressed gap is not grounds to refuse a request" rule): the two fallbacks answer structurally different questions, and each default is independently correct for its own question, not a drift between two answers to the same question. `allBoards() ?? []` (`BoardChrome.swift:57`, `BoardCardMenu.swift:197`, and a fourth un-named site `WorkspaceBrowser.swift:598`) feeds `WorkspaceBoardResolver.board(inFolder:among:)`, whose job is "which board does this folder/marker unambiguously mean" — an empty candidate list on a nil store safely yields `.notFound`, which falls back to selecting the folder rather than guessing a board; conservative is the only safe default there. `boardNameIsAvailable ?? true` (`WorkspaceBrowser.swift:308`) only gates the creation sheet's «Crea» enabled state, and its own doc comment says so explicitly — the real refusal happens at the write path (`createBoard`/`createFolder`), so a stale/optimistic "available" here costs at most a missed early disable, never an actual collision (ADR-0022 §D11). Confirmed this is not even an asymmetry within its own question class: the sibling folder-name check on the same line range, `nameIsAvailable ?? true` (`WorkspaceBrowser.swift:288`), uses the identical optimistic default. Forcing both fallback directions to match would introduce a real regression at whichever site changed (permanently disabling `.notFound`'s safe folder-fallback into a guess, or permanently disabling «Crea» whenever `canvasStore`/`workspace.store` is momentarily nil, for no correctness benefit). No code change. User confirmed via `AskUserQuestion`: close as not-a-bug rather than add cosmetic comments or force a shared default.
- [x] `PG-068` **P3** `BoardCardMenu.copyLink(to:)` silently returned on a `PergamenumLink.canvas(...)` miss, no `recordProblem` — `Sources/Features/Workspace/BoardCardMenu.swift` (`copyLink(to:)`) <!-- src:review opened:2026-08-27 closed:2026-08-31 -->
  - Found at review. Verified live before starting: guard still a bare `return`, at the claimed location. `PergamenumLink.canvas` only returns nil if `URLComponents.url` fails to build — practically unreachable with today's fixed scheme/host, but the board path is a Finder-controlled external input, so the defensive guard itself is correct; only the silent consequence was the bug. Fix: the guard's else-branch now calls `workspace.recordProblem("copia link: impossibile generare l'URL per \(workspace.board)")` before returning — the same `recordProblem` idiom every other recoverable failure in `WorkspaceController` already uses (`+Drawing.swift`, `+Import.swift`, `WorkspaceView.swift:243`). No new test: the nil branch cannot be reached deterministically without adding a seam to `PergamenumLink.canvas` purely to force a failure that cannot occur in practice, disproportionate for a P3 defensive-path fix. No signature/behavior change on the success path, 1936/1936 unit tests green.
- [x] `PG-079` **P3** `CardCommand`'s "plain button vs. argument-taking submenu" split was re-derived by three independent exhaustive switches instead of a property on the enum; the only safety net was a DEBUG-only `assertionFailure`, a silent dead button in Release if a case was ever mis-wired — `Sources/Features/Workspace/BoardCardControls.swift`, `Sources/Features/Workspace/BoardCardMenu.swift` <!-- src:session opened:2026-08-29 closed:2026-08-31 -->
  - Found at Gate 5.06 (type-design-analyzer). Verified live before starting: the three switches (`BoardCardControls.control`, `BoardCardMenuItems.item`, `BoardCardActions.run`) all still existed; also found `.crop` is not symmetric across the two surfaces (bar collapses `.crop`+`.removeCrop` into one dropdown, context menu keeps both as top-level entries) — a bar-layout choice, not the "carries an argument" property, so it stays a per-surface case rather than joining the new property. Fix: new `var carriesArgument: Bool` on `CardCommand` (`Sources/Features/Workspace/CardCommand.swift`), true for exactly `.color`/`.textColor`/`.textAlign`/`.foldHeadings`/`.resize`; a shared `BoardCardMenuItems.argumentItems(for:node:actions:)` factory replaces the two identical five-case submenu-content switches; `BoardCardActions.run` now guards on `carriesArgument` instead of enumerating the five cases before its `assertionFailure`. New test `onlyTheFiveArgumentCarryingCommandsAreClassifiedAsSuch` in `Tests/CardCommandTests.swift` pins the classification by assertion. No signature/behavior change, 1936/1936 unit tests green.
- [x] `PG-078` **P3** "Nota vs Testo" (`node.color != nil`) was re-implemented in three files with no shared accessor; the duplication had already caused the Task 6 pill-alignment bug once — `Sources/Features/Workspace/StickyTextCard.swift:25`, `Sources/Features/Workspace/BoardContentLayer.swift:187`, `Sources/Features/Workspace/BoardFormatBar.swift:218` <!-- src:session opened:2026-08-29 closed:2026-08-31 -->
  - Found at Gate 5.06 (type-design-analyzer) for ADR-0027. Verified live before starting: all three call sites unchanged. Fix: new `var isNote: Bool { color != nil }` on `CanvasNode` (`Sources/Core/Canvas/JSONCanvas.swift`, next to the existing `isGroup` — same idiom, a semantic Bool over the node's own state), the three call sites switched to it. The one non-boolean use (`StickyTextCard.swift`'s `if let color = node.color` unwrap, which needs the value) was left as-is on purpose. No signature/behavior change, 1935/1935 unit tests green.
- [x] `PG-071` **P3** `WorkspaceFolderActions` mixed pure static navigation-rule functions with eight DI closures in one struct — `Sources/Features/Workspace/WorkspaceFolderActions.swift:19-149` <!-- src:review opened:2026-08-27 closed:2026-08-31 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0025 (`workspace-folder-board-separation`). Verified live before starting: 8 DI closures and 5 pure static functions, both counts updated from the entry's "six"/unnamed count. Fix: new `WorkspaceFolderNavigation` enum (pure namespace, no cases) holding the 5 static functions moved verbatim, same file; `WorkspaceFolderActions` now holds only the 8 closures. Both call sites (`WorkspaceView+FolderVerbs.swift`, `Tests/WorkspaceFolderNavigationTests.swift`) already called the static functions only via `WorkspaceFolderActions.<func>`, never through an instance, so the split needed no logic change — a mechanical rename at every call site. No signature/behavior change, 1935/1935 unit tests green.
- [x] `PG-081` **P3** Breadcrumb rendering was duplicated: `VaultController.breadcrumb` and `WorkspaceController.breadcrumb` declared byte-identical anonymous tuples `(title: String, folder: String)`, and `VaultTopBar`/`BoardChrome.BoardTopBar` independently re-implemented the same rendering loop over them — `Sources/Vault/VaultController+Notes.swift:183`, `Sources/Features/Workspace/WorkspaceController.swift:231` <!-- src:session opened:2026-08-29 closed:2026-08-30 -->
  - Found at Gate 5.06 (type-design-analyzer) for ADR-0026. Both sites and both rendering loops verified live before starting, unchanged from the entry. Fix: new `BreadcrumbSegment: Equatable, Sendable` type and one shared `BreadcrumbBar` view (`Sources/DesignSystem/BreadcrumbBar.swift`), both controllers now return `[BreadcrumbSegment]`, both top bars render through the shared view. Extended scope beyond the suggested fix (user choice): the Workspace side's `BoardTopBar` now also carries `accessibilityIdentifier("breadcrumb-crumb-N")` per segment — previously only the Note side had it — and the two `WorkspaceOpenStateUITests` that located the ancestor breadcrumb segment by folder name now use the identifier instead (CLAUDE.md's "never find a control by the words on it"). No index/schema change. 1935/1935 unit test verdi. UI-test verification (`scripts/uitests.sh WorkspaceOpenStateUITests`) still owed by the user.
- [x] `PG-064` **P3** `BoardFileOperations`'s rename/trash/move catch blocks used `error.localizedDescription` instead of `\(error)`, inconsistent with the rest of the chain — `Sources/Vault/BoardFileOperations.swift:153,196,276` <!-- src:review opened:2026-08-27 closed:2026-08-30 -->
  - Found at Gate 5.06 (silent-failure-hunter) on ADR-0025. Lost NSError domain/code detail on `FileManager` failures that every other error site touched by this chain preserves. Verified live before starting: three call sites (rename, trash, move), not two as the entry implied by naming only "rename/trash". Fix: all three interpolations changed to `\(error)`, matching `FolderFileOperations.swift`/`FolderFileOperations+Move.swift`/`VaultSession+Move.swift`/`NoteFileOperations.swift`. No signature change, no test change (no test asserted on the exact string). 1935/1935 unit test verdi.
- [x] `PG-046` **P3** Six Folder/Board-layer plan/outcome types lacked `Equatable, Sendable`, unlike every `NoteFileOperations` sibling — `Sources/Vault/FolderFileOperations.swift`, `Sources/Vault/FolderFileOperations+Move.swift`, `Sources/Vault/BoardFileOperations.swift`, `Sources/Vault/VaultSession+Move.swift` <!-- src:session opened:2026-08-25 closed:2026-08-30 -->
  - The entry's two named facts were both stale (verified before starting): the `boardRename` tuple field on `FolderRenamePlan` no longer exists — removed by ADR-0025 — and the `movedNotes` tuple was already replaced by the `MovedNote` struct in this session's `PG-080` chain. The underlying observation survived: `FolderRenamePlan`, `FolderFileOperations.RenameOutcome`, `FolderFileOperations+Move.MovePlan`/`MoveOutcome`, `BoardFileOperations.MovePlan`/`MoveOutcome`, and `VaultSession+Move.MoveBatchOutcome` all still lacked `Equatable, Sendable`. Scope extended (user choice) from the two originally-named types to all six plus `MovedNote` itself, closing the disparity everywhere in one pass. Pure additive-conformance change — every field on all six types was already `Equatable`/`Sendable`-compatible, so no field or test file needed to change. 1935/1935 unit test verdi.
- [x] `PG-080` **P3** Two near-identical `OperationError` enums (`BoardFileOperations`, `FolderFileOperations`) with duplicated case names and description strings, and the `(old: String, new: String)` "moved note" tuple repeated across three outcome types (`RenameOutcome`, `MoveOutcome`, `VaultSession.MoveBatchOutcome.movedNotes`) — `Sources/Vault/BoardFileOperations.swift:31`, `Sources/Vault/FolderFileOperations.swift:22`, `Sources/Vault/VaultSession+Move.swift:31` <!-- src:session opened:2026-08-29 closed:2026-08-30 -->
  - Found at Gate 5.06 (type-design-analyzer) for ADR-0026. The `OperationError` half was already resolved by `PG-066`, verified before this chain started. The moved-note tuple half turned out to cover a *third* site beyond the two the entry named: `FolderFileOperations+Move.swift`'s `MoveOutcome`, found by grep and confirmed to carry the identical anonymous shape. Fix: new `MovedNote` struct (`old`/`new` fields, no `Equatable` — verified no equality comparison anywhere) at `Sources/Core/Vault/MovedNote.swift`, replacing the tuple in all three declarations (`RenameOutcome`, `MoveOutcome`, `MoveBatchOutcome`) and their three construction sites; every read/test site compiled unchanged since field names and access patterns (`.old`/`.new`, `\.old`/`\.new`) are identical against a named struct. 1935/1935 unit test verdi.
- [x] `PG-066` **P3** `OperationError` was declared identically three times — `Sources/Vault/NoteFileOperations.swift`, `FolderFileOperations.swift` (per `PG-047`'s related NIT), and `BoardFileOperations.swift` <!-- src:review opened:2026-08-27 closed:2026-08-30 -->
  - Found at Gate 5.06 (type-design-analyzer) on ADR-0025. Verified this session before implementing: `NoteFileOperations`'s and `BoardFileOperations`'s `OperationError` enums were byte-for-byte identical; `FolderFileOperations`'s carried one extra case, `wouldNest(String)` (ADR-0026 §D1/§D5). The `RenamePlan`/`RenameOutcome` half of the original claim was checked and rejected: the three types are structurally similar but not semantically identical (`NoteFileOperations.RenamePlan` has no separate outcome type, `FolderFileOperations.RenameOutcome` carries a `movedNotes` field the other two don't, and each type's doc comments describe deliberately different per-file-kind rewrite behavior) — deduplicating them would conflate three distinct concerns, out of scope. Fix: new top-level `FileOperationError` at `Sources/Core/Vault/FileOperationError.swift` (reachable from both connectors via the existing `Sources/Core/**` `sharedSources` glob), replacing all three nested enums; `wouldNest` kept as a case on the shared type, never constructed by Note/Board. 1935/1935 unit test verdi.
- [x] `PG-083` **P2** `VaultController.moveItems` restituiva solo `Bool`, scartando `VaultSession.MoveBatchOutcome` — un batch multi-elemento fallito su disco per più elementi mostrava solo l'ultimo problema (`vault.problems.last`), contraddicendo il doc comment di `WorkspaceMoveConflict` ("every reason, not the first"); e `WorkspaceView+FolderVerbs.moveItems(_:into:)` calcolava il path d'atterraggio dal piano pre-scrittura anziché dall'esito reale, così un fallimento parziale sulla board aperta poteva provare a leggere un path mai scritto — `Sources/Features/Workspace/WorkspaceView+FolderVerbs.swift:36-69`, `Sources/Vault/VaultController+Move.swift:27-37` <!-- src:session opened:2026-08-29 closed:2026-08-30 -->
  - Trovato al Gate 5.06 (silent-failure-hunter) per ADR-0026. Fix: `VaultController.moveItems` ora restituisce l'intero `MoveBatchOutcome` (nuovo `didMove`) invece di `Bool`; `WorkspaceView+FolderVerbs.moveItems` non ripete più `VaultMoveBatch.plan` per conto proprio, usa `outcome.refusals + outcome.failures` per il dialogo e `outcome.moves` (non il piano) per l'atterraggio; `NoteListPane.performMove` idem, unendo i motivi in una riga sola. La guardia "nota non salvata" (`canOperate(onAll:)` → `refusal(forAll:)`) ora riempie `refusals` invece di lasciare solo una riga su `problems`. Tre test nuovi in `Tests/VaultMoveTests.swift` coprono: due falliti nello stesso batch nominati entrambi, un rifiuto del piano che arriva nell'esito del controller, la guardia nota-non-salvata che riempie `refusals`. 1935/1935 unit test verdi.
- [x] `PG-087` **P2** La suite unit scriveva sulla pasteboard reale del Mac: `CommandActions.copyLinkToOpenNote()` usava `NSPasteboard.general` cablata invece di una dipendenza iniettata, e `RowCommandTests.runningCopyLinkOnAClosedRowOpensItThenPutsItsLinkOnThePasteboard` la esercitava, così ogni run lasciava `pergamenum://note?file=b.md` negli appunti dell'utente — `Sources/App/CommandActions.swift:422-426`, `Tests/RowCommandTests.swift:93-105` <!-- src:manual opened:2026-08-30 closed:2026-08-30 -->
  - Segnalato dall'utente vedendolo ricomparire di continuo nel gestore clipboard (Paste), perché `.claude/test-cmd` gira a ogni fine turno tramite l'hook Stop. Stessa classe che `PG-006` cita già come motivo per non automatizzare l'incolla-immagine, e identica a quella che `RecentVaults.volatile()` (`Sources/Vault/RecentVaults.swift:36`) risolse per la lista vault. Fix: nuovo seam `NSPasteboard.volatile()` (`Sources/App/NSPasteboard+Volatile.swift`) + parametro `pasteboard: NSPasteboard = .general` iniettabile su `CommandActions.init`; il test passa `.volatile()` e asserisce su quella invece che su `.general`. `pastePlain()` resta deliberatamente su `.general` (la catena dei responder incolla sempre da lì). 1932/1932 unit test verdi, `pbpaste` confermato invariato dopo la run.
- [x] `PG-004` **P2** `.pergamenum/` synced in iCloud with no exclusion anywhere in the codebase, and produced conflict copies; deciding what moves out of the vault needed an ADR — `Sources/Vault/VaultState.swift` <!-- src:manual opened:2026-08-16 closed:2026-08-22 -->
  - Closed by ADR-0017 (`docs/adr/0017-the-derived-stores-leave-the-vault.md`, PR #82), three slices on `feature/pg-004-state-outside-vault`. **Slice 1**: `VaultState`, the vault id in `settings.json`, `cache.db` and `thumbnails/` moved to Application Support by delete-and-rebuild, D4's backup exclusion, the test-isolation seam for both the unit and UI suites. **Slice 2**: `history/` and `ai-journal/` move rather than delete, the duplicated-vault check, `NoteHistory`/`WriteJournal` take an injected directory. **Slice 3**: connector parity — `VaultResolution.session` and `VaultAPI.journalLog` were already resolving `VaultState.applicationSupportBase()` as of slices 1-2, so this slice was verification, not new code: 1269/1269 unit tests, both `perg` and `pergamenum-mcp` build, `scripts/mcp-smoke.py` all green (its own journal/undo checks confirm the wiring in a throwaway vault), and a hand check on the live iCloud vault (`.../Vaults/Pergamena/`) — a real `note new` through `perg` landed `ai-journal/` and `history/` in the Application Support state directory under the same `vaultID` the app already resolved, `.pergamenum/` stayed at just `settings.json` and `vocabolari.json`, and the test note was trashed and the scratch folder removed afterward.
  - The SPEC amendment (§6.5, §12, the §85 tree) is filed as debt on `PG-015`, per ADR-0017 §D5 — not blocking this closure.
- [x] `PG-003` **P2** M9's version snapshots add a second write-time store beside `WriteJournal`: disposable, but state the vault did not have before <!-- src:session opened:2026-08-16 closed:2026-08-21 -->
  - Folded into `PG-004` rather than resolved on its own: `.pergamenum/history/` carries the exact same iCloud-conflict exposure already tracked there for `cache.db`, so one ADR should decide the scope of all of `.pergamenum/`, not each store separately.
- [x] `PG-005` **P2** `NoteFileOperations` did not go through `VaultSession.write`, so the journal covered only part of a link rewrite and neither connector could offer `note rename|move|trash` — `Sources/Vault/NoteFileOperations.swift` <!-- src:manual opened:2026-08-16 closed:2026-08-21 -->
  - Closed by ADR-0016 (`docs/adr/0016-the-journal-records-a-gesture.md`), five commits on `feature/m13-vault-entire`, not yet merged to `main`: the journal gains a gesture vocabulary (`operation`, `kind`, `pathBefore`, all optional so no migration is written); `transaction`/`moveFile`/`trashFile`/`writeFile` in `VaultSession+Journal.swift`; `NoteFileOperations` splits into a pure plan and a performing half, mirroring `tagRenamePreview`/`renameTag`, so every existing `NoteFileOperationTests` assertion kept its subject; `undo(operation:)` and `undoJournalledWrites` both pre-flight the whole group and refuse all-or-nothing rather than reversing what they can; both connectors get the three verbs, `dryRun` defaulting to true on MCP.
  - `PG-019` (dragging a section in the outline) stays open on purpose: the plan flagged it as a different class of work - a new UI gesture, not a fix - and it gets its own ADR and mockup rather than being designed inside this one.
- [x] `PG-027` The index and the inspector stop describing a note the composer is covering (`5abbae4`): `newNote` meant both "a draft exists" and "the composer is on screen", so opening a note left it underneath. Split into `noteDraft` and `isComposingNote`, with `isOpenNoteVisible` the one rule both panes read. The on-screen check then found the half no unit test could reach — the list's selection is derived from `openNote`, and SwiftUI runs a selection binding's setter only on a change, so clicking the covered note was no change at all (`d8a67b6`) (2026-08-19)
- [x] `PG-028` A typed title survives stepping out of the composer (`5abbae4`): navigating away parks the title, the folder, the topic and the template for the next `Cmd+N`, while «Annulla», Escape, the × and «Crea» all discard through `endNewNote()`. The guard in `parkNewNote` is what makes the order of SwiftUI's `onDisappear` against the button's own action stop mattering. Only a draft with a title is parked, which is the same threshold the parking uses and is what kept `aNewNoteStartsInTheFolderItWasAskedFor` true (2026-08-19)
- [x] `PG-009` M8 Menu comandi ed editor complete, thirteen slices (PR #62, `7d19baa`): slash menu, code fence highlighting, the note's index, heading folding, transclusion in both surfaces, one `CompletionPanel` for every trigger (PG-023), spell check, emoji completion, find/replace with regex, and the floating format bar last — bold/italic/strikethrough/code plus wikilink/link over a selection, its `PanelPlacement` shared with `CompletionPanel` rather than duplicated. Two AppKit defects only found on screen: `NSPanel.hasShadow` ringed the capsule because its native shadow is computed from the window's own rectangular backing store, and a `.clear`-background `Button` was clickable only on its own glyph without an explicit `contentShape`. Left over: `PG-019`, filed under `PG-005` (2026-08-18)
- [x] `PG-023` One panel draws every completion (PR #57, `6625362`): titles, sections, tags and commands all on `CompletionPanel`, which sits under the caret, flips above the line when there is no room, and never crosses the line being typed into — a clamp that held it inside the screen was the first attempt and put it on top of the text (2026-08-18)
- [x] `PG-025` Typing at the end of a note brings the caret into view: the note grows taller on that very keystroke, so the scroll has to come after the growth and not before (`b1b2dfa`, 2026-08-18)
- [x] `PG-024` The editor's text view now grows to what styling makes it draw — 1431 points needed against the 1244 given, so the last 187 were outside the scroll view's reach and the note looked like it stopped at its final heading. Three mechanisms lie: `sizeToFit()` does nothing, `layoutSubtreeIfNeeded()` works in a hand-built test and not in the app, `setFrameSize` works and empties the diary by re-entering SwiftUI's update pass. `layoutViewport()` leaves the resizing to AppKit (`b1b2dfa`, pre-existing on main, 2026-08-18)
- [x] `PG-021` The fold badge opens its own section: the click hit test the transclusion slice built (`onClickInMargin` plus a rectangle the fragment reports) turned out to be exactly what the badge was waiting for (2026-08-17)
- [x] `PG-017` ADR-0010 written and accepted the same day: transclusion is a view of another note, both surfaces draw it, depth one, and a transcluded note counts as a link — the editor half of §D3 is what is left, and it lives in PG-009 (2026-08-17)
- [x] `PG-010` M9 complete: ADR-0011 implemented end to end in three slices — the `NoteHistory` store and its write hook, the restore sheet with its inspector section and `⌘⇧H`, and templates as notes under `Templates/` with `{{date}}`/`{{title}}` (2026-08-18)
- [x] `PG-020` `![[nota]]` no longer claims a file is missing: a note embed is a note, drawn where it stands, and the message names whichever of the two was actually looked for — closed by the transclusion slice rather than patched (2026-08-17)
- [x] `PG-022` M8's first five slices merged: the slash menu and its panel, code fence highlighting, the note's index, and heading folding — every one mockup-first and verified by hand (2026-08-17)
- [x] `PG-001` ADR-0009 accepted after five corrections from Stefano: `has()` defined, `render: board` without `group` is a parse error, the zero and multi `status-*` cases on the board decided, and the borrowed 150 ms replaced by a real measurement — M11 is unblocked (2026-08-17)
- [x] `PG-008` M7 Cattura globale complete: the connector's `capture` (PR #40) and the global panel (PRs #41), verified by hand over another app and over a full-screen one (2026-08-17)
- [x] `PG-016` ADR-0008 accepted and the capture panel's mockup approved (2026-08-17)
- [x] `PG-007` ADR-0008, the global capture panel, written and proposed (2026-08-16)
